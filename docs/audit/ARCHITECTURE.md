# Architecture audit

Scope: the structure of the gamehandler workspace — the layering the project
states and whether it holds, module organisation against the sizes the tree
actually has, duplication, abstraction quality, the error-handling architecture,
testability, and whether the codebase's unusually dense explanatory comments are
accurate and load-bearing or have drifted. The subject is both crates —
`crates/core` (`gamehandler-core`) and `crates/app` (`gamehandler`) — plus the
gate scripts and the reference Python implementation under `gamehandler/` where
a finding's answer turns on parity.

The layering claim is `DECISIONS` D-03: `crates/core` is pure, synchronous and
GUI-free, and must never depend on libcosmic or iced. That claim was verified
mechanically rather than assumed (see "Verified sound"). The parity claim is
D-01: the Rust port is measured against the Python 3 + PySide6/QML reference, so
where a defect exists in both, this document says so — a faithful copy of a
reference bug is a different finding from a port-introduced regression, and the
distinction decides whether the fix is a divergence or a bug fix.

It was performed by reading the tree at commit `d56782d`. The reconnaissance
pass was read-only; the fixes it produced landed separately on branch
`audit-hardening` and each row below that has one carries a **Status** tail.

This audit inherits one rule from `docs/audit/BASELINE.md:193-200`, adopted there
after a fabricated observation was recorded and withdrawn: **text read off an
image is not evidence.** Every claim below is either a line read from source, or
a count produced by running something and reading the raw exit status. Where a
prior report's claim did not survive re-measurement it is marked withdrawn rather
than repeated.

The comment findings are grouped last but are not the least of them. This
codebase's comments are load-bearing: several of its most confident paragraphs
are the *only* record of why a decision was made, and the project has already
been burned by a comment that confidently stated something false. The findings
below are therefore not "the comments are verbose" but "these specific
paragraphs are false, and here is the measurement".

## Findings

| ID | Severity | Finding | Evidence (file:line) | Why it matters | Recommendation |
|---|---|---|---|---|---|
| ARCH-01 | P0 | **A library that fails to load is indistinguishable from an empty library, and the next save writes the empty state over the file.** `Library::load_at` clears the in-memory games and returns early on a read failure or a parse failure; the struct has no field recording that a load was attempted or failed, so the caller cannot tell "no games" from "could not read your games". Any subsequent mutation persists the empty library. The same collapse exists in `Settings::load`, where the read and the parse both fall to `.ok()`. **This is faithful parity, not a port regression** — Python catches `JSONDecodeError`/`OSError`/`UnicodeDecodeError`, sets `raw = []`, and `Library.save` then writes that empty list — and the port also reproduces Python's atomic save. | Collapse: `crates/core/src/models.rs:568-583` (`load_at`), struct with only `path` and `games` at `:519-528`, doc at `:556-566` stating the tolerance as intent. Settings: `crates/core/src/settings.rs:185-196`. Atomic save reproduced: `crates/core/src/json.rs:355-368`. Reference: `gamehandler/models.py:128-147` (catch → `raw = []`, then `save` writes it), atomic save at `:141-147` | The single highest-value defect in the tree. A truncated or partially-written `games.json` — the ordinary outcome of a full disk, a killed process, or a hand-edit — presents as an empty library, and the next add or delete destroys the user's data with no error shown anywhere. Because it is inherited, a fix is a deliberate divergence from the reference and must be recorded as one. | Add a load status to `Library` (`Loaded` / `Missing` / `Unreadable(reason)`) and refuse to persist while the state is not `Loaded`; surface the reason through the existing toast channel. Do the same for `Settings`. Record the divergence next to the existing doc paragraph at `models.rs:556-566`, which currently documents the tolerance as intentional without noting that it is lossy. **Status: FIXED with `BUG-01`.** The load status is now carried in the struct rather than inferred from an empty games list, and the writer refuses to replace a file it could not read; the `LoadStatus` unit tests plus a live `--list` against a truncated `games.json` hold it. |
| ARCH-02 | P1 | **The `view` layer's stated contract is false, and the code that violates it is the code the contract was written to describe.** `view/mod.rs` states that nothing in the layer reads `State` and nothing emits a `Message`, with the widget functions generic over the message type. Two page modules take `&mut State` and match on concrete `Message` variants, and a third spawns an OS process from a view function. | Contract `crates/app/src/view/mod.rs:5-11`. Violations: `crates/app/src/view/installers.rs:619` (`fn update(state: &mut State, message: Message)`), `crates/app/src/view/runners.rs:921` (same signature, 186 lines), `crates/app/src/view/plugins.rs:183-215` (`run_to_completion` spawning a real `std::process::Command` from the view layer, reached from `plugins::install_command` at `:222`). A doc-link in the same file also resolves to the wrong target: `crates/app/src/view/mod.rs:38` | A contract that is stated and then contradicted by the two largest view files teaches a reader to distrust the file that is supposed to define the layer boundary. Concretely: `plugins::run_to_completion` means the view layer performs I/O and process spawning, so a view can block and can fail for reasons no view test can observe. | Either enforce the contract (move `plugins`' process spawn behind a `Message` handled in `main.rs`, and move the two page `update` bodies into `Shell::update` or a `state`-side handler) or rewrite `view/mod.rs:5-11` to describe the two-layer split the code actually has. Do not leave the sentence as-is. Fix the `:38` doc-link while there. **Status: FIXED, by rewriting the docs — and the alternative was priced first.** Enforcing the contract would mean moving `plugins`' `std::process::Command` behind a `Message` in `main.rs` and folding the two page `update` bodies into `Shell::update`, which deletes the page-module structure every screen in the app uses and contradicts the decision that this layer runs blocking work off the UI thread (`view/runners.rs` does the same for its downloads). `view/mod.rs` now describes the split the code has: a three-tier table naming the pure-layout modules (`a11y`, `badge`, `widgets`), the pure-decision modules (`cover`, `cover_cache`, `meta`, `metrics`) and the seven page modules, with what each tier may touch, and `plugins`' spawn is recorded as a deliberate seam rather than a leak — `core` describes the command as *data* (`plugins::install_command`, `plugins::privileged_command`) and this layer runs it off the UI thread. The old claim is quoted and marked false rather than quietly deleted. **The part that matters is not the rewrite.** The claim was wrong for a long time and *nothing noticed*, because prose nothing reads back cannot be wrong. `the_layer_split_the_docs_describe_is_the_split_the_code_has` now reads the table back out of the source: every module `view/mod.rs` declares must be classified in exactly one tier, the two pure tiers may contain no `&State` and no `crate::Message`, and the page modules must contain one or the other — so the list cannot be padded to make the check pass. It was mutation-proved three times; the third probe — `fn sneaky(state: &crate::state::State)` appended to the pure `metrics.rs` — initially *passed*, because the matcher looked for the literal `&State` and `&crate::state::State` does not contain it. That is this row's own defect, in the check written to catch it, so the fully-qualified spellings are matched too and the third probe now fails. The `:38` doc-link is fixed. |
| ARCH-03 | P1 | **`uninstall` reports success for a removal that did not happen.** The function returns `Ok(())` for any target that is a symlink or is not a directory, and the caller maps `Ok(())` to the user-visible string `Removed {runner_id}`. The precondition is reachable in production: the installed-runner listing filters on `child.is_dir()`, which follows symlinks, so a symlinked runner directory is listed as installed and then cannot be removed — and the user is told it was. | `crates/core/src/runners/proton.rs:1110-1120` (`if target.is_symlink() \|\| !target.is_dir() { return Ok(()); }`); listing filter that follows symlinks `crates/core/src/runners/mod.rs:1062`; message mapping `crates/app/src/view/runners.rs:703-708` | A failure that is indistinguishable from success, in a destructive flow, reported to the user as a completed action. The runner remains on disk and still appears in the list, so the user's model of the system is now wrong in a way nothing will correct. | Return `Err(RunnerError::NotInstalled)` (or a dedicated variant) for the symlink case, and let it reach the existing `Err` arm, which already formats `Could not remove {runner_id}: {error}`. If the symlink refusal is deliberate, keep the refusal and change the return so the message is honest. **Status: FIXED with `BUG-03`.** `uninstall` no longer returns `Ok(())` for a removal that did not happen; a symlinked build is unlinked, and the regression test fails on the pre-fix body. |
| ARCH-04 | P1 | **One of the two "open something for the user" helpers discards its spawn failure and returns `Ok(())`; the other reports.** `open_prefix_folder` spawns `xdg-open` with `let _ = ...` and returns `Ok(())` unconditionally, so a machine with no `xdg-open` — or a failure to exec it — produces a silent no-op that the UI reports as success. Its sibling in the same file maps the spawn error into a message. | Silent: `crates/app/src/main.rs:2788-2811` (the `let _ = std::process::Command::new("xdg-open")…spawn(); Ok(())` at `:2803-2809`). Reporting sibling: `crates/app/src/main.rs:2840-2847` (`open_url_command(url)….spawn()….map_err(\|error\| format!("Could not open {url}: {error}"))`) | The user presses "Open prefix folder", nothing happens, and the application says nothing. This is the project's own named defect class — a failure that becomes silently indistinguishable from success — in its purest form, and the fix is already demonstrated twenty lines below it in the same file. | Return the spawn error as `Err(format!("Could not open the prefix folder: {error}"))`, matching `open_url`. The canonicalize fallback at `:2801` is separately fine and its reasoning holds. **Status: FIXED with `BUG-04`.** `open_prefix_folder` now propagates the spawn error as `Err(format!("Could not open the prefix folder: {error}"))`, matching `open_url`. The helper is new, so there is no restorable pre-state; verified end-to-end against the real binary instead. |
| ARCH-05 | P1 | **A core module's own header declares the live easy-install path dead.** The doc states the file "has no *caller*" and that five named functions "have no call site outside their own tests". Every one of the five has a production call site: three are driven by the app's easy-install worker, and two are called inside the module's own live path. | Comment `crates/core/src/installers.rs:15-23`, naming the five at `:11-13`. Contradicting callers: `crates/app/src/main.rs:28-29` (the imports), `:3059` (`easy_install_worker`, whose own doc says it was wired by T-38), `:3073` (`download_installer`), `:3118` (`wait_for_installer`), `:3125` (`wait_for_prefix_idle`); `crates/core/src/installers.rs:1885` (`verify_installer_authenticity`), `:2001` (`wineserver_binary`). The page says so too: `crates/app/src/view/installers.rs:41` | This is not a stale count, it is the module's stated contract inverted. A maintainer reading `core` — the crate that is supposed to be self-describing — is told the entire install path is unwired, and may re-wire it, delete it, or decline to touch it as "not landed". The sibling file at `crates/app/src/view/installers.rs:46-50` records that this exact failure mode already happened once in this project. | Rewrite `:15-23` in the present tense, naming `easy_install_worker` (`main.rs:3059`) as the caller, and keep only the genuinely-open items listed at `:25-30`. **Status: FIXED `9f07c41`.** The header's "no *caller*" section is replaced by a `# Callers` table naming the production call site of each of the five functions, plus `crates/core/tests/wiring_claims.rs`, which reads that table and asserts each row. The guard's first version passed with the defect present — its needle matched the function's own `fn` definition — and `the_needle_does_not_match_a_definition` now pins that (see DECISIONS D-61). **Re-opened in part and re-fixed `75fc739`.** The first `# Callers` table cited `path:line`, and the check asserted the exact line. Within the hour the citations went stale — concurrent edits moved `easy_install_worker` from `:3059` to `:3269` — and the `test` stage failed on correct code. That is `ARCH-16`'s defect inside the `ARCH-05` fix. Every row now names the enclosing function instead (`crates/app/src/main.rs`, in `easy_install_worker`) and `enclosing_function` resolves it at check time. This is also a stronger check than the line was: the old rule for rows into another file was *some line in that file contains a call*, which an unrelated function beside it would satisfy; the new rule is *that call is inside the function the header names*. Measured: three controls — a wrong container name, a bare `path` with no container, and the only production call of `download_installer` commented out — each fail with a message naming the container the call is actually in. |
| ARCH-06 | P1 | **A comment states that the toolkit cannot do something it can, and the paragraph is the recorded reason a known defect is deferred.** `widgets.rs` asserts that `Wrapping` is `None`/`Word`/`Glyph`/`WordOrGlyph`, that "there is no truncation-with-marker mode in this version", and that closing the issue "needs an iced that can elide, so it is a bound on this fix and not a task inside it". At the pinned rev, `Ellipsize::End(EllipsizeHeightLimit)` exists and the `Text` builder exposes `.ellipsize(...)`. The app's own `text` helper returns an `iced::widget::Text` directly, so the method is reachable without leaving libcosmic. | Comment `crates/app/src/view/widgets.rs:564-575`. Falsified against the pinned tree: `/home/gosh/.cargo/git/checkouts/libcosmic-41009aea1d72760b/a401af8/iced/core/src/text.rs:201-213` (the `Ellipsize` enum and `EllipsizeHeightLimit`), re-exported at `.../iced/core/src/widget/text.rs:37`, builder at `:166`; app's `text` returns that type — `.../a401af8/src/widget/text.rs:3` (`pub use iced::widget::Text`), `:7-9`. The behavioural half is `COSMIC-UX.md` UX-26 | The project has already been burned by a comment that confidently states something false, and this one has a cost attached: it converts a task into a constraint, so the defect it describes stays open and the next reader has no reason to check. The test that exists cannot catch the gap either — it asserts height only (`crates/app/src/view/widgets.rs:1253-1310`). | Delete the impossibility claim and apply `.ellipsize(Ellipsize::End(EllipsizeHeightLimit::default()))` at `crates/app/src/view/widgets.rs:578-598`. Add a width assertion to the existing test so the property is actually inspected. **Status: FIXED with `BUG-47`.** The impossibility claim is deleted — the pinned iced does expose ellipsis — and the strategy lives in `name_ellipsize` at `crates/app/src/view/widgets.rs`. |
| ARCH-07 | P2 | **`cargo fmt --check` fails, and formatting is not a stage in the gate.** Measured at HEAD: 542 diffs across 36 files. The pipeline's eleven stages do not include a formatting stage, so nothing prevents the count from growing. | Measured: `cargo fmt --check --all` → exit 1, **542** `Diff in` lines. Absent from the stage list in `scripts/verify.sh`; the clippy stage at `:813-814` is the only style gate and it runs `cargo clippy --all-targets -- -D warnings` | `PLAN.md` T-07 already carries this, so it is a known debt rather than a surprise. The architecture consequence is that the tree's formatting is not enforceable by any command a contributor is asked to run, which means every future diff is mixed with reflow noise. | Add `stage_fmt() { cargo fmt --all --check; }` to `scripts/verify.sh` and land the 542-diff reformat as a single commit with no logic changes, so the noise is quarantined to one revision. **Status: FIXED `d855015`.** The 542-diff half had already landed in `eaddd00`, so `cargo fmt --all --check` exits 0 at HEAD — this row's first clause was stale. The gate half is new: `stage_fmt` is third in `STAGES`. Mutation-proved by appending an unformatted function to `crates/core/src/lib.rs` (fails with `Diff in .../lib.rs:103`) and restoring (passes). |
| ARCH-08 | P2 | **`README.md` documents a property the command it names does not deliver.** The README says the Rust suite "runs with `DISPLAY` and `WAYLAND_DISPLAY` unset" and attributes that to a plain `cargo test`. The stripping lives only in the verification script, which runs the suite under `env -u`. A contributor who follows the README gets a suite that inherits the display variables. | `README.md:340-341` against `scripts/verify.sh:974` (`output="$(env -u DISPLAY -u WAYLAND_DISPLAY -u WAYLAND_SOCKET cargo test 2>&1)"`) | Small in itself, but it is the documented entry point for running the tests, and the property it promises is the one that proves the core crate needs no display server. A reader who trusts the README and gets a different result has no way to tell whether the layering claim or the README is wrong. | Reword `README.md:340-341` to name `scripts/verify.sh` as what performs the unsetting, or state that the suite is headless by construction (`crates/core` has no GUI dependency) rather than claiming the ambient environment is stripped. **Status: FIXED `d855015`.** Verified by the method the row asked for — running the documented command with the variables set. `cargo test --workspace` with `DISPLAY=:0` and `WAYLAND_DISPLAY=wayland-1` is green (0 failed), so the outcome claim held and only the mechanism claim was false. The README now states the property as a fact about `crates/core`'s dependency graph rather than about the ambient environment. |
| ARCH-09 | P2 | **Errors lose their context at several boundaries, and one of them makes a local write failure indistinguishable from a legitimate "not found".** A cover lookup that fails because the *destination* is unwritable surfaces as "no Steam cover found"; two JSON decodes discard line and column; a metadata reader collapses six distinct failures into an empty map. | Cover: `crates/core/src/covers.rs:1044-1057` — the icon fallback is `.and_then(\|exe\| icon_cover(...).ok())` and the terminal error is `CoverError::SteamFailed { message: steam_error }`, so `icon_cover`'s failure is discarded entirely. Decodes: `crates/core/src/runners/proton.rs:209-211` and `:560-562` (`map_err(\|_\| RunnerError::Http { message: "Unexpected GitHub releases response" })`). Metadata: `crates/core/src/runners/mod.rs:977-995` (symlink, absent, unreadable, non-UTF-8, non-JSON and non-object all return an empty map) | "The network said no" and "the disk is full" are different problems with different fixes, and the code reports them with the same words. The discarded decode position is the difference between a truncated response (retry) and a changed GitHub schema (fix the parser). | In `covers.rs`, carry the icon error into the terminal variant — or at minimum log it — so a write failure is not reported as a lookup miss. In `proton.rs`, put the `serde_json` error's line/column into `RunnerError::Http`'s message. In `read_metadata`, return a `Result` or a reason alongside the map so its six arms are distinguishable. **Status: FIXED `937ef5b` (and `d602716` for the other two boundaries).** Two of this row's three boundaries were already closed before it was worked: the cover-arm provenance by `BUG-13` and the metadata reader's six collapsed arms by `BUG-14` (`MetadataRead::{Object, Missing, Damaged}`). The third — `proton.rs`'s two JSON decodes, which discarded the position — is now fixed: `get_text`'s `from_utf8` failure names the **byte offset**, and `fetch_available`'s `serde_json` failure carries the **parse position** rather than the bare sentence `Unexpected GitHub releases response`. Both are regression-tested with measured arms, and each was mutation-proved by reverting the fix and watching its test fail. This row's two citations, `proton.rs:209-211` and `:560-562`, **did** land on the functions they name (`get_text` at `:207`, `fetch_available` at `:558`), which is worth stating because it is the file's third row whose citations were checked and only the second whose citations held. |
| ARCH-10 | P2 | **The error taxonomy is strong inside `core` and collapses at two boundaries.** `core` defines eight error enums with `Display` and (where relevant) `From` impls; the persistence APIs instead return bare `io::Error`/`serde_json::Error`, and the application layer's de-facto error type is `Result<_, String>`, including every `Message` payload that carries a failure. | Eight enums: `crates/core/src/runners/mod.rs:106` (`RunnerError`), `crates/core/src/runners/archive.rs:108` (`ArchiveError`), `crates/core/src/runners/shell.rs:60` (`ShellError`), `crates/core/src/installers.rs:498` (`InstallerError`), `crates/core/src/plugins.rs:287` (`InstallError`), `crates/core/src/covers.rs:544` (`SaveIconError`), `crates/core/src/covers.rs:625` (`CoverError`), and the private `RunFailure` at `crates/core/src/installers.rs:1540`. Bare `io::Error`: `crates/core/src/models.rs:604-660`, `crates/core/src/settings.rs:202-207`, `crates/core/src/json.rs:113/347/355`, `crates/core/src/runners/desktop.rs:194`. `Result<_, String>`: 19 sites in `crates/app/src` | A caller cannot distinguish a recoverable persistence failure from an unrecoverable one, and because the type is `String`, the app layer cannot add a variant later without touching every site. The inconsistency is also structural: `core` is documented as the self-describing crate, but its most-used API surface is the one place the taxonomy stops. | Introduce a `PersistenceError` in `core` with a small set of variants and a `Display`, and have `models`/`settings`/`json` return it. This is additive — each existing `io::Error` maps to one variant — and it is the change that makes ARCH-01's unrecoverable case expressible as a type rather than as a convention. **Status: FIXED `937ef5b`.** `crates/core/src/json.rs` now defines `PersistenceError` with a variant per failing step — create directory, write temporary, replace, serialize — plus `WouldDiscardUnreadable` for `Library::save`'s gate, so the two kinds of failure `ARCH-01` distinguishes are distinguishable **by type** instead of by an `io::ErrorKind` no caller read. `models` and `settings` return it; the app still renders it, now with the path and the step in the message. The app layer's second collapse is closed too: `view::plugins::InstallRunError` replaces `Result<bool, String>`, so `plugins::InstallError` survives to the handler as its own variant rather than as a formatted string. Regression tests: `every_write_step_that_can_fail_maps_to_its_own_variant`, `a_write_failure_still_carries_the_os_error`, `a_refused_write_reaches_the_toast_as_a_refusal_not_a_write_failure`, `a_failed_write_reaches_the_toast_naming_the_path_and_the_step`, `a_json_decode_reports_where_the_payload_stopped_being_json`, `a_core_install_error_reaches_the_message_as_its_own_variant`. |
| ARCH-11 | P2 | **Module organisation: the four large files are large because of their test modules, and the production halves have concrete seams that no one has cut.** The sizes that motivate the concern are mostly test code. | Measured boundaries: `crates/app/src/main.rs` test module `:3848-9343` (5495 of 9343 lines, 138 tests); `crates/core/src/installers.rs` `:2179-4265` (2087 of 4265, 63 tests); `crates/core/src/runners/proton.rs` `:1124-2777` (1654 of 2777, 48 tests); `crates/app/src/view/runners.rs` `:1125-2614` (1490 of 2614, 49 tests). Production seams: the CLI block `crates/app/src/main.rs:83-566`, easy-install `:2953-3573`, choosers `:3195-3362`, the cover-fetch task `:3262-3298`; in core the three halves are already banner-delimited at `crates/core/src/installers.rs:634`, `:1111`, `:1905`, and the second `use` block at `:1115-1118` is the residue of the boundary that was never collapsed | "Split it up" is not actionable; these four seams are. The mid-file `use` block at `installers.rs:1115-1118` is the concrete evidence that the file is two files sharing a scope: a `use` after 1100 lines of code is what a module boundary looks like when it is drawn as a comment. | Cut, in this order — each is mechanical and none changes behaviour: `crates/core/src/installers/{prefix,download,wizard}.rs` at `:814-1110`, `:1111-1904`, `:1905-2177`; `crates/app/src/cli.rs` from `main.rs:83-566`; `crates/app/src/easy_install.rs` from `:2953-3573`. **Two constraints must be respected:** `crates/app/tests/dispatch_coverage.rs:280-281`, `:384-385` and `:446-448` panic if `pub enum Message`, `fn update(&mut self, message: Message)` or the page dispatch leave `crates/app/src/main.rs`, and `:611-612` reads view modules by literal filename — so `Message`, `Shell::update` and page dispatch stay, and no existing `view/*.rs` may be renamed. Do not split `enum Message`: 65 variants in one enum is what makes the dispatcher's exhaustiveness worth having. **Status: PARTIAL — the `cli.rs` seam landed, the other three did not, and one prescription in this row was wrong.** `crates/app/src/cli.rs` now holds lines 72-569 of `main.rs` as it stood: `struct Cli`, `run` (the old `main`, renamed, with its doc comment), `list_lines`, `list_games`, `library_lookup`, `launch_report`, `launch_game`, `launch_game_at`, `DISPLAY_VARS`, `display_present`, `no_display_hint`, `display_refusal`, `enum GuiStart` and its `impl`, `start_gui` and `run_gui`. `main.rs` keeps `fn main() -> ExitCode { cli::run() }` with a new doc comment, because the binary's entry point cannot live anywhere else; measured before/after, `crates/app/src/main.rs` is **11659 → 10277** lines and `crates/app/src/cli.rs` is **0 → 1443**, **+61 net** across the two. Every moved line is byte-identical except four enumerated edits, each forced: `fn main` → `pub fn run` (the entry point stays behind and calls it); `pub(crate)` on `launch_report` and `launch_game` (two intra-doc links in `main.rs` name them, so they are no longer private to the crate root); the `rustfmt` reflow of the first of those signatures, which is where most of the +61 is; and the two `main.rs` → `cli.rs` strings in the window-floor test below. **30 tests moved with their subjects** into `#[cfg(test)] mod tests` in `cli.rs`, bodies and doc comments verbatim — measured as CLI-symbol-referencing tests, not the row's estimate of 31, which counted a 31st that does not exist. Two helpers moved (`env`, `native_game_library`); **two did not, and the reason is a count**: `library_with` (13 callers, 8 of them in `main.rs`) and `ShortcutEnv` (3 callers, 2 in `main.rs`) are majority-owned by the tests that stay, so they became `pub(crate)` and `cli.rs` imports them rather than the reverse. One further test moved that the symbol scan does not catch — `the_window_floor_is_the_references_and_the_builder_asks_for_it` grades `run_gui`'s `Settings` builder by reading it out of `src/main.rs`, and that construction moved, so the test moved with it and now reads `src/cli.rs`. `cargo test --workspace` is **1128 passed / 0 failed / 1 ignored, identical to the pre-change baseline** — a move between modules changes no total. **What contradicts the row:** its citation `main.rs:83-566` is roughly right for the seam's extent but wrong at both ends (the block is `:72-569`, starting at the `/// Command-line interface.` doc block the row's own prose names but its line range excludes, and ending at `run_gui`'s closing brace, six lines past the `:566` it gives, which would cut `run_gui`'s body in half); and its "31 tests" is 30. The `installers/{prefix,download,wizard}.rs` and `easy_install.rs` seams are **not cut** — `crates/core/src/installers.rs` is already banner-delimited at the cited lines and `crates/app/src/easy_install.rs` already exists as its own file, so the ordering this row prescribes is partly stale rather than merely undone. |
| ARCH-12 | P2 | **Two god-objects: a 968-line dispatcher and a 33-field state struct.** `Shell::update` is one function with 61 arms; `State` carries every page's data in one struct. | `crates/app/src/main.rs:1620-2587` (`fn update(&mut self, message: Message)` — 968 lines, 61 arms, measured); `crates/app/src/state.rs:685` (`State`, 33 `pub` fields) | The dispatcher's length is mostly long arms rather than deeply nested logic, and the exhaustiveness check it buys is real. The cost is that no single arm can be unit-tested without constructing the whole `State`, which is why the file's tests are mostly end-to-end. `State`'s field count is the same problem from the other side. | Not a rewrite. Extract the four longest arms into free functions taking `&mut State` (the file already does this for `open_prefix_folder`/`open_url`), and group `State`'s page-scoped fields into per-page structs (`LibraryState`, `InstallersState`) so a handler takes the one it needs. Keep `Message` and the `match` where they are. **Status: PARTIAL — the dispatcher half landed, and the row was not updated to say so.** `save_game_form`, `cover_fetch_finished`, `cover_file_chosen` and `run_prefix_tool` are free functions taking `&mut State` (`crates/app/src/main.rs`), extracted in `3b6ee5a` under `ARCH-11`, and the four arms are one line each. Re-measured against the tree rather than the row: `Shell::update` is **965** lines and **59** arms, against the row's 968 and 61 — the length barely moved because the extracted bodies were replaced by calls rather than removed, which is the point of the recommendation and not a failure of it. **The second half is not done, and the reason is a measurement the row did not carry.** `State` has **38** `pub` fields, not the 33 the row records, and `crates/app/src/main.rs` alone holds **367** `.state.<field>` references. The seven the summary named account for **39** of those — `easy_busy` 17, `running_install` 9, and the rest single-digit — because the fields a `LibraryState`/`InstallersState` grouping would move are not the busy ones. **None of the three most-referenced fields is page-scoped at all:** `toasts` 53, `settings` 49 and `library` 42 are shell-wide, so grouping them is not available and the realistic call-site count for this half is comfortably over 200. That is a pure mechanical rewrite, ranked 8th of 9 in the brief's own priority order (maintainability, above only cosmetic), against six open `P2` rows that are not. Recorded as a measured cost rather than done or dropped: the extraction half is closed and committed, the grouping half is open with its price written down, so the next reader decides with the number in front of them instead of re-deriving it. |
| ARCH-13 | P2 | **A hand-rolled Python lexer used as a test oracle is duplicated byte-for-byte between the two crates.** `join_adjacent_literals` exists twice with identical bodies, down to the closure and the loop tail; its three call sites all parse the same reference file. | `crates/core/src/plugins.rs:613-639` and `crates/app/src/view/plugins.rs:315-341` (bodies compared token for token); call sites `crates/core/src/plugins.rs:1094`, `:1154`, `crates/app/src/view/plugins.rs:455` | If the lexer is subtly wrong — an unhandled `'''`, a backslash before a newline, a comment between adjacent literals — both crates' oracle tests agree with each other and disagree with the reference, which is this project's own named defect class (a green test whose assertion a different mechanism satisfies). Two copies means the next edge case must be fixed twice to stay honest, and nothing detects a fix applied once. | Move it to one place — core's test-support surface, since it is a pure `&str -> String` with no core-internal dependencies — and have the app's test module call it. **Status: FIXED** (`91cdfa6`). The recommended home was "core's test-support surface", and the obvious reading of that — a `#[cfg(test)] mod` — does not work here: `cfg(test)` is set for the crate under test and not for its dependencies, so an application test cannot call into `gamehandler-core`'s `#[cfg(test)]` items however they are declared, and a module written that way would have deduplicated nothing while looking as though it had. The lexer therefore lives in `crates/core/src/oracle_support.rs` behind a non-default `test-support` feature, reached from `gamehandler` by a `[dev-dependencies]` edge and from core's own tests by a self-dev-dependency. Both bodies were compared token for token before the deletion, as the row says they were for the finding, and the three call sites now read `crate::oracle_support::` and `gamehandler_core::oracle_support::`. The module carries four tests — the prefixed-literal join the reference actually contains, the comma that is the only thing that stops a join, whitespace surviving *inside* a literal, and the two-prefix-letter limit — and the second of those is a correction: its first version asserted that two bare adjacent literals would not join, which is wrong, because in Python they do. The measurement that keeps the module out of a release binary is `cargo tree -e features --no-dev-dependencies`, which the feature edge does not appear in; `scripts/verify.sh` builds both ways, so a release binary that had grown a link here would fail there rather than ship. The row's last sentence is the part worth keeping: nothing detected a fix applied to one copy, and that is now "nothing can".|
| ARCH-14 | P2 | **A comment says a shared helper becomes warranted when a third copy appears; the third copy already exists.** | Comment `crates/app/src/view/settings.rs:744-749` ("A deliberate second copy… If a third caller ever appears, the right move is a shared `#[cfg(test)]` helper, not a third copy"). Copies: `crates/app/src/main.rs:8920-8947`, `crates/app/src/view/settings.rs:753-779`, `crates/app/src/view/credits.rs:913-940`; a fourth variant of the same traversal at `crates/app/src/view/widgets.rs:1962-1983` (`struct Collect`) | The paragraph states a count that is wrong and a rule whose own trigger has fired, so the next reader either trusts the count and adds a fourth copy or follows the rule and does a refactor they were told was unnecessary. | Do what the note says: one `#[cfg(test)]` helper taking `Element<'_, M>` in `view/mod.rs` (or `view/testkit.rs`), used by all four sites. The four bodies are already the same ~27 lines. **Status: FIXED.** One `#[cfg(test)]` module, `crates/app/src/view/testkit.rs`, holds the traversal; all six copies are gone and the count is the finding's own correction, because six had accumulated where the row measured four and the comment said "a deliberate second copy". `drawn_strings` delegates to `testkit::traversal`, `view::widgets`'s harness re-exports it rather than keeping a fourth `Seen`/`Collect`/`renderer`, and `view::a11y`'s `renderer` — a byte-identical two-line copy — re-exports the shared one. `traversal_at_width` moved with it, because it is the same traversal at a real width and the `INFINITY` limit in the plain one is precisely why a wrap is invisible to it. **The refactor was caught by a test that already existed:** `the_layer_split_the_docs_describe_is_the_split_the_code_has` failed on the new module, because it reads `view/mod.rs`'s classification table as the complete contract for every declared module — so `testkit` needed a fourth row (*Test support*, `#[cfg(test)]`) rather than a quiet exception. Two dangling citations went with the copies: `a11y.rs` quoted the "if a third caller ever appears" note at a `settings.rs` line by then occupied by other code, and that note is deleted, so the quote is re-pointed at what replaced it rather than left quoting a line that no longer exists. Verified by the count rather than by reading: `grep -rn "fn drawn_strings\|fn traversal\b\|fn renderer()" crates/` returns the three definitions in `testkit.rs` and nothing else. |
| ARCH-15 | P2 | **The repo-root test helper is copied four times with three different derivations, and one copy's doc cites a precedent that does not use its derivation.** | Copies: `crates/core/src/plugins.rs:588-595`, `crates/core/src/credits.rs:491`, `crates/app/src/view/plugins.rs:301-308`, `crates/app/src/view/credits.rs:394-401`. Bespoke variant with a false citation: `crates/app/src/view/form.rs:854-862` ("derived from `CARGO_MANIFEST_DIR` for the reason `main.rs`'s tests derive theirs"), while `crates/app/src/main.rs:8996`, `:9258` and `:9309` use `join("../../…")` chains. Further inline copies at `crates/app/src/view/settings.rs:794`, `view/state.rs:1090`, `view/form.rs:1564`, `view/runners.rs:1619`, `:2481`, `main.rs:6543` | This is the path every reference-reading test depends on. A workspace restructure breaks some tests with a `panic!("… is unreadable")` and leaves others passing, which is the worst of both — a partial failure that looks like a data problem. The false citation makes the divergence read as deliberate. | One `repo_file(relative: &str) -> String` per crate in a `#[cfg(test)]` support module (`crates/core/src/lib.rs`, `crates/app/src/view/mod.rs`), all call sites migrated, and `form.rs:854-862`'s doc corrected. **Status: FIXED.** One `repo_file`, in `gamehandler_core::oracle_support` — the module `ARCH-13` already created for exactly this, behind the non-default `test-support` feature that `[dev-dependencies]` turns on in both crates and that `cargo build`/`--release` cannot reach (so it is not in the binary, which a `#[cfg(test)]` module in `core` could not achieve for the app's tests — `cfg(test)` is not set for a dependency). **The count in the row was low.** It measured four copies; the tree had four named `repo_file` definitions plus a fifth named `read_repo_file` in `app::state`, and eleven further inline sites doing the same read-and-panic by hand at their own derivation — `launch_opts.rs` and `main.rs` ×4 reading `runners.py`/`Main.qml`/`bridge.py`, `settings.rs` ×3 reading `SettingsPage.qml`. All sixteen now call the helper. **One site deliberately does not:** `core::installers::authenticode_root_candidates` joins `../../data` in *production* code, and a `--features test-support` helper is not reachable from a release build — moving it would either fail the build or drag a test-only module into the binary, so it stays and the reason is here rather than in a comment nobody checks. The four panic messages are one message now, and it says what the failure means rather than only that it happened: a missing reference is the oracle being absent, not a data problem. **The false citation is corrected rather than deleted:** `form.rs`'s doc claimed its path was "derived from `CARGO_MANIFEST_DIR` for the reason `main.rs`'s tests derive theirs", and `main.rs` used a bare `join("../..")` while `form.rs` used `.parent().and_then(...)` — so the precedent named was not one, and the doc now records that. Verified by resolution, not by reading: the tests that read the reference pass from both crates (`core::credits::tests` 14, `app::credits::tests` 14), and `grep -rn 'fn repo_file\|fn read_repo_file' crates/` returns one definition. |
| ARCH-16 | P2 | **A comment embeds a grep transcript as its own proof, and the transcript's line numbers no longer land.** The paragraph's argument is that the reader need not take it on faith because the command is given; running that command returns different lines. | Comment `crates/app/src/view/meta.rs:67-77` cites `view/library.rs:343` and `:360`. Actual call sites: `crates/app/src/view/library.rs:432` and `:448` — an offset of +89. The project has already written down why this matters: `crates/app/src/view/widgets.rs:638-640` ("a pointer that no longer lands is worse than no pointer, because it is trusted") | The claim the transcript supports may well still be true, but a reader who runs the command cannot tell whether the *claim* is stale or only the *citation*, so the paragraph fails at the one job it was written for. | Re-measure and paste the real output, or replace the line numbers with symbol names (`row_labels`, `grid_body`), which cannot go stale. **Status: FIXED `0c92f9e`.** The transcript is gone; the two call sites are named (`grid_body`, `row_labels`) and the grep a reader runs themselves returns those two and the definition. Re-measured before the edit: the cited lines had drifted +570 and held unrelated code. |
| ARCH-17 | P2 | **Three copies of the card surface, two of which hardcode the radius that a comment says is what keeps them in sync.** The comment sets "three copies" as the threshold for moving to one place and asserts the numbers being equal is what keeps the pages looking like one application; the single-source constant is read by only one of the three. | Copies `crates/app/src/view/widgets.rs:794`, `crates/app/src/view/runners.rs:645`, `crates/app/src/view/installers.rs:592`; the literals at `crates/app/src/view/runners.rs:650` and `crates/app/src/view/installers.rs:597` against `crates/app/src/view/metrics.rs:43` (`CARD_RADIUS = 14.0`), used only at `widgets.rs:801`; the only test reads `widgets.rs`'s own copy (`crates/app/src/view/widgets.rs:1852-1853`). The comment is `crates/app/src/view/installers.rs:586-591` | Changing `CARD_RADIUS` silently leaves two pages at 14.0 — the exact divergence the sentence promises cannot happen — and nothing fails. The comment's own threshold has been reached, so its second half reads as a commitment not kept. | Point both call sites at `metrics::CARD_RADIUS` now (one line each), then collapse all three onto `cosmic::theme::Container::Card`, which `crates/app/src/view/credits.rs:318-322` already uses.  **Status: FIXED** (`5a03977`). `widgets.rs` holds the only definition, now `pub(super)`; `installers.rs` and `runners.rs` import it and their copies are gone, along with the two `14.0` literals — a grep for `radius: 14.0` across `crates/app/src/` now returns nothing. The guard the row asked for ("verify with a grep for the hardcoded radius outside the definition") is behavioural rather than textual: `only_one_page_defines_the_card_surface` reads all three modules' source with `include_str!` and fails if any but `widgets.rs` defines the surface, and also asserts the two pages import it. It was mutation-checked by re-adding a hand-built copy to `runners.rs`, which failed the guard with the message naming the offence. **The row's second direction is explicitly not taken, and that is a decision, not an omission.** It proposed collapsing all three onto `cosmic::theme::Container::Card` as `credits.rs` already does. Measured side by side rather than assumed equivalent, the two are not the same surface: the class resolves to `corner_radii.radius_s` = 8.0 on `background(false).component.base` (dark `#2E2E2E`), while this surface is `metrics::CARD_RADIUS` = 14.0 on `background(false).base` (dark `#1B1B1B`). Adopting the class would have restyled every card on all three pages — a visual redesign, and one that would have silently overridden the port's own numbers without anyone choosing to. The row also undercounted the tests' reach: it said the only test read `widgets.rs`'s own copy `at widgets.rs:1852-1853`, which is true and was exactly the gap — that test proves the constant reaches the renderer and cannot notice a fourth copy, which is how the second one arose.|
| ARCH-18 | P2 | **Six comments across four files cite the settings-validation fallback at line numbers that do not contain it, and the citations are ambiguous in a workspace with two `settings.rs` files.** The cited ranges land in `from_dict`'s header and in unrelated branches. The substance of every one of them is correct. | Citations: `crates/app/src/theme.rs:94`, `:109`; `crates/app/src/view/settings.rs:230`; `crates/app/src/main.rs:1827`, `:1874`, `:6543`. The fold is at `crates/core/src/settings.rs:155-159`. The claim itself is confirmed by `docs/migration/oracle/FINDINGS.md:261` (F-E) | The bare basename is the worse half: `crates/app/src/view/settings.rs:230` citing `settings.rs:89-91` points a reader at the file they are already reading. These citations are how parity is checked, so a wrong one costs a manual search every time. | Cite `core/src/settings.rs:155-159`, or `Settings::from_dict` by name, at all six sites; drop the bare basename everywhere. **Status: FIXED `0c92f9e`.** All seven citations now name the function (`Settings::from_dict`) and the full path `crates/core/src/settings.rs:155-163`, which is where the fold actually is. The bare basename is gone from every site, so the two-`settings.rs` ambiguity cannot recur. |
| ARCH-19 | P3 | **Three Python citations land on a blank line.** | `crates/app/src/view/meta.rs:43-44` cites `bridge.py:323` (blank; the code is at `:319`); `crates/app/src/view/plugins.rs:131` cites `bridge.py:1016` (blank; the emit is at `:1020`); `crates/core/src/runners/desktop.rs:125` cites `runners.py:1443-1446` (`:1443` blank, the `def` is at `:1444`, and the range does reach it) | Small, but these are the citations a maintainer uses to check parity, and the codebase has a documented off-by-one convention (citing the line above a `def`), which makes these indistinguishable from correct citations at a glance. | Point each at the line with content. **Status: FIXED.** **The row undercounted: it is six, not three.** A sweep of every Python citation in the tree — 620 of them — found 16 whose line is blank, and of those six are *single-line* citations, where a reader following the pointer lands on nothing at all. The other ten are ranges whose first line is the blank separator above a block; the range still covers the definition, so those are recorded here rather than rewritten. Fixed, each verified against the reference: `view/meta.rs:43` `bridge.py:323` → **319** (`if row["category"] != "Uncategorized"`, the only line that compares the literal); `view/plugins.rs:126` `bridge.py:1009` → **1012** and `:131` `bridge.py:1016` → **1020** (the two `notify.emit` calls — the second was not in the row at all, found by the sweep); `runners/desktop.rs:125` `runners.py:1443-1446` → **1444-1446** (the row called this one benign because the range reaches the `def`, which is right, but the range now starts on it); `core/covers.rs:180` and `view/cover.rs:62` `covers.py:95` → **94** (`COVER_ACCENTS = 8`); `runners/families.rs:88` `runners.py:90` → **93** (`RUNNER_FAMILIES`), `:233` `:231` → **232** (`runner_guide_details`), `:264` `:260` → **261** (`_looks_like_archive`), `:572` `:343` → **341-342** (`family`). The sweep is the durable half: re-running it over `crates/**/*.rs` is one pass with the reference tree beside it, and it now reports zero. ||
| ARCH-20 | P3 | **Two comments record the size of a mutation-testing result, the two numbers disagree with each other, and neither matches the current suite.** Both paragraphs are the record of a coverage gap, and the number is what makes the record credible. | `crates/app/src/view/form.rs:3060` ("312 tests"), `crates/app/src/main.rs:833` ("322"); measured at HEAD: `cargo test -p gamehandler --bin gamehandler -- --list` → **504 tests**. The row's own cited ranges (`form.rs:2026-2029`, `main.rs:1104-1108`) are stale as well — neither holds the sentence it quotes. | A reader cannot tell which figure was the later measurement, or whether the gap has since closed. This is the codebase's own rule applied to its own comments: a count is evidence only with the commit and the build it came from. | Re-measure both mutations at HEAD and restate with the commit, or drop the number and keep the mechanism ("no test reads this call site"), which is the durable half. **Status: FIXED.** Re-measured, and **the two halves turned out not to be the same finding at all** — which is why the row could not be settled by renumbering, and why "restate with the commit" was the wrong half of the row's own advice to take. **(1) The `form.rs` gap has closed** and its number is deleted rather than corrected. The paragraph records `#97`'s third route — the value must be the runner **id**, not the label — and ends by proposing a source scan over `view/installers.rs`. That scan is `both_installers_selectors_are_routed_through_their_own_mapping` (`form.rs:3083`), landed in `a719972`: it reads `view/installers.rs` out of the tree and requires each `widget::dropdown` callback to name exactly one of the two mappings. Re-measured: the row's exact mutation — the pointer callback at `installers.rs:593` replaced by `Message::SetInstallRunner(runner_choices[index].1.clone())` — now **fails** that scan (`the callback ... does not go through exactly one of the two mappings`) where the comment says it leaves the binary green. The block now says so and carries no count. **(2) The `main.rs` gap is live**, and the fix its paragraph proposed would not have closed it. Measured: swapping `Message::RemoveRunnerConfirmed` for `Message::UninstallRunner` at `remove_runner_dialog`'s `primary_action` (`main.rs:856`) leaves **all 504 green** — the same result the comment claimed at 322, so the gap is real and the number was the only stale part. The paragraph's proposed fix was "a named press helper carrying the value, as `remove_press` does for the delete button", and **that helper's own doc says it does not close its call site**: `runners.rs:795-798`, "`installed_card` could stop calling this and build the message itself, and that mutation survives — the third such gap, beside `uninstall_line`'s call site (A) and `install_press`'s (B)". A helper makes the *value* readable; it does not make the *call* unswappable. So the comment was pointing at a remedy this codebase had already measured as insufficient for its own gap class — the audit's own recurring shape, a fix that reads as closing a hole it does not touch. Closed instead by reading the source, which is where the only record of the choice lives: `the_runner_prompt_s_remove_button_sends_the_confirmed_removal_and_not_the_direct_one` (`main.rs`) finds the dialog's body by its column-0 definition, walks `on_press`'s parentheses to the matching close, and requires `Message::RemoveRunnerConfirmed` there and **not** `Message::UninstallRunner`. Mutation-verified in both directions: the swap above fails it, and an anti-vacuity probe (`button::destructive` → `button::standard`) fails with "the runner prompt no longer draws a destructive \"Remove\" button, so this scan has no subject". **Scope stated rather than implied:** the game dialog's Remove button has the identical shape one function up and is **not** covered, because the game prompt's twin of this mutation has not been measured and a scan written for an unmeasured site would be a check whose coverage is an assumption — named in the test's own docblock. **The instrument caught its own scope too:** the first draft of the `main.rs` header documenting this was 69 lines, and ARCH-22's guard failed on it (`doc comment is 69 lines, which is longer than a dialog block`) — the argument was moved into the test's docblock, where the length bound does not reach, rather than the bound being raised. `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, the `doc` stage and `cargo test --workspace` are clean. |
| ARCH-21 | P3 | **The shortcut guard test is not enforced by the gate that is supposed to enforce it.** `view/settings.rs`'s test reads two constants; both are dead under the module-level allow, and deleting the allow — which the TODO explicitly instructs — turns them into a dead-code failure rather than a passing check. | Allow `crates/app/src/main.rs:49-56` (`#[allow(dead_code)] mod view;`, whose comment records that without the declaration `cargo test` reports success 44 assertions short); guard `crates/app/src/view/settings.rs:679` (`the_page_does_not_claim_a_shortcut_the_shell_does_not_implement`), reading `IMPLEMENTED_SHORTCUTS` at `:156` and `UNWIRED_SHORTCUTS` at `:168` | Both facts are true simultaneously and neither is recorded: the guard runs today, and it cannot survive the change the TODO asks for. A reader who removes the allow as instructed meets fifteen dead-code errors, of which two are the guard's inputs. | Note in `settings.rs` that the two constants are `cfg(test)`-only by construction and should be moved under `#[cfg(test)]` when T-09 removes the allow, so the guard survives the change instead of blocking it. |
| ARCH-22 | P3 | **A 58-line dialog docblock describes the runner dialog but sits above the game dialog**, leaving the runner dialog undocumented. | Docblock `crates/app/src/main.rs:1056-1113`; the function it sits above is `remove_game_dialog` at `:1127`; the function it describes is `remove_runner_dialog` at `:1147` | The block contains the only record of why the inline `Column` composition was chosen over `popover`, so a reader looking for that reasoning in the right place will not find it, and a reader who finds it will apply it to the wrong dialog. | Move the block down to `:1147`, or split it: the composition rationale belongs above whichever dialog is built first, and the runner-specific half belongs on `remove_runner_dialog`. **Status: FIXED.** The row understated it: **three** blocks were tangled, not two, and the merged comment is **107** lines, not 58. `page_entry_task`'s block ran into `remove_runner_dialog`'s with no blank `///`, and that one into `remove_game_dialog`'s — so all of it attached to the last function in the chain. `remove_game_dialog` was documented with two other functions' prose, `remove_runner_dialog` had none, and `page_entry_task`, whose block the row's own cited lines open with, had none either. Each block is now re-homed above the function it describes, and the row's cited range `:1056-1113` was stale before this change (it holds `view_body` at this revision), which is the third time a line citation in this document has drifted — the guard below is written against function names rather than line numbers for that reason. **The guard is a two-way claim**, because the defect had two halves: `every_dialog_docblock_names_the_dialog_it_sits_above` in `main.rs` requires that every dialog has a comment (no orphans) **and** that each comment names its own subject (no mis-attachment), taking each dialog's phrase from the reference's own QML name — `removeDialog`, `removeRunnerDialog`, `page.openGameMenu`. A weaker "there is a `///` line above each `fn`" test passes on the broken file, since there *was* a `///` line above `remove_game_dialog`; it was 107 lines long and described something else. Reading the source is a weaker instrument than a call and the strongest available — no `Operation` arm carries a doc comment. Mutation-verified in both directions: re-merging the blocks fails on the length check (`'remove_game_dialog`'s doc comment is 67 lines"), and stripping one block fails on the orphan check (`'remove_runner_dialog` has no doc comment"). `cargo fmt --check`, clippy `-D warnings`, the `doc` stage and `cargo test --workspace` (1130 passing) are clean. |
| ARCH-23 | P3 | **The page-handler convention is inconsistent: two page modules own an `update`, the third does not.** Installers and Runners each expose `fn update(state, message)`; Plugins is handled inline in `Shell::update`. | `crates/app/src/view/installers.rs:619`, `crates/app/src/view/runners.rs:921`, against `crates/app/src/main.rs:2510-2552` (the Plugins arms inside `Shell::update`) | A contributor adding a page has no rule to follow and will pick whichever they saw first. Given ARCH-02, this convention is also the thing that decides whether the `view` contract can be honest. | Pick one. Moving Plugins' arms into `view/plugins.rs` is the smaller change and makes the three consistent; moving all three into `Shell::update` is the change that lets `view/mod.rs:5-11` be true. |
| `ARCH-24` | — | **Three launch settings are written, persisted and never read by anything.** Gamemode, BattlEye and EAC are accepted, stored and surfaced in the UI, but nothing acts on them, while gamescope in the same function does raise when unsupported. **Inherited from the reference**, so a fix is a divergence. | `crates/core/src/runners/launch_opts.rs:738-742` and `:705-718` (silent), against `:744-749` (gamescope raises); the settings are seeded and persisted like the others at `crates/app/src/state.rs:290-304` and `crates/app/src/main.rs:1878-1935` | A setting with no effect is exactly the "advertises a capability that does not work" shape, and it is the largest such surface in the app: three of the fifteen launch options do nothing. | Either implement the three, or mark them in the UI as accepted-but-not-applied, or remove them. The one thing not to do is leave them indistinguishable from the twelve that work; `COSMIC-UX.md` UX-02 records that the toggles themselves cannot be operated without a mouse, which is a separate problem. **Status: WITHDRAWN — not a defect.** Refuted by measurement, and kept rather than deleted so a reader who has heard the claim sees it was tested. All three settings are read and acted on: `game.gamemode` at `crates/core/src/runners/launch_opts.rs:757` (wraps the command with `gamemoderun`), `game.battleye` at `:724` and `game.eac` at `:731` (set `PROTON_BATTLEYE_RUNTIME` and `PROTON_EAC_RUNTIME`). Each has a named passing test — `gamemode_wraps_only_when_gamemoderun_is_installed`, `an_enabled_anticheat_runtime_uses_what_the_host_found`, `a_disabled_anticheat_runtime_is_an_empty_value_not_a_missing_key`, `the_anticheat_lookup_is_skipped_entirely_without_proton_features` — so "nothing acts on them" is false at four independent points, not one. The cited lines do not hold what the row says either: `:738-742` is the `virtual_desktop` branch and `:705-718` is `nvapi`/`fsr`. **What survives, recorded as a note and not as a finding:** the settings are silently ignored when their prerequisite is missing — no `gamemoderun` on `PATH`, or no anticheat runtime found — whereas `gamescope` with no binary raises `RunnerError::GamescopeMissing`. Python does exactly the same (`runners.py:1240-1243` gates on `shutil.which("gamemoderun")` with no `else`; `:1219-1224` guards the runtimes the same way), so this is reference behaviour at parity, and the row itself conceded that "a fix is a divergence". It is a UX wart inherited from the reference, not a port defect, which is why the severity cell is now `—`.|
| ARCH-25 | P3 | **A module-level `#[allow(dead_code)]` covered the whole view layer, suppressing ten never-used items, including a dead widget stack.** The allow was deliberate and its removal was the definition of done for T-09, but it hid more than the scaffolding it was written for. | Allow was at `crates/app/src/main.rs`, on `mod view;`. Forcing the lint past it surfaced 10 never-used items, not the 15 the row named. The behavioural half is `COSMIC-UX.md` UX-11 | A clippy-clean run proved nothing about this module, because the allow suppressed by construction — the gate was green for a reason unrelated to the code being clean. That is the project's own defect class applied to a lint rather than a test. | Done, and the measurement corrected the row twice over. **Status: FIXED.** The allow is gone and `cargo check -p gamehandler --all-targets` reports **zero** warnings without it, so lint now reaches the whole view layer rather than trusting that it does (commit `e8e9de6`). **The row's own evidence was wrong in both directions**, which is the audit's named defect class appearing in the audit's own document: it named 15 items where 10 exist, and two of the fifteen it named — `cover_preview` (`view/widgets.rs`) and `preview_label` (`:296`) — are **live**, reached from the game form's picker at `view/form.rs:889` and `:2021`. A count in a finding that is never re-run against the tree it describes drifts exactly as a count in the report does, and this one had. **Nine items were genuinely unused and are deleted:** `RowWindow::len`/`contains` (the window is read through `start`/`end` arithmetic at both call sites), and the grid-tile cover stack `cover_tile`, `tile_cover_spec`, `tile_height`, `PORTRAIT_RATIO` — the library grid draws a *card* (`card_cover_spec`, the cell less its chrome), so that stack's only caller was `initials_specs()`'s own `"grid tile"` entry, a test asserting about a plate the application does not draw. The helper's own doc already objected to exactly that for the picker's box one entry below; the array drops 3 -> 2 and the objection now reads as a deletion rather than a caveat. **Two were production declarations read only by tests** and are now `#[cfg(test)]`, with their docs corrected rather than quietly gated: `IMPLEMENTED_SHORTCUTS`/`UNWIRED_SHORTCUTS` (ARCH-21's two-way shortcut accounting, which has no production reader at all) and `PAGE_TITLE`. The last is a *correction, not a demotion*: `CreditsPage.qml:10` sets that string as its `Kirigami.ScrollablePage.title`, which is page **chrome**, and the body's first child is the `level: 3` heading the port draws as `LEAD_HEADING` — the port carries the chrome title in the nav bar, read from `Page::Credits::label()`, so the constant's job is holding that, and the QML's third copy, to one string. `TextRow::id`, an `&'static str` naming a QML object and read only by the reference check, went the same way. **Gating `PAGE_TITLE` broke a scanner, and the scanner caught its own breakage** — the case worth recording. `runners.rs`'s `link_button_production_source` cut a file's production source at the first `#[cfg(test)]`, on the stated grounds that "nothing production lives below a test module here". `PAGE_TITLE` sits at `credits.rs:108`, so the cut moved from line 395 to 108 and the scan silently stopped reading that file, while its own `links >= 3` anti-vacuity assert went red reporting "the scan found only 2 calls" — a message that points at the call sites rather than at the cut. The cut is now the precise rule its name promised: the first `#[cfg(test)]` that a `mod` follows. **The regression test for it took two attempts, and the first is the instructive one:** it called `test_module_start` and asserted on its offset, and reverting the *call site* to `text.find("#[cfg(test)]")` left it green — the helper was right and the scan was not using it. A test of the instrument that the instrument's entry point does not run is this module's own defect class, so it now asserts through `link_button_production_source` and the mutation is re-run to confirm it fails. Mutation-verified in both directions: the planted P-33 defect is caught in the region the stale cut had stopped reading, and the reverted cut rule fails the new test. `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings` and `cargo test --workspace` (1129 passing) are clean. |
| ARCH-26 | P3 | **Four comments cite `RunnerManager::choices` at line numbers that hold `system_wine` instead, and they had drifted before this session's merges rather than because of them.** Every one names `runners/mod.rs:1119`; at `babaeef`, the commit that first wrote them, `:1119` is the `system_wine` doc comment and `choices` is at `:1196`. The function has since moved to `:1322`. | `crates/app/src/view/settings.rs:275`; `crates/app/src/view/form.rs:2087`, `:2710`, `:2772`. **`crates/app/src/view/widgets.rs:708` also names `:1119`, and is correct as written** — it is a *historical* note ("the citation went `mod.rs:1061` → `runners/mod.rs:1119`") that states what the number was at the time of an earlier correction, so it is deliberately left alone. | A reader following one of the four lands on real, plausible code — a different function — so the citation reads as verified rather than as broken. This is the same fault as ARCH-18 and ARCH-19 and the third such row: the `path:line` citation is how parity is checked here, and it is the one kind of claim in these files that a command can settle. | Point all four at `runners/mod.rs:1322` (`:1322-1327` for the range), which is where `choices` is. **Status: FIXED.** All four now name `:1322`/`:1322-1327`, and each was checked against the tree rather than re-derived from the function's name. **No commit hash is given, and the reason is the one thing this row has in common with its own subject:** the fix and this record are the same commit, so any hash written here would name a revision that does not contain it. A citation that cannot be made true is worse than an absent one, which is the fault this row reports. |

## Verified sound

Each item below was checked and confirmed during this audit, and is cited so it
can be re-checked rather than trusted.

- **The stated layering holds.** `crates/core/Cargo.toml` declares only `serde`, `serde_json`, `tar`, `xz2`, `bzip2`, `flate2`, `rustix` — no libcosmic, no iced, no `cosmic`, no `winit`. A `use`-census across `crates/core/src` finds no reference to any of them, direct or aliased. The HARD RULE is stated at `crates/core/Cargo.toml:13-17`. D-03 is maintained.
- **The clippy gate is green at HEAD**, in the same configuration the gate uses: `cargo clippy --all-targets -- -D warnings` (the `clippy` stage, `scripts/verify.sh`'s `stage_clippy`; cited by function rather than by line because the line numbers in this file have drifted twice). **Since ARCH-25 that green means something about the view layer too.** It did not before: a module-level `#[allow(dead_code)]` on `mod view;` suppressed the whole layer by construction, so the gate was green for a reason unrelated to whether the code was clean. The allow is gone and the layer lints clean without it, which is what makes this bullet a claim rather than a formatting report.
- **The test suite passes.** 1129 tests: 497 in the app binary, 19 across its six integration binaries (`app_id_lockstep` 3, `dispatch_coverage` 5, `page_roster` 1, `pending_pages` 3, `which_cwd` 1, `wiring_claims` 6), and 613 in core, one of them ignored. The gate runs them headless (the `test` stage in `scripts/verify.sh`), which is what makes the layering claim observable rather than merely asserted.
- **Every `Result`-carrying message is delivered.** The dispatcher test at `crates/app/src/main.rs:5450-5671` runs every message through the real `update` and pins the set with a non-empty effect. `toast_task` (`crates/app/src/main.rs:2603-2608`) returns its push task rather than dropping it, so no error notice is lost to a discarded `Task`.
- **The theme decision recorded in `crates/app/src/theme.rs:11-29` is correct.** libcosmic at this rev has no `theme()` trait hook to override; the paragraph says so and the tree confirms it. The system preference is respected through `theme_for`/`apply` at `crates/app/src/theme.rs:114-165`, and `Message::SetColorScheme` correctly returns `theme::apply(...)` as its task (`crates/app/src/main.rs:1846-1870`) rather than merely storing the value.
- **The nav bar is correctly delegated.** `crates/app/src/main.rs:3728-3730` and `:3831-3834` hand the model and the nav widget to libcosmic rather than reimplementing responsive collapse; the model is built once from `Page::ALL` at `:955-966`.
- **The JSON compatibility layer is precise.** `write_python_file` (`crates/core/src/json.rs:355-368`) reproduces Python's atomic tmp-then-rename, and `parse_lenient` (`:113`) handles the BOM and `clamp_depth` (`:132-171`) handles the depth limit. `to_python_string` at `:347` matches Python's escaping. This is the layer ARCH-01 is built on and it is not the part that is wrong.
- **The `float_roundtrip` feature is genuinely load-bearing**, and its justification comment is accurate: without it, `serde_json` does not round-trip the float timestamps the reference writes.
- **The metrics constants' arithmetic is accurate** where it is checked. `NOTIFY_VOICES` (`crates/app/src/main.rs:9092`) really does carry 39 entries, matching 39 `notify.emit` call sites in `bridge.py`. The fixture timestamps and the 58/300 ratio recorded in the parity notes are correct at HEAD.
- **`metrics.rs`'s zero-slack chrome arithmetic is correct** — the tile, list-row and card heights compose exactly from the constants around them, and the compile-time assertions that pin them are meaningful. The one exception is the assertion about a widget nothing draws, recorded as `COSMIC-UX.md` UX-11.
- **`shortcuts.rs`'s reasoning about `Ctrl+F` is correct.** libcosmic's own `keyboard_nav::subscription()` matches `Character("f")` with Control and routes it to `Application::on_search`, so it is not a control for this port's accelerator table; the module says so and it is right.
- **The withdrawal in `docs/audit/BASELINE.md:110-128` is correct and its reasoning is sound.** There is no close-without-save path: `self.state.game_form = None;` is assigned only after `Library::add`/`update` returns `Ok`, and a write error returns a toast without closing. The audit repeats this withdrawal and does not re-file the P0.
- **The `docs/audit/BASELINE.md:177-200` withdrawal is respected.** The fabricated status bar is not re-derived here, and the long-title grid overflow that BASELINE listed as open item #4 **did not reproduce**: a layout probe measured the name box at 176×19.6 px inside an available width of 188 px — clamped and single-line. That item is withdrawn rather than repeated, and the surviving defect is the missing ellipsis marker (`COSMIC-UX.md` UX-26), which is a different claim with different evidence.
- **The two `cfg(test)` integrations that gate the refactors are correctly written.** `crates/app/tests/dispatch_coverage.rs` computes the set of view modules from what `main.rs`'s production code names rather than listing them (`:17-22`), and it pins the three structural facts ARCH-11's seams must respect (`:280-281`, `:384-385`, `:446-448`). `crates/app/tests/pending_pages.rs` pins the empty pending list. Any refactor must satisfy these, and they are the reason the seam recommendations are shaped the way they are.
