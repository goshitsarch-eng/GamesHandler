# GameHandler — bugs and reliability findings

**Scope.** Correctness and reliability defects in the Rust workspace at
`d56782d`: logic errors, incorrect state transitions, off-by-one, error paths
that silently swallow failures, resource leaks, races, bad handling of
empty/missing/malformed data, and — the project's own dominant defect class —
**a check that passes without inspecting what it claims**. Security posture is
`SECURITY.md`; advertised-vs-implemented is `FEATURES.md`. This document does
not repeat either.

**Method.** Every finding below is either (a) read directly out of the source
and cited, or (b) reproduced by running something. Where a finding came from a
sub-audit rather than my own reading, I re-checked the citation before
including it. Two claims raised during the audit were **refuted by measurement
and are recorded as refuted** in §"Refuted" rather than quietly dropped; one
premise of the audit brief was also wrong and is recorded in `FEATURES.md` §0.

**Severity.**

| Level | Meaning |
|---|---|
| **P0** | Silent, irreversible data loss, or a failure the user cannot perceive at all |
| **P1** | A user-visible false statement (success reported for a failure), a primary flow unreachable, or a whole-suite check that cannot fail |
| **P2** | A real defect with a bounded or recoverable impact |
| **P3** | Edge case, cosmetic divergence, or a comment/test that describes something the code does not do |

Two findings are worth stating before the table, because they frame it:

- **The port is faithful.** Almost every P2/P3 below is a *faithful copy of the
  reference's own behaviour*, verified against `gamehandler/`. That is a
  correctness property, not an excuse — but it means the fix direction for those
  rows is "deliberately diverge", which is a decision the project has already
  made once (the `B-07` stderr drain) and has the machinery to record.
- **The failure paths are where the divergence is.** The happy paths were ported
  with the reference open; the failure paths were ported from memory of what a
  failure path looks like. `BUG-01` through `BUG-10` are all on that seam — as,
  in the most expensive instance, is `BUG-35`.

---

## Findings

**On the numbering.** The table is sorted by severity, as required; the IDs are
assigned in discovery order and are therefore **not** monotonic (the P0 at
`BUG-35` was found last). IDs are stable so that the prose references below —
and any other document that cites one — keep pointing at the same finding.
Finding sources are noted in each row: most are my own reading or reproduction,
and the rows marked *sub-audit* came from a delegated sweep whose citation and
load-bearing measurement I re-checked myself before including it. **Two rows,
`BUG-46` and `BUG-47`, are contributed by the sibling
`docs/audit/COSMIC-UX.md`**; I read each mechanism claim at source before keeping
it — for `BUG-46` that `view_with_overlays` returns the form before consulting
the page and that neither `focus_library_search` nor `shortcut_for` clears
`game_form`, and for `BUG-47` the widget construction in `widgets.rs`. The
live-observation halves are the UX document's, not mine, and `BUG-47`'s is
recorded above as **withdrawn in its strong form** — the sibling's "long titles
overflow into the neighbouring tile" did not survive being measured, and what
stands is the narrower truncation defect.

**One row is withdrawn outright.** `BUG-11` (duplicate categories in the filter
dropdown) was my own reading of the sort/dedup keys, and running both
implementations on the same fixture refutes it: the reference returns 39
categories and so does this port, with the same members in the same ten
fold-groups. The row is struck through rather than deleted, because the
regression it *meant* to guard is real — degrading the sort's primary term
reproduces the described symptom exactly — and that regression now has a test
where before it had only a paragraph. Two claims were previously recorded as
refuted by measurement; this is the third, and the first that I wrote myself.

| ID | Severity | Finding | Evidence (`file:line`) | Root cause | Impact | Fix direction |
|---|---|---|---|---|---|---|
| **BUG-35** | **P0** | **Every runner install of a real Proton build is refused after the archive has been downloaded and extracted**, because the symlink check computes each link's parent directory relative to the *current scan directory* instead of the candidate root. `walk_links(root)` recurses as `walk_links(&path)`, so `root` is always the directory being scanned and `path` is always its direct child — `path.strip_prefix(root)` yields the bare file name and `.parent()` is **always `""`**. The escape test is then `normpath("" + "/" + target)`, i.e. `normpath(target)`, so **any target whose first component is `..` is judged to escape**, however far inside the tree it lands. Wine builds are full of exactly those. *Sub-audit (agents `a4a7776ae5fb4959f`); claimed figure re-measured by me.* | `crates/core/src/runners/archive.rs:748-752` (the pinning), `:757-770` (the escape test), `:772` (the recursion that re-roots it), `:721` (called from `validate_staged_runner`); `crates/core/src/runners/proton.rs:1073-1074` (`extract_archive` **then** `validate_staged_runner`). Contract: `gamehandler/runners.py:644-657` — `path.relative_to(candidate).parent`, where `candidate` is fixed and `os.walk`'s `current` is not. **Measured three ways:** (1) the real function driven from a probe binary — `bin/link -> ../wine` → `Err(EscapingLink)`, `bin/i386/link -> ../../wine` → `Err(EscapingLink)`; (2) Python's rule on the same trees — `normpath("bin/../wine")` is `"wine"`, inside; (3) the installed `~/.local/share/Steam/compatibilitytools.d/Proton-CachyOS Latest` — **2,068 symlinks, Python's rule refuses 0, this rule refuses 1,818** | One recursive parameter used for two meanings: "the directory to scan" and "the root the relative path is computed from". Python keeps them distinct (`os.walk` yields `current`; `candidate` stays fixed) and the port merged them when it replaced the generator with direct recursion | The runner install — the feature the whole Runners page exists for — downloads a 100 MB+ tarball and then fails with `Refusing escaping link in runner`, which reads as a security refusal of the user's archive. GE-Proton and Proton-CachyOS (`crates/core/src/runners/families.rs:120-123`) both contain such links. The check is only ever **over**-strict, so nothing escapes and this is not a security hole — it is a functional break. Corroborating residue on this machine, with the caveat stated plainly: five abandoned staging directories under `~/.local/share/gamehandler/runners/` (`.install-735fdpye` … `.install-zrkihfsw`, all 0700, each with a `runner.archive` of **0 bytes** and an empty `extracted/`), left behind because `StagingDirectory::drop` (`crates/core/src/runners/proton.rs:774-780`) never ran. Those five died in the *download*, not at this check — so they are not evidence of the refusal; they are evidence that **no install on this machine has ever reached the post-extraction check**, which is why the defect survived a T-19 walk that reported the Runners page as verified | Thread the base through the recursion (`walk_links(base, current)`), compute `relative_parent` as `path.strip_prefix(base).parent()`, and keep the lexical landing test as it is — it is correct once its input is. Add the missing case: **`subdir/link -> ../sibling`**. The two existing tests cannot see this — `a_relative_symlink_that_stays_inside_is_allowed` (`archive.rs:1371-1382`) is a depth-1 link with no `..`, and `a_symlink_that_escapes_once_moved_is_refused` (`:1339-1352`) uses `../bridge/back`, which both rules refuse. That pair passes while the feature is broken, which is this project's dominant defect class in its most destructive form: the check compares a quantity that is structurally always `""` **Status: FIXED.** Fixed at `crates/core/src/runners/archive.rs` by threading the base directory through the recursion so `relative_parent` is computed against the candidate root rather than the current scan directory, which is what Python's `path.relative_to(candidate).parent` does. The missing case this exposed — **`subdir/link -> ../sibling`** — is now a test; the two that existed (a depth-1 link with no `..`, and a link both rules refuse) passed in every state of the world including the broken one. |
| **BUG-01** | **P0** | A `games.json` the app cannot parse is reported and treated as an **empty library**, and the next write makes the loss permanent. `load_at` clears the in-memory library first, discards the parse error, and returns `()` — there is no load-status field on `Library` and no caller that can distinguish "no games" from "one bad byte". Every subsequent mutation (`add`, `update`, `remove`, `mark_played`) then writes the emptiness back over the user's file via `save`. | `crates/core/src/models.rs:568-583` (`let Ok(source) = … else { return; }`, `let Ok(Value::Array(entries)) = json::parse_lenient(..) else { return; }`, after `self.games.clear()` at `:569`); `crates/core/src/models.rs:604-613` (`save`/`save_to` write unconditionally); `crates/core/src/models.rs:645-673` (`add`/`remove`/`update`/`mark_played` all `save`); no backup/refusal path anywhere (`grep backup\|load_error\|corrupt` → prose only). **Executed:** with `GAMEHANDLER_CONFIG_HOME` pointed at a config dir holding `[{"id": "alpha-1", "name": "Alpha"` (truncated), `gamehandler --list` printed `GameHandler: the library is empty` and exited **0**, and the file was left as-is until a write | Python's `models.py:128-147` has the same swallow, so this is inherited — but Python has no `load_status` to add and this port is the last place the decision can change. `parse_lenient` is all-or-nothing: the per-entry skip at `:577-583` only runs *after* the whole document parses, so one syntax error anywhere in a 5,000-game file empties all 5,000 | The user sees an empty library, adds one game (or marks one played), and the original file is gone with no message at any point. Reachable triggers, none exotic: a single syntax error; invalid UTF-8 anywhere in the file (`read_to_string` rejects it); `EACCES`; a disk error | Give `Library` a `load_status` set by `load_at`; make `save*` refuse (or rename the original to `games.json.corrupt.<ts>`) when the loaded state was `Unreadable`/`Unparsable`; surface the status as a toast at startup and as a non-zero `--list` exit **Status: FIXED.** Fixed by giving `Library` a load status and making the write path refuse to overwrite a file it could not read, so no parser divergence can be destructive. Paired with BUG-02, which is the input that made the loss reachable. |
| **BUG-02** | **P0** | A `games.json` that **the reference Python app writes** cannot be read by the port at all: `serde_json` rejects a lone UTF-16 surrogate in a `\uXXXX` escape, CPython's `json.loads` accepts it. Combined with `BUG-01`, the first user action after upgrading destroys the library. | `crates/core/src/json.rs:102-114` (`parse_lenient` hands the text straight to `serde_json::from_str`); `crates/core/src/json.rs:213-264` (`sanitize` never inspects `\u` escapes) and `:270-280` (`string_end` skips string bodies by design). The writer: `gamehandler/covers.py:325` sets a game's name from `Path(exe_path).stem`, so a non-UTF-8 filename goes through `os.fsdecode`'s surrogateescape and `json.dumps(ensure_ascii=True)` emits a lone surrogate verbatim | Strictness mismatch in one direction: the sanitizer exists to be *more lenient* than `serde_json` (NaN, Infinity, trailing commas, `1e400`), and this case was missed. Verified by compiling this tree's own `json.rs` unmodified and diffing against `python3 -c 'json.loads(…)'`: `[{"name":"Caf\udce9"}]` → Python `Ok`, port `Err(lone leading surrogate in hex escape)`; the same in a **key** → same; a surrogate-free file → both `Ok` | A library created by the reference app on a machine with a non-UTF-8 filename in any game path loads as empty, and `BUG-01` then overwrites it. The existing tests do not cover it (`crates/core/src/json.rs:612` `parse_lenient_reads_what_python_reads`, `:549` `sanitize_leaves_ordinary_documents_untouched`) — an assertion set that passes while blind to the one input that loses data | In `sanitize`, detect an unpaired `\uD800-\uDBFF` / `\uDC00-\uDFFF` escape in a string body and rewrite it to `�`; independently, fix `BUG-01` so no parser divergence can ever be destructive **Status: FIXED.** Fixed at `crates/core/src/json.rs:213-264` — `sanitize`'s string-body branch now detects an unpaired `\\uXXXX` surrogate escape and rewrites it to the replacement character, and sets `rewrote = true` so its own edit is not discarded by the `if !rewrote` fast path. That second half was a defect in my first attempt: without it the rewrite was computed and thrown away. `parse_lenient_reads_what_python_reads` plus the surrogate fixtures fail without it (2 of the 5 new assertions; the other 3 pass either way and are not load-bearing, stated plainly). |
| **BUG-03** | **P1** | `"Removed {runner}"` is toasted for a removal that never happened. `uninstall` returns `Ok(())` for a symlinked or absent target, and `uninstall_line` maps `Ok(())` to the success sentence. A **symlinked** build is listed as installed (`installed_protons` filters on `child.is_dir()`, which follows symlinks, and `ProtonRunner::is_available` then resolves a wine binary or `proton` script through the link), so the row is drawn with a working Remove button. | `crates/core/src/runners/proton.rs:1116-1118` (`if target.is_symlink() \|\| !target.is_dir() { return Ok(()); }`); `crates/app/src/view/runners.rs:703-708` (`Ok(()) => format!("Removed {runner_id}")`); `crates/app/src/view/runners.rs:1120-1127` (`remove_runner` calls it); `crates/core/src/runners/mod.rs:1060-1068` (`installed_protons`) and `:687-688` (`ProtonRunner::is_available`) | A no-op is expressed as the same value as a success — the reference's `uninstallRunner` also silently returns, but Python's rows are a re-read `Property`, so it has no "Removed" sentence to falsify | The user removes a build, is told it is gone, and it is still there (the row reappears on the next refresh). Two different outcomes share one message, which is precisely the class the brief asks about | Return a distinguishable `Ok(Removal::AlreadyGone)`, or an `Err`, and render "Nothing to remove for {id}"; or have `installed_protons` skip symlinks so the button cannot exist **Status: FIXED.** Fixed at `crates/core/src/runners/proton.rs:1116-1123` — a symlinked target is unlinked with `remove_file` rather than treated as absent, so the row's Remove button removes what the user is looking at. The Python reference toasts `Removed {id}` unconditionally while its `uninstall` returns early for a symlink, and its `shutil.rmtree` on a symlink raises — so the guard is load-bearing there and the defect is inherited. The port **deliberately diverges**, keeping the safety property (never delete *through* the link) while making the success message true. `a_listed_symlinked_build_can_actually_be_removed` fails without it. |
| **BUG-04** | **P1** | `open_prefix_folder` discards the `xdg-open` spawn result, so the prefix-folder menu item reports success when nothing opened. On a host without `xdg-open` (a minimal Flatpak, a container image) `spawn()` fails with `ENOENT`, the error is dropped, and `Ok(())` takes the success arm, which pushes no toast. **The sibling function 40 lines below does it correctly** — `open_url` maps the same failure to a message that is toasted. | `crates/app/src/main.rs:2804-2809` (`let _ = std::process::Command::new("xdg-open") ….spawn(); Ok(())`); success arm at `:2183-2186` (`Ok(()) => cosmic::task::none()`); contrast `:2845-2847` (`open_url`'s `map_err(format!("Could not open {url}: {error}"))`) and `:2204` (its toast) | Two sibling functions written to different standards; the doc at `:2800-2802` argues the child is deliberately unreaped and generalises that to the *result* being discardable | The click does nothing and says nothing. `FEATURES.md` `P-49` is therefore only Partial | `let result = …spawn().map(\|_\| ()).map_err(…)` and toast on `Err`, exactly as `open_url` does **Status: FIXED.** Fixed at `crates/app/src/main.rs` — `prefix_folder_command` plus a shared `spawn_detached` helper that maps a failed `spawn()` to a message naming what could not be opened, exactly as the sibling `open_url` already did. Verified end-to-end on the real binary rather than by a restorable pre-state; `a_detached_spawn_that_cannot_start_says_so` pins the exact string. |
| **BUG-05** | **P1** | A game's `additional_app` helper that never started is reported as a launch that honoured the configuration. The helper's `spawn()` error is discarded with `let _ =`. | `crates/core/src/runners/launch.rs:373-379` (`let _ = Process::new(&extra_argv[0]) … .spawn();`); the comment at `:371-372` says the failure "is not the title's" | Python's bare `subprocess.Popen(extra_argv, env=env)` raises `FileNotFoundError`/`PermissionDenied` and that propagates out of `launch()` into `Could not launch "<name>": …` | A missing or non-executable helper binary is silently dropped; the title launches and the user has no way to know their trainer/overlay did not | `?` the spawn result, matching Python's abort **Status: FIXED.** Fixed at `crates/core/src/runners/launch.rs:373-379` — the helper's spawn result is now propagated with `?`, matching Python's `subprocess.Popen` raising out of `launch()`. The first version of the test measured the wrong binary (a Windows title prepends `runner_executable`, so `extra_argv[0]` was the runner and the error was a `PermissionDenied` for a helper that provably did not exist); it is now a **Linux** title, so `extra_argv` is the helper alone. Verified by reverting the `?`. |
| **BUG-06** | **P1** | An empty `GAMEHANDLER_DXVK_ROOT` silently launches **without DXVK** where the reference refuses. `Env::var(..).map(PathBuf::from)` turns `""` into `PathBuf::from("")`, and `Path::new("").is_dir()` is `false` (empty path → `ENOENT`), so the `game.dxvk && !uses_proton && dxvk_root.is_dir()` gate is skipped entirely. Python's `Path("")` normalises to `"."`, whose `is_dir()` is `True`, so `install_bundled_dxvk` is entered and raises `Bundled DXVK runtime is unavailable`. | `crates/core/src/runners/launch.rs:352-356`; the divergence is *argued the other way* in the port's own docs at `crates/core/src/runners/launch_opts.rs:402-406`, and the test that pins the refusal (`launch_opts.rs:2094` `an_override_pointing_nowhere_fails_rather_than_falling_back_to_the_built_in_root`) covers only `install_bundled_dxvk`'s `None` path, never `launch`'s pre-gate — so the branch is untested | A lossy `PathBuf` conversion used as a boolean | A box that exports the override empty (a common way to "unset" it) gets a game launched with no DXVK DLLs and no DXVK `WINEDLLOVERRIDES`, silently, where the reference aborts with a message | Gate on the decision, not on `is_dir()` of a lossy path: treat `""` as `Path::new(".")`, or pass `None` and let `install_bundled_dxvk` fail with `DxvkUnavailable` — which is the state Python reaches **Status: FIXED.** Fixed at `crates/core/src/runners/launch.rs` — an `empty_as_dot` helper turns an empty override into `Path::new(".")`, whose `is_dir()` is true, reproducing Python's `Path("")`. Writing the test corrected the finding itself: a *nonexistent* override path is **not** refused, in either implementation (`Path("/typo").is_dir()` is `False` in Python too), so that half is pinned as anti-vacuity showing the fix is narrow. The empty case is refused by both. |
| **BUG-07** | **P1** | The Library's primary create flow — **"Add game"** — has no visible control once the library holds at least one game. The reference draws it as a page-header action that is present whether or not the library is empty; the port draws the equivalent button **only inside the `NoGames` empty state**. | Reference: `gamehandler/qml/LibraryPage.qml:18-23` (`actions: [ Kirigami.Action { text: "Add Game"; icon.name: "list-add"; onTriggered: root.openGameForm("") } ]`). Port: `crates/app/src/view/library.rs:339` (`.push(button::standard(ADD_FIRST_GAME).on_press(Message::OpenNewGameForm))`) is inside `EmptyState::NoGames` only; `grep -rn "OpenNewGameForm" crates/app/src/view/` returns exactly that one production hit; the toolbar (`crates/app/src/view/library.rs:294-329`) draws search, category, sort and the view toggle and nothing else; `EmptyState::None` (`:357-363`) draws only the grid or the list | The reference's four page-header `actions:` blocks were ported as page content; three of the four (form Cancel/Save, Runners Refresh, Library Add) were relocated or lost in the move | With ≥1 game the only route to the form is `Ctrl+N` (`crates/app/src/shortcuts.rs:146`), which is invisible in the UI **and** is swallowed while a text field has focus (`crates/app/src/shortcuts.rs:86-102`, measured at T-19). A fresh user who adds one game can then only add a second by discovering an undocumented accelerator | Add an "Add game" button to the toolbar row at `crates/app/src/view/library.rs:299-327` emitting the existing `Message::OpenNewGameForm`; no new handler needed **Status: FIXED.** Fixed at `crates/app/src/view/library.rs:299-330` — an `ADD_GAME` button pushed into the toolbar row, emitting the existing `Message::OpenNewGameForm`. `the_toolbar_offers_add_game_once_the_library_is_not_empty` walks the built widget tree; reverting the button fails it with the page's own drawn strings in the message (`["Switch to list", "HA", "Hades", "System Wine", "Play"]`), which is also the proof that no test had ever built `view()` for this page. |
| **BUG-08** | **P1** | The Plugins page never re-detects the host. `Message::RefreshPlugins` has a working handler and **no producer anywhere**, and `page_entry_task` handles only `Page::Runners`. The reference re-detects on every arrival at the page. | Reference: `gamehandler/qml/Main.qml:41-44` (`function showPage(name) { … if (name === "plugins") backend.refreshPlugins() }`). Port: `crates/app/src/main.rs:2512-2515` (the handler), `crates/app/src/main.rs:1164-1175` (`page_entry_task`'s `_ => cosmic::app::Task::none()`), `crates/app/src/main.rs:3679` (rows primed once at `App::init`), `crates/app/src/main.rs:2537` (rebuilt after an in-app install). The only constructions of the variant in `crates/app/src` are two `#[cfg(test)]` decoys (`crates/app/src/view/installers.rs:1157`, `crates/app/src/view/runners.rs:2228`). `crates/app/src/main.rs:1549` describes it as "the refresh" among the page's live controls | A page-entry hook that was only ever built for one page, so the second page's hook has an arm and no trigger | Install MangoHud in a terminal, click Plugins — the button still says "Install" and stays that way for the session. Adding the helper from outside the app is the ordinary way to install it | Add a `Page::Plugins` arm to `page_entry_task` (`crates/app/src/main.rs:1164`) calling `state.refresh_plugins(…)` — which is exactly what the reference's `showPage` does **Status: FIXED.** Fixed at `crates/app/src/main.rs:1189-1202` — a `Page::Plugins` arm on `page_entry_task` calling the same `state.refresh_plugins(SystemPluginEnv)` the message's handler calls, so there is one implementation of re-detect. `arriving_at_the_plugins_page_re_detects_the_host` empties the rows first and asserts on the state the page draws, not on the returned task (this arm's task is `Task::none()`, so a task-unit count would pass on an arm that did nothing). Reverting the arm fails it with `left: 0, right: 5`. |
| **BUG-09** | **P1** | The Runners page has **no Refresh control**, so after a failed fetch the error is on screen with nothing to press. The reference's action exists specifically for that state and is enabled precisely because the status is not `loading`. | Reference: `gamehandler/qml/RunnersPage.qml:21-26` (`text: "Refresh"`, `enabled: backend.releasesStatus !== "loading"`, `onTriggered: backend.fetchReleases(page.selectedFamilyId)`). Port: the whole toolbar is `crates/app/src/view/runners.rs:433-445` (a "Family:" label and a dropdown, which only fires on *change*); the failure text is built at `:216-218` and drawn at `:458-460` as a bare `text::body`; `grep -rn "Refresh" crates/app/src/view/*.rs` returns only `Message::RefreshPlugins` inside a test | Same relocation as `BUG-07`/`BUG-08`: the page-header `actions:` block was not ported | After a rate limit or an offline start the user sees `Could not fetch builds — …` and must discover that switching family away and back is the retry | Add a Refresh button beside the dropdown emitting `Message::FetchReleases { family }`, gated with `on_press_maybe` on `status != Loading` (`crates/app/src/view/plugins.rs:263` is the existing pattern) **Status: FIXED.** Fixed at `crates/app/src/view/runners.rs:457-463` — a `button::standard(REFRESH)` beside the family dropdown, gated with `on_press_maybe` on `status != ReleasesStatus::Loading`, naming the *currently selected* family so a refresh cannot silently move the user. `the_runners_page_offers_refresh_when_a_fetch_has_failed` walks the built widget tree and asserts both halves — the control is drawn in the error state, and the error text stays beside it. Reverting the button fails it with the page's drawn strings. |
| **BUG-10** | **P1** | `stage_test` has no floor on the number of tests run: an emptied or fully `#[ignore]`d workspace is green. The stage asserts `cargo test`'s exit code and that each crate recompiled — never that anything executed. **A check that passes without inspecting what it claims**, at the level of the entire Rust suite. | `scripts/verify.sh:977` and `:996` (`[ "$status" -eq 0 ] \|\| return 1`, twice); `:920-933` (`require_compiled`, which greps for `^ +Compiling ${crate} v`); measured: a crate with no `#[test]` at all → `running 0 tests / test result: ok. 0 passed` → rc 0, and the same crate with its only test `#[ignore]`d → rc 0. `cargo clean -p` and `Compiling` both still succeed, so every `ok` line prints | The exit-code-only idiom, applied to a runner whose zero-test case is a success | Delete every `#[test]` in the workspace and the gate stays green. The one guard that would notice is the oracle-freshness check, which is a *different* stage | Parse each config's `test result: … N passed` lines, require N above a checked-in floor, and FAIL when no `test result` line is present **Status: FIXED.** Fixed at `scripts/verify.sh` — `stage_test` now calls `require_tests_ran`, which parses each config's `test result: … N passed` lines and fails when none is present or the total is under a checked-in floor (900 workspace-wide, 500 for the core crate alone). Driven directly with four cases: the real run, an empty suite, a suite under the floor, and a config that produced no `test result` line at all. |
| ~~**BUG-11**~~ **WITHDRAWN** | — | **Withdrawn 2026-09-13: false positive; the code is correct.** The claim was that `Vec::dedup` after the sort loses case variants where Python's `set` keeps them, because the sort key (`to_lowercase()`) and the dedup key (`PartialEq`) are different keys. The premise is wrong: the sort's **primary** term *is* the fold, so entries whose folded forms are equal always land adjacent and `dedup` removes exactly what the `set` removes — nothing can sort between two entries that compare equal on the leading term. Re-measured against the reference on a 40-game fixture (ten base categories × spelling/upper/lower/alternating, run through the real `Library.categories`): the reference returns **39** entries and this port returns **39**, the single collapse being `RPG`, whose upper form is itself. Same length, same set, and the same **ten** fold-groups with the same members. The two lists are *not* identical element-for-element, and the test now written deliberately does not claim they are: within a fold-group the reference's order is its `set`'s hash order, which is arbitrary, while this one keeps insertion order — a distinction no dropdown can show. The earlier "60-game fixture, 14 entries, `Puzzle` repeated up to 8 times" figure did not reproduce and its provenance is unknown. | `crates/core/src/models.rs:799-813` vs `gamehandler/models.py:201-203`; measured by running both implementations on the same fixture rather than by reading the sort key. **The regression it was meant to guard is real and is now guarded:** changing the primary sort term to `a.cmp(b)` reproduces the described symptom exactly — 28 groups instead of 10, with `PUZZLE`/`puzzle`/`Puzzle` in three separate groups — and `case_variant_categories_fold_into_the_same_groups_as_python` fails on it | A set-vs-adjacent-dedup translation that was *assumed* rather than executed; the two keys are not in fact different | None — the dropdown lists distinct categories, as its doc claims | None. Regression test added at `crates/core/src/models.rs` |
| **BUG-12** | **P2** | The Settings page advertises four keyboard shortcuts; **three of them do not fire while a text field has focus**, which is the state a user is most often in when reaching for one. The port's own module doc records the divergence and the reason (a focused `text_input` swallows the key despite not binding it, so Qt's `Qt.ApplicationShortcut` semantics are not reproduced), and the guard that is meant to catch a mismatch cannot: it greps `main.rs` for the *text* `keyboard::listen()` and asserts a non-empty list. | Advertisement: `crates/app/src/view/settings.rs:107-112` (`SHORTCUTS`, four rows) rendered at `:509-513`. Divergence, measured by the project: `crates/app/src/shortcuts.rs:86-102`. Guard: `crates/app/src/view/settings.rs:687-694` (`text.contains("fn subscription") && … .contains("keyboard")` feeding an `assert_eq!`); `UNWIRED_SHORTCUTS` at `:168` is `[&str; 0]` and its doc (`:158-166`) defines a category that does not cover a *conditionally* wired accelerator | A source-text check standing in for a behaviour check, over a divergence that is conditional on focus rather than on existence | The page a user visits to learn the shortcuts is wrong in the common case. `Ctrl+F` is the exception — libcosmic binds it (`crates/app/src/main.rs:3756`, `on_search` at `:3775`) | Render the caveat in the row text (or add a third list for the condition) and make the guard assert the caveat is *rendered*, not that a word appears in `main.rs` **Status: FIXED — both halves.** The informational half: the page no longer needs a caveat, because the behaviour it would have apologised for is gone. `SHORTCUT_FOCUS_CAVEAT` and the test that drew it (`the_shortcut_caveat_is_drawn_with_the_rows_it_qualifies`) are **deleted**, not qualified — a page that apologises for a defect it no longer has is the same lie as one that stays silent about a defect it does. The behavioural half: `subscription()` composes `event::listen_raw`, and its handler `accelerator` (`crates/app/src/shortcuts.rs:187`) no longer filters on `Status` at all, so all four accelerate from `Captured` as well as `Ignored`. `Ctrl+F`, which libcosmic's `keyboard_nav` subscription claims and gates on `Ignored`, is answered by `search_the_framework_cannot_reach` (`:217`) in exactly the complementary status, so the two are mutually exclusive by construction rather than doubly bound. `shortcuts.rs:86-102`, cited above as the divergence, now holds the record of its resolution — a focused `text_input` captures *every* key because libcosmic's input ends its focused branch with `shell.capture_event()` unconditionally (`src/widget/text_input/input.rs:2261-2262`), so the editing-bindings list was the list of keys it *acts* on, not the list it lets through. The predecessor guard is replaced by tests that measure rather than read: `an_accelerator_reaches_the_shell_while_a_text_field_has_focus` (`shortcuts.rs:583`) builds the Library's real search box, takes the focus through the same operation Tab runs, *measures* that the tree leaves `Captured`, and takes the reference's own message out of the subscription's other end — with a control asserting that a typed `x` reaches `on_input`, so a tree that never took the focus cannot make it pass vacuously. `every_row_this_page_prints_is_a_key_the_window_answers` (`view/settings.rs:910`) drives all four rows against the rows the page actually prints, in the page's order, and requires **exactly one** message per key so that binding `Ctrl+F` twice fails. `the_record_beside_the_rows_names_where_they_are_answered` (`:1038`) is the one remaining source read, and only because the property *is* about documentation, which has no runtime representation. Mutation-verified from the lead's tree rather than taken on report: restoring the old `status != Status::Ignored` gate in `accelerator` fails this row's regression test plus `the_search_key_is_answered_once_in_each_state` and `a_captured_keystroke_is_only_an_accelerator_with_exactly_control`, with `Ctrl+n` producing `[]` where the reference produces `openGameForm("")`. **What remains outside this row's reach:** a *window* — that winit delivers the key to the runtime at all, and that `on_search`'s focus operation lands on the search box — recorded as a Phase 3 T-19 item in the module doc (`shortcuts.rs:110-113`), exactly as P-68's own acceptance says. |
| **BUG-13** | **P2** | A cover-art **write** failure is reported to the user as "no Steam cover found". `icon_cover(...).ok()` destroys the `SaveIconError`, so a full disk, a read-only `covers_dir` and a genuinely absent icon are one outcome, and the message the user reads names the wrong subsystem. `SaveIconError::Io` exists precisely to carry it. | `crates/core/src/covers.rs:1048-1056` (`.and_then(\|exe\| icon_cover(query, exe, game_id, covers_dir).ok())` at `:1050`, then `Err(CoverError::SteamFailed { message: steam_error })` at `:1055`); the rendered text is `covers.rs:627`; `SaveIconError` at `:544` with its `Io` variant at `:554` | `.ok()` on an error that the function has a variant for; the reference's bare `pass` mirrors it, and the port already keeps `steam_error` for provenance (`:1044-1047`) but not the icon error | The actual cause is unreachable from the message, and the artwork was present — the user is told the wrong thing about why their cover did not appear | Keep the `SaveIconError` beside `steam_error` the same way and include it when the icon was the only candidate. **Status: FIXED.** Fixed at `crates/core/src/covers.rs` — `CoverError::IconWriteFailed { steam_error, covers_dir, icon_error }` carries the `SaveIconError` the `.ok()` destroyed, plus the directory, because the `io::Error` names no path; built only from `SaveIconError::Io`, so `NoIcon` stays silent and faithful. Verified by restoring the `.ok()` body: 47 of 48 covers tests passed under the defect, so the module had been certifying it. The test's `covers_dir` is a regular *file* rather than a mode-restricted directory, so the `create_dir_all` failure is `EEXIST` under any uid and umask — a `0500` directory would have been writable to a root runner and the assertion vacuous. |
| **BUG-14** | **P2** | `read_metadata` collapses four distinct failures into an empty map, and the consequences are silent. A symlinked root, a missing file, an unreadable file, invalid UTF-8, unparsable JSON and a non-object document all return `Map::new()`; `read_family_id` then returns `""`, which yields the wrong family label **and** makes the pre-namespacing install scan unable to match — so a legacy install whose metadata is merely damaged reads as *not installed*, which is the exact outcome that scan exists to prevent. | `crates/core/src/runners/mod.rs:977-995`; `:998-1006` (`read_family_id`); `:611-621` (`family_label` → `"Downloaded runner"`); `crates/core/src/runners/proton.rs:640-647` (the legacy scan), with its own rationale at `:590-592` ("the UI would offer to install a build that is already on disk") | A deliberate port of Python's `_read_metadata` — the port documents a *different* divergence there (`crates/core/src/runners/mod.rs:964-975`, invalid UTF-8) and did not extend it to "unreadable" | A damaged metadata file is indistinguishable from a build with no metadata; the user is offered an install of a build they already have | Return a distinguishable empty-vs-damaged result and treat "damaged" as "assume installed" for the legacy scan. **Status: FIXED.** Fixed at `crates/core/src/runners/mod.rs` (`MetadataRead::{Object, Missing, Damaged}`) with the legacy scan's arm at `proton.rs`: damaged metadata now reads as installed, a deliberate divergence from Python recorded in place with its cost — the assumption is content-blind, so the directory also answers for other tags and families, and the UI offers Remove where it would offer Install. The symlinked root stays `Missing`, faithful to `runners.py:693-694`. Verified by restoring the collapse: both new tests fail, and mutating `Missing => true` fails the *first* assertion, so both directions are discriminated. Residual, stated rather than papered over: a damaged file has no user-visible surface of its own, because the family is unrecoverable and the label stays `"Downloaded runner"`. |
| **BUG-15** | — | An unknown runner family id **silently becomes the default family** instead of erroring, so `fetch_available` fetches and returns *a different family's releases*. Python raises `KeyError("Unknown runner family: {id}")`. | `crates/core/src/runners/proton.rs:443` (`family.unwrap_or(DEFAULT_FAMILY_ID)`), with Python's behaviour cited in the port's own doc at `:433-436` | `unwrap_or` conflating "no family given" with "the family given is not in the catalogue" | The page shows builds the user did not ask for, badged with the wrong family. Verified **unreachable from the current GUI** (`crates/app/src/view/runners.rs:784` passes a catalogue-derived id) — reachability is via any future caller, which the surrounding docs explicitly allow | `match family { None => default, Some(id) => family_by_id(id)? }` **Status: CLOSED — NOT A DEFECT.** The recorded behaviour does not occur: `resolve_family` (`crates/core/src/runners/proton.rs:442-444`) applies `unwrap_or` to the `Option` *inside* a call that returns a `Result`, so `None` takes the default and `Some("nonsense")` propagates Python's `KeyError` text byte-identically. The function had **no test at all**, which is how the row was written without the measurement to settle it. It now pins default, named and unknown as three separate assertions — the defect is the conflation of two of them — and reintroducing that conflation (a `match` falling back to the default for any unresolvable id) fails the test. |
| **BUG-16** | **P2** | Both `desktop-metainfo` and the cargo-sources coverage half pass **without inspecting what they claim**. (a) When the Flatpak build tree is absent — a `flatpak-build` that failed under `--keep-going`, or a stale tree — the stage silently re-points at the repo's *source* files and still prints `ok desktop-metainfo`, so a reader takes "installed copies validated" from a run that validated the sources. (b) The cargo-sources check decides on `missing`, never on `count`: if the lock becomes registry-only or the `source = "git+…"` shape changes, the loop body never runs, `count=0 missing=0`, and the stage passes having covered nothing. | (a) `scripts/verify.sh:1687-1688` (`[ -f "$desktop" ] \|\| desktop="$ROOT/data/$APP_ID.desktop"`, same for metainfo), with the fallback echoed at `:1689-1696` and the verdict line unchanged. (b) `scripts/verify.sh:1550` (`[ "$missing" -eq 0 ] \|\| return 1`) fed by the `sed` at `:1530`, with the count printed but never required at `:1541-1549`. The generator half at `:1598`/`:1606` is genuinely strong — regenerate-and-set-compare — so only the coverage half is unbounded | A fallback that preserves the *label* while changing the *subject*; and a count that is reported rather than required | Both stages can report green having examined nothing (a) or nothing relevant (b) | (a) return a distinct skip code when the fallback is taken, or split "sources well-formed" from "installed copies validated"; (b) fail when `count -eq 0` **Status: FIXED.** `stage_desktop_metainfo` now tracks which subject it read and returns 99 (prerequisite absent) or 98 (`--skip-flatpak`) instead of 0 when it fell back to the repository's sources rather than the installed copies; the cargo-sources coverage half now requires a nonzero `count`, so an empty comparison fails rather than passes. Both halves verified by restoring the pre-fix body — the fallback path returned 0 and the empty comparison passed. Committed `332b2ab`. |
| **BUG-17** | **P2** | The placeholder guard cannot see a renamed placeholder, and the "no-results" test for the installers page asserts nothing about what is drawn. (a) `Arm::is_pending` is `body.starts_with("pending_page(")` and `PINNED_PENDING` is empty, so a page whose body became `todo_page(..)` or `container(text::body("Coming soon"))` is classified as *ported* and both tests stay green. (b) `an_empty_catalog_renders_the_placeholder_rather_than_nothing` builds the element and drops it — the only property observed is "does not panic". | (a) `crates/app/tests/pending_pages.rs:170-171` (`is_pending`), `:82` (`const PINNED_PENDING: [&str; 0] = []`), `:229` (`body: body.trim().to_string()`) — and note `Arm.body` is built from the `Page::` line only, so a placeholder moved into one of `view_body`'s five block arms (`crates/app/src/main.rs:1357`, `:1386`, `:1406`, `:1417`, `:1431`) yields body `"{"` and is also invisible. `scripts/verify.sh:1944-1949` defers this whole claim to this file. (b) `crates/app/src/view/installers.rs:1443` (`let _element: Element<'_, Message> = view(page);`) — its sibling at `:1426` has real assertions before the same line | A pin that asserts the *absence of one spelling* rather than the presence of the property it stands for; and a smoke call in place of an assertion | (a) is guarded against a collapsed parse (`assert!(!arms.is_empty())` at `:337`, plus the `Page::ALL` count check) but not against a rename — which is the failure mode it exists for. (b) Replace the empty-catalog branch with a blank column and the test still passes under a name that claims otherwise | (a) assert the positive: every `Page` arm contains a call to a `view::<module>::view(` whose module is in `view/mod.rs`, and parse arm bodies with brace matching as `crates/app/src/view/form.rs:1697` already does. (b) traverse the built element and assert the placeholder text is among the drawn strings, as `crates/app/src/view/settings.rs:753` (`drawn_strings`) does **Status: FIXED.** (a) `Arm::body` is brace-matched from the arm's own `=>` and a new guard, `every_page_arm_draws_a_view_module`, requires every arm to call `view::<module>::view(` for a module `view/mod.rs` declares — verified by renaming a page body to a non-`pending_page` placeholder and watching it fail. (b) the empty-catalog test now walks the built tree with `drawn_strings` and asserts the placeholder heading, explanation, page explainer and Catalog heading are drawn and no Install button is. Committed `332b2ab`. |
| **BUG-18** | **P2** | The notify-voice guard's haystack contains its own needle: `read_crates` walks back in the same `#[cfg(test)] mod tests` that holds the `NOTIFY_VOICES` literal table, so `port.contains(voice)` is satisfied by the table itself and cannot fail. Deleting the port of any notice leaves the assertion green. | `crates/app/src/main.rs:9286` (`assert!(port.contains(voice), …)`), haystack built by `read_crates` at `:9217-9238`, needle table `const NOTIFY_VOICES` at `:9092` — all inside the same test module. All 39 voices also occur in real production code today, so the check currently agrees with reality; the blindness is latent | A grep whose input includes the pattern | One voice (`"toast_task(result.unwrap_err())"`) already passes on a bare substring of two production call sites (`crates/app/src/main.rs:2273`, `:2338`), which is the shape that will drift | Cut the test module out of the haystack, as `crates/app/tests/dispatch_coverage.rs:520`'s `production_src` does, or require the voice in a file other than the emitting test module **Status: FIXED.** `read_crates` now calls `without_test_code(&text)`, which cuts `#[cfg(test)] mod tests` at the module level, with two self-checks at `crates/app/src/main.rs:10058` and `:10080` asserting the instrument actually engaged before its result is used. Verified in both directions: a deleted production voice fails, and a `read_crates` reverted to the raw concatenation leaves the haystack holding its own needle and also fails. Committed `332b2ab`. |
| **BUG-36** | **P2** | `SystemLaunchEnv::environ()` **panics** on any non-UTF-8 environment variable, aborting the process on the way into a launch. `std::env::vars()` is documented to panic on a name or value that is not valid Unicode — it is `vars_os()` with an `unwrap()`. Every launch reads this map, so the blast radius is the widest of any finding here. Python's `dict(os.environ)` cannot fail and passes the raw bytes through. | `crates/core/src/runners/env.rs:74-76` (`std::env::vars().collect()`); callers `crates/core/src/runners/launch.rs:451`, `crates/core/src/runners/mod.rs:522`, `:718`, `crates/core/src/runners/launch_opts.rs:792`. **Measured** with a probe binary linking this crate: `env "$(printf 'BAD\xff)=1')" <probe> environ` → `panicked at library/std/src/env.rs:168:57: called \`Result::unwrap()\` on an \`Err\` value: "BAD\xFF)"`, rc 101. A sibling site has the same shape one crate over: `crates/core/src/paths.rs:35-38` (`SystemEnv::var` → `std::env::var(key).ok()`), which turns a non-UTF-8 `HOME`/`GAMEHANDLER_GVFS_ROOT` into "unset" and silently falls back — `netpaths.rs:205-210` depends on it | A `String`-typed API standing in front of an OS interface that is bytes. `var`/`environ` were modelled on `os.environ`, which is `str`-keyed, but CPython gets there by surrogate-escaping rather than by failing | Game launch, and every code path that dumps the environment for an installer, dies on a launcher whose environment carries one non-UTF-8 variable — a path, a locale, a mangled `%`-escape in `WINEPREFIX`. The GUI has no recovery: it is an abort in the middle of `launch` | Build the map from `env::vars_os()` and keep `OsString` through to `Command::envs`; where a `String` map is genuinely required, lossy-encode at one documented boundary rather than panicking. For `SystemEnv::var`, `var_os` + `to_string_lossy` when the value is only ever used as a path **Status: FIXED.** Fixed at `crates/core/src/runners/env.rs:100-135` — `decode_environ` builds the map from `std::env::vars_os()` and drops entries that cannot be represented, where `std::env::vars()` **panics**. Confirmed by direct probe before the fix: a minimal `std::env::vars().count()` binary under a non-UTF-8 environment panics with `Err value: "\xFF"` at `library/std/src/env.rs:168`, rc 101 — matching the real binary's pre-fix rc 101. The fixed binary both launches (rc 0) and passes the environment through (a dumped child saw `HOME`/`MYVAR`/`PATH`). |
| **BUG-37** | **P2** | `which_in` answers `/bin:/usr/bin` when `PATH` is **set but empty**, where CPython returns `None` — so the port finds and injects wrapper programs the reference refuses to find. The `.filter(\|value\| !value.is_empty())` runs *before* the fallback, so `Some("")` and `None` become indistinguishable. The doc at `:88-91` asserts the wrong rule and offers the empty-`PATH` case as its rationale ("a Flatpak-launched app with a deliberately empty `PATH` still finds `/usr/bin/wine`") — which is precisely the case CPython refuses. | `crates/core/src/runners/env.rs:120-123`; `DEFPATH` at `:92`; doc claim at `:88-92`. **Measured against CPython 3.14.7 on this host:** `shutil.which('sh', path='')` → `None`; `PATH=''` with an executable `./sh` in cwd → `None`; unset → `/usr/bin/sh`; `PATH=':'` → `'sh'` (cwd). Port, same four cases: `Some("")` → `Some("/bin/sh")` (divergent), `Some(":")` → `None` (divergent), `None` → `Some("/bin/sh")` (agrees), `Some("/usr/bin")` → agrees. *Sub-audit; measured by me.* | The empty-value filter models "unset or empty" as one condition, which is right for `paths::home` and wrong here: `shutil.which` distinguishes them (bpo-35755) | `which` decides `mangohud`/`gamemoderun`/`gamescope` (`crates/core/src/runners/launch_opts.rs:730-745`), `wine` (`mod.rs:453`), `umu-run` (`mod.rs:729`), `winetricks` (`launch.rs:473`) and the package managers (`plugins.rs:262-274`). With `PATH=""` the port injects a wrapper into argv that the reference would not, and reports a runner available where the reference raises `Runner 'x' is not available`. The second measurement above is the same class in the other direction: `PATH=":"` must look in the current directory and the port searches two directories that were not asked for | `None => DEFPATH`, `Some(v) if v.is_empty() => return None`, `Some(v) => v`; correct the doc, and add the case that distinguishes them (the current test cannot: see the empty-entry test at `env.rs:290-330`) **Status: FIXED.** Fixed at `crates/core/src/runners/env.rs:155-200` — the `PATH` read is now `match env.var("PATH") { Some(v) if v.is_empty() => return None, Some(v) => v, None => DEFPATH }`. The first version of the new test used a scratch-directory probe and **passed under the defect**, because such a probe is missed whether the empty `PATH` searches `DEFPATH` or searches nothing; it now asserts on `sh`, which is on the default path and so separates the two readings. The `PATH=":"` half moved to `crates/core/tests/which_cwd.rs` — a separate process, because the unit test named for it never left the working directory and `set_current_dir` in the shared test process would move every concurrently running test's relative paths. Both halves verified by restoring the defect. |
| **BUG-38** | **P2** | `in_flatpak` short-circuits on an empty `FLATPAK_ID` where Python falls through to the marker file, so inside a sandbox that sets the variable empty the app does **not** believe it is sandboxed and offers the host's package manager. | `crates/core/src/plugins.rs:244-249` (`match env.var("FLATPAK_ID") { Some(value) => !value.is_empty(), None => env.exists("/.flatpak-info") }` — the `Some` arm is terminal); contract `gamehandler/plugins.py:107-109` (`bool(os.environ.get("FLATPAK_ID")) or Path("/.flatpak-info").exists()`), where `or` evaluates the right operand whenever the left is falsy and `""` is falsy. **Measured**: `bool(os.environ.get('X')) or Path('/etc/os-release').exists()` → `True` with `X=""`. *Sub-audit (agents `a4a7776ae5fb4959f`); contract re-measured by me.* | `match` on `Option` transcribed from an `or` — the two are equivalent only where the first branch is total, and here `Some("")` must fall through | `detect_package_manager` (`plugins.rs:258-278`) returns a host manager instead of `""`, so the Plugins page offers an install command that cannot work in the sandbox, and `install_hint` names the wrong mechanism. Reachable on any sandbox that starts the app with `FLATPAK_ID` exported empty | `if env.var("FLATPAK_ID").is_some_and(\|v\| !v.is_empty()) \|\| env.exists("/.flatpak-info")` **Status: FIXED.** Fixed at `crates/core/src/plugins.rs:245-257` — `if env.var("FLATPAK_ID").is_some_and(\|v\| !v.is_empty()) \|\| env.exists("/.flatpak-info")`, which is the reference's `or` shape. The existing test passed under both readings because it only exercised empty-*without*-marker; the fixture that shows the defect is empty *with* the marker, and it now also asserts that `detect_package_manager` returns `""`. Verified by restoring the `match` and watching `the_flatpak_marker_is_either_the_variable_or_the_file` fail. |
| **BUG-39** | **P2** | `create_partial` names the mode it does not set: the doc argues the temp file is `0600` "which `mkstemp` gives and `create_new` does not — and it is set explicitly below", but nothing below sets it. The file is created with the process umask. Python's `mkstemp` really does give `0600`. *Sub-audit; measured by me.* | `crates/core/src/installers.rs:1691-1694` (the claim), `:1699-1719` (the function: `create_new(true)` and no `set_permissions`); the only caller at `:1764`; no `set_permissions` call in the module outside tests (`grep set_permissions` → `:3267`, `:4030`, `:4055`, all inside `#[cfg(test)] mod tests` at `:1952`+); Python `tempfile.mkstemp` at `gamehandler/installers.py:612-613`. **Measured**: a `create_new` file under umask 022 → mode `644` | A comment describing the reference's guarantee, transcribed next to an implementation that does not reproduce it — the same shape as `BUG-23b` | The installer download sits at `0644` for the duration of the transfer on a default-umask host: readable by every local user, where the reference's is readable only by its owner. No privilege crosses (`crates/core/src/installers.rs` writes into the user's own data directory), so this is a hygiene defect rather than a disclosure — but the sentence asserting otherwise is what a reviewer would rely on | One `set_permissions(0o600)` on the returned handle, or state the divergence and drop the claim **Status: FIXED** (2026-09-13). The `set_permissions` branch was the right one: the file is the installer payload for the length of a network transfer, and `mkstemp` does give it 0600 (`installers.py:612-614`). Fixed at `crates/core/src/installers.rs:1708-1745` — `.mode(0o600)` on the `OpenOptions` that does the `create_new`, so the file is never wider than 0600 even for the instant before the explicit `set_permissions(0o600)` that follows (which covers a umask carrying owner bits). The doc comment now names the defect it had, and the two tests that pin it are `a_partial_download_is_private_to_its_owner_and_cannot_be_redirected` (the mode read from the real `.part` file, inside the transfer's own progress callback — the only moment it exists) and `a_guessed_partial_name_is_neither_followed_nor_reused` (planted symlinks at the names this process's id generates, which is what makes `create_new`'s exclusivity the load-bearing half). Both fail on the restored defect: `assertion left == right failed: left: 420 right: 384` (0644 vs 0600) and `the .part file was readable by more than its owner: [420]`. A battery of hostile names (`hostile_partial_names_fail_cleanly`) pins that no name is a panic, which is also the run's file-safety evidence. |
| **BUG-40** | **P2** | `bounded_attempt`'s "a bounded parse that yields an icon is final" claim is **false**, and the result it returns is silently the wrong icon. The bounded view reads only through the resource section's raw bytes; a lower-numbered RT_GROUP_ICON whose payload lives in a *second* section is unreadable there, so the group loop falls through to a higher-numbered group and `extract_icon` returns it without ever consulting the full file. *Sub-audit (agents `a3009904bc0853d69`), reproduced by the agent against the reference; citations re-read by me.* | `crates/core/src/exe_icons.rs:212-214` (the claim: "the full file cannot hold a *better* icon for the same headers"), `:202-204` (the early return that makes it final), `:169-176` (the group loop that falls through), `:208-221` (`bounded_attempt`). Reproduced: a 131,092-byte PE (over the 64 KiB probe at `:84`) with group 1 pointing into `.data` past `.rsrc`'s raw end and group 2 inside `.rsrc` → reference `extract_icon` returns group 1's image (`0xAA`, the icon Windows shows per `gamehandler/exe_icons.py:227`), port returns group 2's (`0xBB`). The port's own test for this shape (`:1022-1072`) uses one group, so it exercises only the safe fallback | A doc claim treated as a licence to skip the fallback: "inconclusive" was defined as "the bounded parse found nothing", and this case is "the bounded parse found *something else*", which is indistinguishable from success | Wrong cover art written into the library for an executable with two resource sections. Nothing fails, which makes it harder to notice than a missing icon | Distinguish "read failed because the offset was past the *bounded* end" from "past EOF" and force the full read in the first case; or make the bounded result final only when the bounded end reaches the file end **Status: FIXED** (2026-09-13). Both mechanisms in the first fix direction, because they turn out to be one condition. `icon_bytes` is now a thin wrapper over `parse_view` (`crates/core/src/exe_icons.rs:131-133`), which returns a `Parsed { icon, clipped }` (`:112-123`); `clipped` is set wherever a read stopped short of what the view's own headers asked for — the read past the end of the bytes in hand (`:206-216`), a section-table or directory-entry walk the end of the buffer cut short, and a header parse that failed at all (`:136-147`, since the headers themselves may simply be past the end of *this* view). `bounded_attempt` (`:278-294`) falls back to the full read on `clipped` whatever the icon was. `icon_bytes` still ignores the flag, so its contract is unchanged. Reproduced against the reference before the fix, as the row's own mechanism: `gamehandler/exe_icons.py` on this fixture returns group 1's image (`0xAA`, the 1,128-byte DIB at offset 98,304) while `icon_bytes` over the first 65,536 bytes returns group 2's (`0xBB`) — the wrong answer the bounded parse was free to give. The Rust-built fixture was diffed byte-for-byte against the Python-built one: same length (98,324) and four differing bytes (offsets 424-425 and 432-433, inside the region the relocation leaves dead), and the oracle returns 0xAA from `extract_icon` and 0xBB from `icon_bytes` over the first 64 KiB on **both**. Test `a_group_the_bounded_view_cannot_read_does_not_hand_the_icon_to_the_next` (`:1260`), which asserts the bounded answer is 0xBB *and* the extracted answer is 0xAA, so neither a parse that refuses everything nor one that answers from the wrong group passes. Verified by restoring the pre-fix `bounded_attempt` alone (the BUG-41 clamp kept): the extraction assertion fails, `left:` … `187, 187, 187` (0xBB) against `right:` … `170, 170, 170` (0xAA). |
| **BUG-41** | **P2** | `file_offset`'s EOF check rejects a whole resource the reference reads by **clamping**, so a truncated executable loses an icon Python recovers. The module states the opposite as a deliberate equivalence: "Clamping cannot produce a valid icon (the group/image structure checks reject short reads), so returning `None` at the first out-of-range read matches the production (`mmap`) path exactly." The reference has no such check and slices with `data[offset:offset+size]`, which clamps. *Sub-audit; citations re-read by me.* | `crates/core/src/exe_icons.rs:350-352` (`if offset + length > data_len as u64 { return None; }`); the claim at `:33-39`; contract `gamehandler/exe_icons.py:86-94` (no check) and `:212` (the clamping slice). Reproduced: the reference's own 2-image fixture (`tests/test_exe_icons.py:53-86`) truncated by 4 bytes → Python returns a **valid** 1-image ICO (entry 0 survives, entry 1 is dropped), port returns `None`. The port's truncation test (`:928-931`) cuts to `RSRC_FILE_OFFSET+8`, far too aggressively to see this | A guard added for a reason that is true in general and false at the boundary, with the general case written down as if it settled the boundary | A partially written or packer-padded executable loses its icon where the reference recovers one. Graceful (`None`), so nothing fails — but the sentence is load-bearing for every other "faithful to the `mmap` path" claim in the module | Clamp as the reference does and let the structure checks decide, or keep the check only as a signal to try the full read before concluding `None` **Status: FIXED** (2026-09-13). Clamped, as the first direction says, because the row's premise is now measured rather than argued: `mmap` **does not raise** on a slice past the end — it clamps exactly as `bytes` does (`bytes(m[5:100])` on a 10-byte mapping is `b"56789"`, and past the end it is `b""`) — so the reference's `extract_icon` never relied on a `ValueError` here and the sentence that claimed a `None` "matches the production (`mmap`) path exactly" was the opposite of the truth. `file_offset` (`crates/core/src/exe_icons.rs:428-445`) no longer takes a `data_len` and no longer refuses an out-of-range read; what it keeps is the refusal the reference makes, of an address no section maps and of a length past the section's *raw* bytes. The clamping slice lives in `parse_view`'s `read` (`:206-216`), and it now clips to `data.len()` the way `data[offset : offset + size]` does. Reproduced against the reference first: the reference's own two-image fixture (`tests/test_exe_icons.py:53-59`) cut by four bytes → Python `icon_bytes` and `extract_icon` both return a **1,150-byte** ICO holding one 16×16 image (entry 0 survives, entry 1 is dropped), the port returned `None`. The Rust fixture is byte-identical to the Python one (6,654 bytes; sha256 `4988c9c8358a41ae…` for each). Tests `a_payload_cut_short_by_the_end_of_the_file_is_clamped_not_dropped` (`:1062`) and `a_truncated_file_read_off_disk_keeps_the_same_image` (`:1090`). Restoring the pre-fix `file_offset` and `read` fails both (`the entry that survives the cut`). The hostile-input corpus added with this fix, `a_hostile_executable_is_never_a_panic`, is 7,758 cases — every prefix of a real executable plus poisoned header fields plus 1,024 deterministic single-byte corruptions — of which 1,075 yield an icon and 6,683 are refused, so the panic-freedom is claimed over inputs that reached the walk. |
| **BUG-46** | **P2** | **Navigating away from the open game form leaves the form on screen.** With a form open, `Ctrl+F` and `Ctrl+,` are still answered, and **neither path clears `state.game_form`** — so the body keeps drawing the form while the sidebar highlight moves to Library or Settings. Reached in one keystroke from the state a user is most often in: ordinary page navigation while a form is open. *Contributed by `docs/audit/COSMIC-UX.md`; both mechanism claims read at source by me.* | `crates/app/src/main.rs:3775` (`on_search` → `focus_library_search`), `:1314-1321` (`show_page(Page::Library)` and a focus task, no `game_form` clear); `crates/app/src/shortcuts.rs:149` (`Ctrl+,` → `Message::NavigateTo(Page::Settings)`); `crates/app/src/main.rs:1466-1468` (`view_with_overlays` returns the form **before** consulting the page); the only production clears are `:1656` and `:2024` (the form's own Cancel/Save). The `Ctrl+,` half was confirmed by the UX audit with a sidebar-region-only capture delta — the form body is a separate view and does not change. | `view_with_overlays` is documented as the place "the two meet" and deliberately refuses to draw the page under the layer — "the composition that drew both would let a test find the form's strings and the library's in the same body" — which is right for rendering and wrong for navigation. The mutual clear the port *does* implement is `confirm_delete`/`confirm_remove_runner`, and the comment above the form arm names the intended model ("each QML layer closes the other"): the layer's own keyboard hooks close it, and the frame never reaches the layer at all. **Not reproduced live by me:** the shell that would be in the inconsistent state after `Ctrl+,` was not exercised through to a close | The rail and the body disagree about which page is showing: the sidebar entry is highlighted and the page it names is not drawn. Any later navigation re-highlights correctly, so it is recoverable rather than reachable-loss | Clear `state.game_form` on a navigation that did not originate from the form's own Cancel/Save — cleanest as a `Message::NavigateTo` arm that clears it before delegating to `show_page`, which also makes the invariant testable (`FocusSearch`/`Ctrl+,` are pure `update` messages, unlike Cancel/Save which are form-supplied). `Ctrl+F` routes through `on_search` and needs the same clear **Status: FIXED.** Fixed at `crates/app/src/main.rs:1295-1322` — `show_page` now calls `State::clear_overlays`, which is the reference's layer teardown (`Main.qml:37-45`: pop every layer, clear the page stack) expressed as one function. It lives in `show_page` rather than in a `Message::NavigateTo` arm, which is what the row recommended and is the wrong place: the sidebar's own clicks never pass through that message, and `Ctrl+F` comes in through `focus_library_search`. Both routes are driven in `navigating_away_closes_the_layers_the_page_was_covering`, which also drives the form's own Cancel so the fix is not passing because everything closes everything. A second test, `clear_overlays_names_every_overlay_field_on_state`, holds the method's body against the fields it must cover — a list is the thing that goes stale, and the navigation test alone would not notice a fourth modal left out of it. Verified by deleting one assignment. |
| **BUG-47** | **P2** | A card's and a row's **title and subtitle have no ellipsis marker**, so a name too long for its column is cut mid-glyph with nothing to say it was truncated. The tile is the fixed-width case; the row is the same widget pair, so it shared the defect. `docs/images/t19-long-title-tile.png`, cited in an earlier draft of this audit as evidence of the *overflow* this was originally believed to be, **does not exist** — `docs/images/` is not in this tree at all, and the archived walk output it referred to is under `docs/migration/`. The finding below is the corrected one, settled by measurement rather than by reading a screenshot. *Raised by the sibling `docs/audit/COSMIC-UX.md`, whose "long titles overflow into the neighbouring tile" claim I refuted; the narrower claim underneath survived.* | `crates/app/src/view/widgets.rs` `name_and_subtitle` — both `text(title_of(game))` and `text(subtitle)` were built with no `wrapping` and no `ellipsize`; the fix adds `.wrapping(Wrapping::None).ellipsize(Ellipsize::End(EllipsizeHeightLimit::Lines(1)))`. `EllipsizeHeightLimit` is **not** re-exported from `widget::text` — only `Ellipsize` is (`iced/core/src/widget/text.rs:37`); the correct path is `cosmic::iced::core::text::EllipsizeHeightLimit`. **Measured**, with the drawn box width taken from the widget traversal rather than read off an image: at 1280 px the long name occupies 404 px of a 404 px column (`name_box.width <= available` and `> available * 0.5`, so the assertion is not vacuous). | "No ellipsis" and "overflow" were conflated in the original claim. `Wrapping` and `Ellipsize` are **orthogonal** in iced — `Wrapping` decides how a line is broken, `Ellipsize` what happens after — so the widget could have wrapped (it did, invisibly, at `Length::Fill`) while still being unable to mark the cut. Fixing the ellipsis did not require the overflow premise to be true | A user with a long game name cannot tell whether the title is complete or truncated, and two similarly-prefixed titles are indistinguishable in the grid | Done. The wiring itself — that `.ellipsize(..)` reaches `Format.ellipsize` at `iced/core/src/widget/text.rs:809` and thence `cosmic_text::Ellipsize` — is **not** asserted by any test, and that is stated rather than papered over: iced has no downcast, `Text::format` is private, and `Operation::text` reports the pre-shaping fragment, so no test can observe a rendered `…`. A source-text guard for it would be a second instance of `BUG-12`'s defect class, which this audit declines to add. The wrapping half is pinned by `the_card_and_row_names_are_ellipsized_at_the_end`; the ellipsize half is reviewed by eye and recorded here as unverified **Status: PARTIAL.** The wiring is in place but **not asserted by any test**, and the row's strong form is withdrawn: the sibling's 'long titles overflow into the neighbouring tile' did not survive being measured. The ellipsize *wiring* cannot be asserted because iced has no downcast and `Text::format` is private, so the remaining gap is recorded rather than papered over with a guard that would not inspect it. |
| **BUG-48** | **P0** | **The GUI died on its second frame, with `Downcast on stateless state`, in the accessibility wrapper added for UX-01/UX-02/UX-03.** `Accessible` reported its id through `Widget::id`, and `input_with_id` gives the wrapper and its own inner `TextInput` *the same* `Custom` id. Iced keys its named-state map by that string alone, so the wrapper's tree arm consumed the single entry and left the input stateless while its tag still claimed `text_input::State`; the input's `diff` downcast it. Introduced in `8f7269e`, found by the audit's own smoke test, and bisected to it. *Found by S1; mechanism traced to the framework line by me.* | `crates/app/src/view/a11y.rs` `Accessible::id` (was `Some(self.id.clone())`, now `None`); `input_with_id` (both halves one id); crash at `/home/gosh/.cargo/git/checkouts/libcosmic-41009aea1d72760b/a401af8/iced/core/src/widget/tree.rs:505`, reached from `src/widget/text_input/input.rs:621` through `Accessible::diff`. The map is keyed by the string: `Internal::Custom`'s `Hash`/`PartialEq` are both the name (`iced/accessibility/src/id.rs:160-171`). Measured in the built tree: the wrapper and its inner input both carried `Id(Custom(2, "gamehandler.library.search"))`, and only **one** key was left in `NAMED` after the handoff while both nodes lost their state | A field that serves two incompatible purposes. `Widget::id` is the *tree's* identity and iced feeds it back into `Tree::diff`; the node id is the *node's* name and is meant to be stable across frames. The wrapper needed both from one value, and the two requirements are contradictory — a stable `Custom` name in the tree slot is exactly what the named-state branch treats as "this subtree was hoisted and stripped" | Total application failure: the window never drew a frame. `scripts/verify.sh`'s `smoke-test` stage caught it (`gui-stays-up: exited after 1s, status 101`), which is the guard working as designed | Report `None` from `Widget::id`, as the toolkit's own `Named` widget does (`src/widget/named.rs:105-119`), and keep the id in the field for the node and for `operation.focusable`. Nothing is lost: focusing by name matches the `Custom` string through `IdEq` (`iced/core/src/widget/operation/focusable.rs:39-51`, `iced/accessibility/src/id.rs:188-205`), not this slot, which is why `Ctrl+F` still reaches the search box. **Status: FIXED.** `Accessible::id` returns `None`; the input keeps its `Custom` id, so `Ctrl+F` and the four tests that read the control's id are unchanged. `a_named_wrapper_survives_the_runtimes_named_state_handoff` replays the runtime's exact frame sequence — take, diff, clear (`iced/runtime/src/user_interface.rs:106`/`:111`/`:119`) — against a real wrapper, and **its first version was wrong in the direction that matters**: it cleared the map before diffing, which skips the lookup that causes the bug, so it passed against the broken wrapper. Corrected to the runtime's order and proven by restoring `Some(self.id.clone())`, which fails it with the original panic. `element_id` in the a11y harness also read `Widget::id`; it now reads the id the *node* is published under, so the four tests that use it assert what they meant rather than where it happened to be stored. Verified end to end: the release binary runs under `weston` and stays up (`rc=124` from `timeout`). |

| **BUG-42** | **P3** | Three path/identifier conversions that do not do what the code around them says. (a) `join_all` keeps a `.` component the reference's `pathlib` deletes — the doc enumerates empty, absolute and `..` and concludes "the two agree", having missed `.`. (b) the GVFS scan returns a **lossy** path that does not exist. (c) `ProtonRunner::version` uses `Path::file_name`, which is not Python's `name`. | (a) `crates/core/src/netpaths.rs:364-380` (claim at `:368-372`), contract `gamehandler/netpaths.py:97`/`:100` — `PurePath.joinpath` drops `"."`. Reproduced: `sftp://server/pub/game.exe/.` → reference resolves to the real mounted file, port returns the URL unchanged → `resolve_game_paths` then raises `UnreachableShare` ("not mounted yet") for a share that **is** mounted. (b) `crates/core/src/netpaths.rs:126-128` (`found.to_string_lossy()`) against the correct `OsString` join at `:427-446`; reproduced with a mount directory named `sftp:host=server,user=j\xf6rg` → reference returns a path whose `exists()` is `True`, port returns one containing U+FFFD whose `exists()` is `False`. (c) `crates/core/src/runners/mod.rs:691-698` vs the module's own `pure_posix_name` at `:324-329` and its use at `:629`; `Path::file_name()` is `None` when the last component is `..`, so a runner path ending in `..` gives `version() == ""` where Python's `PurePosixPath("a/..").name` is `".."`; reachable because `RunnerManager::get` (`:1087-1096`) joins a user-editable `runner` id from `games.json` and tests only `.exists()`. (b) *sub-audit; (a) and (c) sub-audit, reachability settled by the agent* | Three separate "close enough" conversions — `Path::join` for `joinpath`, `to_string_lossy` on a value that becomes a syscall argument, `Path::file_name` for `PurePath.name` — each documented as equivalent on the strength of the cases that were considered | (a) a mounted share is reported unreachable; (b) libcosmic hands Wine a path that does not exist, which is the failure mode the module's own doc says it exists to avoid; (c) a display-only empty version string | Drop empty and `"."` components in one place after unquoting; return `PathBuf`/`OsString` from the resolver (or re-check `exists()` on the lossy string before returning it); use `pure_posix_name` in `version` |
| **BUG-43** | **P3** | Two more "the doc says the two agree" pairs, both verified sound-but-for-the-claim. (a) `extract_icon`'s fallback buffers the whole executable (up to `MAX_EXECUTABLE_BYTES`, 512 MiB) whenever the bounded end is unavailable — e.g. an executable with **no** resource directory, where `_sections_and_resource_root` returns `None` — where the reference `mmap`s it and pays nothing; the doc claims "same worst case". (b) `math`-style: for the 25,000-case URL fuzz corpus, the port returns a resolved value on inputs where CPython's `.port`/host parsing **raises** (`sftp://server:notaport/...`, `file://us:er[fe80::1%25eth0]:x/...`): deliberate and strictly safer, but `netpaths` records none of it and no test pins those inputs. | (a) `crates/core/src/exe_icons.rs:26-32` (claim), `:201-205` (the unconditional fallback), `:247-253` (`vec![0u8; len]`), `:304-306` (the `None` that triggers it). (b) `crates/core/src/netpaths.rs:103-133` and `crates/core/src/installers.rs:1208-1215`; the raise-vs-`None` port arm at `installers.rs:1305-1330` is at least covered by its own test (`:3130-3141`). Both *sub-audit* | (a) a bounded-read optimisation whose bound is absent exactly when it is needed, with a comment asserting otherwise — the same shape as `BUG-40`/`BUG-41`. (b) a divergence that is correct but unrecorded, in a module whose review standard is that every divergence is written down | (a) memory and IO regression during a library scan; results identical, so nothing fails visibly. (b) the next reader cannot tell "faithful" from "unnoticed" | (a) bound the fallback through the highest section's raw end, or refuse it above a threshold, and reword the claim. (b) record it beside the other documented divergences and add the two inputs to the existing URL battery |
| **BUG-19** | **P3** | `sanitize` accepts a bare `-` as the number `null`, so `[-]`, `[-]`-style malformed numbers and `{"last_played":-}` parse where Python raises `JSONDecodeError`. The port is *more lenient* here and *less* lenient in `BUG-02` — the two strictness errors point in opposite directions. | `crates/core/src/json.rs:232` (routes `b'-'` into `number_end`), `:283-287` (consumes `-`, then returns unconditionally when no digit follows), `normalize_number` falling back to `null`. Executed: `[{"id":"1","name":"a","last_played":-}]` → port `Ok [… "last_played":null]`, Python `JSONDecodeError: Expecting value: line 1 column 37` | No "a number must have at least one digit" check after the sign | A malformed timestamp silently becomes `null`/`0.0` rather than failing the parse. Same empty-parse→overwrite blast radius as `BUG-01`, though unreachable from a file the app itself writes | Have `number_end` return `start` (zero-width) when no digit was consumed, so the caller cannot synthesize a well-formed `null` from a malformed token |
| **BUG-20** | **P3** | `Library::all("recent")` / `all("added")` do not treat `-0.0` and `0.0` as equal, so two games whose timestamps tie present in a different order than the reference. `f64::total_cmp` distinguishes the two zeros; Python's `-g.last_played` key does not, so Python falls through to the name tie-break. The doc at `models.rs:618-620` concedes that these two sorts "diverge from Python on equal keys" without naming this as the reachable case. | `crates/core/src/models.rs:626-634` (the `total_cmp` arms); `-0.0` is preserved through the load path because `crates/core/src/models.rs:413-421` guards with `numeric >= 0.0` and `-0.0 >= 0.0` is true. Executed against the reference: stored `[("ZZZ", 0.0), ("AAA", -0.0)]`, `recent` → port `["ZZZ","AAA"]`, Python `['AAA','ZZZ']` | `total_cmp`'s total order vs IEEE `==`, used for a key Python compares with `==` | Ordering divergence on a hand-edited or third-party `games.json`. The port's order is self-consistent and the app never writes `-0.0` itself (`mark_played` uses the clock) | Normalise `if x == 0.0 { 0.0 } else { x }` before `total_cmp`, so the name tie-break applies as in Python — or state this case in the existing doc |
| **BUG-21** | **P3** | Four `.trim()`-where-Python-has-a-truthiness-test divergences on paths and identifiers, each of which shifts a value in the permissive direction. (a) `working_directory` and (b) `prefix_path`; (c) a whitespace-only `WINEPREFIX`; (d) a whitespace-only `game.prefix_path`. | (a) `crates/core/src/runners/launch.rs:386` (`game.working_directory.trim()`) vs `runners.py:1419`; (b) `crates/core/src/runners/launch.rs:443-448` vs `runners.py:1482`; (c) `crates/core/src/runners/launch.rs:333` with `crates/core/src/runners/mod.rs:1144-1152` — the doc claims Python treats a whitespace-only `WINEPREFIX` as absent, but `runners.py:1399`'s `if prefix:` is a truthiness test, so `" "` is truthy and is `mkdir`'d there; (d) `crates/core/src/runners/mod.rs:365-371` (`game_prefix`) already does it correctly with `is_empty()`, which is the standard the other three should meet | Rust's `trim()` used where Python's `or` semantics were being transcribed | A working directory of `" /games "` chdirs to `/games` here and raises `FileNotFoundError` in Python; `prefix_path` `" "` targets the per-game default prefix here and a directory literally named `" "` there. Edge-only, but it is a divergence in a path the port otherwise copies faithfully | Compare against `is_empty()` (no trim), as `game_prefix` already does |
| **BUG-22** | **P3** | `merge_dll_overrides` trims `;` from **both** ends where Python `rstrip`s only, so a leading `;` survives the reference and is removed here. The comment states the port's behaviour as if it were the reference's. | `crates/core/src/runners/launch_opts.rs:365` with the helper at `:116-117` (`trim_matches(';')`), vs `runners.py:1035` (`current.rstrip(";")`); the doc at `crates/core/src/runners/launch_opts.rs:349` says "Both sides are trimmed of their `;`". No test covers a leading `;` — the merge cases at `:1171-1178` use `"x;"`, trailing only | `trim_matches` (both ends) transcribed where `rstrip` (right end) was meant | `WINEDLLOVERRIDES=";dxgi=n,b"` yields `dxgi=n,b;<extra>` here and `;dxgi=n,b;<extra>` in Python. Wine ignores the empty element, so it is cosmetic — but it is a real divergence in a transcribed function | `trim_end_matches(';')`, and correct the comment **Status: FIXED** (`b3ad80b`). Verified against `runners.py:1035` before the change: the reference strips the *fragment* from both ends and `rstrip`s the *accumulated* value, and the port used one both-ends helper for both. `python_rstrip_char` now names the distinct operation and is used only where the reference `rstrip`s. **The row was right, including about the missing test — and that test is why the divergence survived.** The merge table had six rows and every one used a *trailing* `;` (`"x;"`), so the leading-`;` direction was unrepresented in both the code and its coverage. A seventh row now uses `;dxgi=n,b` against a `;`-padded fragment; swapping the both-ends helper back in drops the leading `;` and fails it. One correction to my own first attempt at that row, recorded in the test: I wrote the expected value from the intent and CPython disagreed about the interior spaces, which the reference preserves. The expectation in the tree is the reference's output.|
| **BUG-23** | **P3** | Three ways a `launch` failure is turned into a non-failure. (a) `failure()`'s `try_wait` error returns `None`, which is the same value as "the title is still running", so an `ECHILD` or a transient `waitpid` error is indistinguishable from a healthy launch. (b) `is_executable`'s doc describes an owner/group-aware check the body does not perform — it is any-execute, always. (c) the helper child from `BUG-05` is spawned and immediately dropped, so it is never reaped; the comment claims "exactly as Python leaves it", but `Popen` registers in `subprocess._active` and the next `Popen` reaps it, which the launch path performs on every launch. | (a) `crates/core/src/runners/launch.rs:279` with the comment at `:275-278`; (b) `:494-502` (doc) vs the single `mode() & 0o111 != 0` body, and `runners.py:1490` is `os.access(…, X_OK)`; (c) `:373-379` (the dropped `Child`) with the comment at `:371-372` | Reusing a success value for a failure case (a); a doc comment that survived a simplification of its body (b); and a comment generalised from the reference's *intent* rather than its mechanism (c) | (a) a dead title hides behind a healthy-looking grace period. (b) `WINESERVER` may be advertised for a `wineserver` its own user cannot execute, pointing winetricks at a binary the exec will reject. (c) one permanently unreaped zombie per launch with a helper, bounded by launches but permanent for the session | (a) return a distinct third outcome for a wait error. (b) implement the described check (`rustix::fs::access`) or correct the comment, as `env.rs`'s `which_in` does honestly. (c) drop the `Child` behind a tiny detached reaper, as the codebase already does for `run_version` |
| **BUG-24** | **P3** | The rolling stderr buffer trims to exactly `limit` bytes where Python keeps the trailing chunk, and the port's `chunks.len() > 1` clause is **dead code**: Python compares *chunk counts* and keeps the last chunk even when it overshoots, while the Rust buffer is one flat `Vec<u8>` where the same expression is a byte count, subsumed by `chunks.len() > limit`. The test pins the port's behaviour and its docstring describes Python's guard. | `crates/core/src/runners/launch.rs:163-166` with the comment at `:160-162` and the test at `:999-1018`, vs `runners.py:1339-1340`. Measured: for limit 4 over `first` then `SECOND`, Python's `_ErrorTail.text()` is `"SECOND"` and the Rust test asserts `"COND"` | A data-structure change (chunks → flat buffer) that left the guard and the comment behind | No user-visible difference — `readable_error` keeps the last 4 lines / 240 chars and both windows end at the same byte. It is a fidelity and test-honesty defect: the port's bound is stricter than claimed and the comment asserts a guard that is inert | Either state the stricter bound as a deliberate divergence and correct the docstring, or drop the dead clause so it does not read as Python's guard |
| **BUG-25** | **P3** | Two functions apply **opposite policies to the same input**. `uninstall` propagates `safe_install_id`'s refusal; `is_installed` swallows it and joins the raw value onto the runners directory. | `crates/core/src/runners/proton.rs:1114` (`let install_id = safe_install_id(runner_id)?;`) vs `:611` (`safe_install_id(&install_id).unwrap_or(install_id)`); `safe_install_id` at `crates/core/src/runners/archive.rs:232-242` rejects `/`, `\` and a leading `.` | Two call sites of one helper, one of which added a fallback | For an id `safe_install_id` rejects, the *probe* reads a path that may sit outside `runners_directory`. Read-only, so no traversal is exploitable, and reachable only from a hand-constructed `ReleaseInfo` (`proton.rs:846-848`) — but the divergence becomes a bug the day someone reuses the helper | `safe_install_id` and fall back to `system_wine` on `Err`, matching `is_installed`'s sibling callers **Status: FIXED** (`08da989`). Both of `is_installed`'s joins now go through a new `install_directory`, which validates the id and returns `None` rather than a path. The fallback was inert because its input had already been through `safe_install_id` *inside* `install_id_for`, one module away — which is the finding's own argument, and the reason the fix is at the join rather than at the caller: the caller can only see that the helper ran somewhere else. Nothing observable moves, since `None` here means "no directory name" and every caller already answered "not installed" for that; the row's 30-odd existing `is_installed` tests are untouched. The row's "reachable only from a hand-constructed `ReleaseInfo`" line is doing real work in the verification: **no production input can reach the guard**, so a test written against `is_installed` would pass against the old `unwrap_or` too, and the teeth are on `install_directory` called directly (seven rejected ids, plus the trim). Two mutations, both fatal — `Some(join(install_id))` fails on the trim, and the exact `unwrap_or` shape fails on `""` with `Some("/runners/")`. The second half of the row's fix — "fall back to `system_wine` on `Err`" — is not what `Err` means here and was not written: `is_installed` answers a question about a directory, and `safe_install_id` refusing an id is the answer "not installed", the same value its sibling refusal already returned two lines above.|
| **BUG-26** | **P3** | The archive's containment root falls back to an **unresolved** path: if `canonicalize(destination)` fails, the root stays unresolved while the member path goes through `resolve_missing`, which resolves as far as it can — so a resolved member is `starts_with`-compared against an unresolved root. Members are lexically stripped of `..` first, so the reachable outcome is a *false rejection* rather than an accepted escape; I could not construct the accepting case and am not claiming one. | `crates/core/src/runners/archive.rs:420` and `:498` (`fs::canonicalize(destination).unwrap_or_else(\|_\| destination.to_path_buf())`); the member resolver at `:358-376`; the lexical strip at `:400-416`; the invariant the module states at `:112` | A security check that degrades to a lexical comparison when resolution fails — it has lost the property it was written for even though the failure direction is safe | A legitimate member can be refused with no explanation on a tree where `canonicalize` fails | Refuse the extraction rather than degrading the root, so the check cannot silently become lexical **Status: FIXED** (`b3ad80b`). `containment_root` refuses with the new `ArchiveError::DestinationUnresolvable` where the old code fell back to the unresolved path, and both call sites use it — the member check and the link check — so they cannot resolve the root differently, which was this defect's other half. The row was right that the reachable outcome is a false rejection and not an accepted escape; the fix is a refusal rather than a *degraded* refusal all the same, because a check that has lost the property it was written for is not a check, and the answer it returns looks like an answer either way. The row gave the fallback as `:420` and `:498`; it was at `:435` and `:512` at this revision, found by grep rather than by the line numbers. Verified by `an_unresolvable_destination_is_refused_rather_than_degraded` — both the helper directly and, separately, `destination_path`, the function the escape check actually runs — with `a_real_destination_resolves_to_itself` as the positive half so the pair cannot pass against an unconditional `Err`. Restoring the `unwrap_or_else` fallback fails the first.|
| **BUG-27** | **P3** | A typo in the Steam AppID field is silent: `"abc"` or `"12 34"` parses to `0`, i.e. "no appid", and `GameForm::apply` reports no error — it blocks only an empty name. | `crates/app/src/state.rs:494` (`self.steam_appid.trim().parse().unwrap_or(0)`), with `apply`'s error type already `Result<Game, String>` (`:429`). Python does the same (`gamehandler/bridge.py:430-432`), so it is parity | `unwrap_or` on user input at the one seam whose signature already carries a validation error | The user gets a game that silently will never match its Steam artwork, and no hint why | Report the parse failure through the `Err` arm `apply` already returns |
| **BUG-28** | **P3** | Six worker-to-UI sends discard their message with `let _ =`, including the two that carry *why* a launch failed. If the receiver is gone (window closed mid-install) the completion evaporates with no record at all — not even the `eprintln!` the CLI path uses. | `crates/app/src/main.rs:2722`, `:2730`, `:2737` (the `LaunchStarted`/`LaunchWatchFinished` trio), `:3067`, `:3088`, `:3128`; `crates/app/src/view/runners.rs:899`, `:912`; contrast the CLI's `eprintln!` at `crates/app/src/main.rs:310`, `:326`, `:336` | Best-effort sends, correct in the ordinary case, with no fallback channel | Benign today (a closed window has nobody to report to) — reported because it is the one place a failure can disappear between the worker and the UI without a trace, which is the class this audit is looking for | Log on send failure, or make the loss explicit in the comment the sends already carry |
| **BUG-29** | **P3** | One production `expect` on a value a future page addition invalidates: `activate_page` runs on every nav-bar click and panics if a `Page` variant is absent from `Page::ALL`. | `crates/app/src/main.rs:979` (`Page::ALL.iter().position(..).expect("every Page is in Page::ALL")`) | An invariant that a lockstep test (`crates/app/tests/app_id_lockstep.rs`, `dispatch_coverage.rs`) is meant to hold, promoted to a panic in release. I did **not** verify that those tests enumerate `Page::ALL` exhaustively, so the guard is **unverified** | A future `Page` variant added without updating `Page::ALL` crashes the app on a nav click rather than failing a test | Fall back to the first page, or `debug_assert!` — the shell already uses `debug_assert!(self.pages_agree())` (`crates/app/src/main.rs:1256`) for the same shape |
| **BUG-30** | **P3** | Two tests in the suite are **tautologies** — they compare a value against the expression that defines it, so they can only fail if the delegation they are made of is edited. (a) the badge paddings are asserted equal to the very expressions that define them (and the comment claims this establishes derivation); (b) `the_plate_shade_defers_to_core` compares a one-line delegation against its own callee. (b) is self-documented ("A tautology today, and kept anyway"); (a) is not. | (a) `crates/app/src/view/badge.rs:118-119` against the definitions at `:42` and `:45`. (b) `crates/app/src/view/cover.rs:453-456` with `accent_of` at `:182-184`. Note `the_radius_rounds_the_ends…` (`badge.rs:109`) is **not** tautological — `999.0 >= 28.0` really would fail on a regression | A test written from the constant rather than from the rendered result | A wrong value written into both the constant and its test — which is how a wrong value actually arrives — passes | Assert the rendered padding, or delete the test and let the constant be the single source of truth |
| **BUG-31** | **P3** | Three `scripts/verify.sh` hygiene defects, all of which make a run look cleaner than it was. (a) with no `flock` the locked stages run unserialised after a warning, so two concurrent runs can validate a tree swapped underneath them while every line says `ok`. (b) `git status` errors are swallowed into an empty-string comparison, so with no `git` or outside a repository the run reports a clean tree having observed nothing. (c) `check_stage_table` enforces the stage-name bijection, not the mapping — a permutation that preserves it runs the wrong functions under the right names and exits 0. | (a) `scripts/verify.sh:739` (`return 0` in the `! command -v flock` branch), with the file's own argument at `:715-722` and the trade recorded at `:2606-2614`; (b) `:2214-2223` and `:2214-2223` (`git status --porcelain 2>/dev/null`); (c) `:2257-2294`, with the admission written at `:2247-2251` | Deliberate residuals in (a) and (c) — both are argued in-place; (b) is not argued | (a) interleaved runs; (b) the T-17 dirtiness *note* is wrong (blast radius is a note, not a verdict); (c) requires two coordinated edits | (a) return 99 or refuse the locked stages without `flock`; (b) fail hard when `git status` errors; (c) no fix proposed in-code — the comment's candour is the mitigation | **Status: FIXED.** All three. **(a)** the `flock`-absent branch returns 99 instead of 0, so the four stages that touch `build-flatpak/` are *skipped as an unrequested prerequisite* (`finish_skip` with `requested=0`) and the run exits 3 rather than reporting them `ok` from a tree it could not lock; the old comment's reason for returning 0 — that failing would "take the earlier stages down with it" — described something the alternative does not do, since every earlier stage has already run and reported by that point. `--skip-flatpak`/`--skip-smoke` still mark *their own* two stages requested, so a box without util-linux can still take the fast iteration loop it wants while `desktop-metainfo` and `flatpak-contents` — which read `build-flatpak/` and which neither flag names — stay unrequested skips. **Six configurations probed** with a `PATH` directory symlinking every binary but `flock`: no flags → `req=[] unreq=[cargo-sources-fresh flatpak-build smoke-test desktop-metainfo flatpak-contents]`, exit 3; `--skip-flatpak` → `req=[flatpak-build smoke-test]`, exit 3; `--skip-smoke` → `req=[smoke-test]`; both flags → `req=[flatpak-build smoke-test]`. **A first version of this fix got it wrong and the probe caught it**: it let `--skip-flatpak` release the lock entirely, and then measured `ok desktop-metainfo` and `ok flatpak-contents` under a live tree with no lock held — the exact hazard the lock exists to close. **(b)** `git status` goes through `status_probe`, which distinguishes an empty answer from a failed command by its exit status rather than folding both into the empty string through `2>/dev/null`; a failed read exits 3 with `did not run (the tree's state could not be read)` naming every stage, and a failed *second* read is a third state (`STATUS_AFTER_UNREAD`) reported as "T-17 is unverified" instead of silently agreeing with the first. Probed by running a copy of the script outside any repository: `fatal: not a git repository`, exit 3, all sixteen stages listed as unrun. **(c)** `banner_check` now pairs each banner with the function it heads, by line number, in two ways: `stage_*` definitions in file order must equal STAGES' function column in table order (the permutation case — mutation `cli → stage_test` with `test → stage_cli` now exits 2 naming both lists), and each stage's function must be defined after its own banner and before the next (a banner moved one function down now exits 2 with `the "# Stage: build" banner does not head stage_build`). The old code's own comment declared this undetectable; it was undetectable by a *sequence of names*, which is what it compared. The claim that this tree had a misplaced banner was **checked rather than assumed** — it did not, and `banner_check` passed before the change; what changes is that the comment no longer tells the next reader not to look. |
| **BUG-32** | **P3** | Two smaller unbounded checks. (a) The oracle stage's fixture count is *reported* and never required, and `fixture_hashes` sends `find`'s errors to `/dev/null`, so a vanished fixture directory yields the same empty evidence as a clean one to the read-only half (the `diff -ru` at `:1314` does fail loudly today, so this is a missing floor rather than a live hole). (b) The Flatpak declares-match is applied over **all** modules while completeness is scoped to `gamehandler`, and the declares-half is the one that runs alone under `--skip-flatpak`. | (a) `scripts/verify.sh:1330` (the count echo) and `:1268-1272` (`fixture_hashes`), read-only check at `:1333-1338`; (b) `:1800-1801` (`def is_dest`), used at `:1813`, scoped at `:1829` (`MODULE = "gamehandler"`), `--skip-flatpak` path at `:1917-1918` | A floor never added; and a predicate whose scope is wider than the check it feeds | A right-tail install from another module, or under an extra path prefix, satisfies the declares-half | Add the non-zero floor; scope the declares-half to the `gamehandler` module and match from a fixed destination root |
| **BUG-33** | **P3** | The EasyInstall toast's Play action launches the game but does not dismiss the toast, so a stale "Installed … ▶ Play" toast (duration `Long`, 15 s) sits over the Library the user was just navigated to. Clicking the action in QQC2 dismisses the notification and *then* runs it. | `crates/app/src/main.rs:2478-2484` (`.action("Play".to_string(), move \|_\| installed_play_message(&game_id))`), with the divergence recorded at `:2459-2469` and the navigation at `:2471` | The action closure is handed neither the `ToastId` nor a way to remove itself | Cosmetic; self-documented rather than hidden | Build the variant the comment names — one carrying both the game id and the toast id |
| **BUG-44** | **P3** | An unreadable runners directory renders as a normal, empty, system-Wine-only list. Two independent swallows with no Python counterpart: the directory-level `read_dir` error becomes `Vec::new()`, and per-entry errors are dropped by `filter_map(Result::ok)`. Python's `for child in sorted(self.runners_directory.iterdir())` raises `OSError` and `sorted()` propagates it, so either failure reaches the caller. *Sub-audit; citations re-read by me. Impact corrected downward from the sub-audit's own first draft.* | `crates/core/src/runners/mod.rs:1051-1057`; contract `gamehandler/runners.py:737-738`. This feeds the *listings* only — `installed_protons()` (`:1049`) → `choices()` (`:1121`) and `all_runners()` (`:1073`). The sub-audit's initial claim that a game then "launches under a different runtime" does **not** follow from this site: `RunnerManager::get` (`:1087-1096`) never calls `read_dir` — it joins the id and tests `candidate.exists()` | Two best-effort call shapes (`if !exists { return }` and `filter_map`) applied to a directory whose unreadability is a reportable condition | A mode-000 `runners/`, an `EIO`, or a directory removed between the `exists()` at `:1050` and the `read_dir` at `:1053` all render as "no builds installed" where the reference surfaces an error | Keep `[]` only for `ErrorKind::NotFound` and propagate anything else; replace `.filter_map(Result::ok)` with `?` **Status: FIXED** (`c7d4299`). `installed_protons()` is replaced by `scan_installed() -> InstalledScan`, a three-way answer rather than a `Result`, because the row's two swallows are not the same failure: an unreadable directory has no list at all, an unreadable *entry* leaves the rest intact, and the `filter_map` was not merely hiding a failure — it was hiding *builds*. Only `NotFound` is empty now; a runners folder that does not exist yet is an empty list and specifically not a problem, which is the half the row does not mention and the half that a "report every failure" reading of it would have got wrong. The message rides back on `Message::RunnersRefreshed` rather than being pushed where the scan happens, so the existing stale-reply token covers it — a superseded scan's problem is as stale as its rows. `all_runners` ignores it deliberately: refusing to offer System Wine because a listing failed turns a reporting bug into a game that cannot launch. Verification is a real `chmod 000` directory, not a mock — the mutation `Complete(Vec::new())` in place of `Unreadable` fails with `got Complete([])` — and it skips, saying so, for a privileged uid where the mode does not close the directory. **The entry-level half has no killing test, and that is recorded rather than papered over**: provoking a failing `DirEntry` needs a `getdents` error mid-scan, which is not portable to provoke, and a mock would only assert that the code calls the function the test names. What is pinned instead is what `Partial` *means*, since "simplify it away" is the likely next edit: dropping `problem()` to `None` fails it, and so does returning `&[]` from `runners()`. On the app side the assertion is `Task::units()` rather than `is_some()`, because the dropped arm also returns `Some(Task::none())` and an `is_some()` assertion was measured passing against the boundary that drops it.|
| **BUG-45** | **P3** | `python_repr`'s string branch is not `repr()` and duplicates — incorrectly — the `python_str_repr` twelve lines above it. The double-quote branch escapes nothing at all (`format!("\"{text}\"")`), and the single-quote branch escapes only `\` and `'`, missing every control character. `python_str_repr` (`:467-495`) already implements the rule correctly. *Sub-audit; both bodies read side by side by me.* | `crates/core/src/runners/families.rs:501-512` vs `:467-495`; reached only from `:431` and `:437` (both inside `python_str`'s container arms, **including the object-key path**), which `asset_name`'s container branch (`:377-383`) reaches — so it needs a GitHub asset `name` that is an array or object holding such a string, i.e. pathological input rather than merely unusual. The oracle is blind to it because `run_runners_vectors.py`'s `_name_cases` carries no escaping array case (all 760 vectors pass) | A second implementation of one rule in one file, the older and simpler one kept for the container path after the correct one was written for the top level | Divergent rendering of an identifier downstream; no security consequence. Recorded for its shape, not its impact: the file contains the correct implementation and does not use it | `Value::String(text) => python_str_repr(text)` — one implementation, and the two then agree **Status: FIXED** (`1143aed`). `python_repr`'s string arm is now `Value::String(text) => python_str_repr(text)`, so the file holds one rule rather than two. Both bodies were read side by side before the change, as the row says they were for the finding. The regression is an equality between the two functions rather than a table of expected strings, and that is a choice about this defect rather than a habit: a table would have had to guess inputs the oracle already misses, whereas the equality fails whenever either implementation moves. It exercises both routes — a bare `Value::String` and a `Value::Array` element — the second being the container path this was actually reached by. Restoring the old body fails it with `the string arm diverged for "a\nb"`. All 31 oracle-vector tests still pass, and the row is right that they were blind here: `_name_cases` carries no escaping array case, which is the reason this survived, not an oversight the fix leaves behind.|
| **BUG-34** | **P3** | The Library card's and row's **"More actions" button and tooltip have no port equivalent**. The reference draws a `view-more-symbolic` tool button on each tile whose tooltip says "More actions"; the port builds only a `mouse_area` with `on_double_click`. All eight menu entries work — but only via right-click, with nothing on screen saying so. | Reference: `gamehandler/qml/LibraryPage.qml:209-213` (grid card, with `QQC2.ToolTip.text: "More actions"`) and `:281-284` (list row, same button, no tooltip). Port: `crates/app/src/view/widgets.rs:413-422` (card) and `:477-490` (row); `grep -rn "view-more\|More actions" crates/app/src/view/` returns nothing. The menu itself is real: `crates/app/src/view/library.rs:400-403`/`:454-457` wrap each tile in `context_menu(..)`, and all eight entries map to real messages (`:516-529`) | A control that lives in the tile template rather than in the page's `actions:` block, and so was not on the porting checklist | Play/Edit/Find cover/Winecfg/Winetricks/Open prefix/Create shortcut/Remove are undiscoverable by mouse — functional but reads as "the feature is missing" | Draw a `button::icon(..)` on the card and row emitting the first menu entry's message, with the reference's tooltip |

---

## Verified sound

Checked because each is a shape that normally hides a defect of the class above,
and each held. Citations are to the check, not to prose.

- **`crates/app/tests/dispatch_coverage.rs` is the strongest check in the tree, and it states its own limits.** `Guard::parse` asserts the variant set and the arm set agree exactly (`:709-713`), floors both at 40 (`:689`, `:696`), cuts `#[cfg(test)]` at brace depth zero (`production_src`, `:520`), and the mutation test (`:1436-1495`) re-empties an arm and requires the guard to report *exactly* `live + named` — so a guard that reports everything cannot pass. Its header (`:40-114`) enumerates nine limits and names which three could yield a false pass. That is the correct shape for this project's dominant defect class.
- **No `Message` variant is dead and no arm is an unmarked no-op.** I extracted all 65 variants from `crates/app/src/main.rs:567-910` and every construction site with `#[cfg(test)]` regions cut: all 65 have a producer. `Shell::update` (`:1640-2588`) has no wildcard arm, and the three empty arms each carry a written reason (`:1821`, `:2240`, `:2584`). The one variant with no *producer* is `LaunchWatchTick`, and its reason is written down twice (`:1598`, `:2684`: the grace watch is a timeout, not a poll).
- **`B-07` — the port fixes a real upstream race by construction.** `LaunchedGame.failure()` in Python reads a stderr buffer filled by a separate daemon thread after `wait()` returns, so the read can beat the drainer and the real Wine/Proton error is replaced by a generic status line (~30% of the time, measured by two independent parties — `docs/migration/PLAN.md:297-310`). The port drains before reading (`crates/core/src/runners/launch.rs:102-200`) and `failure_message` (`crates/core/src/runners/mod.rs:938-947`) falls back to the status line only when the runner truly said nothing. This is the model for how a divergence should be recorded and tested.
- **The launch environment's *order* is a faithful transcription.** Prefix env, then the parsed block early, `create_dir_all` on the stripped `WINEPREFIX`, the Proton gate asked about the **pre-options** argv, then the DXVK install, then `apply_launch_options` re-applying the block last — matches `gamehandler/runners.py:1390-1412` step for step (`crates/core/src/runners/launch.rs:330-357`, `crates/core/src/runners/launch_opts.rs:762`). The toggle matrix at `launch_opts.rs:636-764` reproduces every `proton_features` gate, the wrapper nesting order, `setdefault` for `WINE_FULLSCREEN_FSR_STRENGTH`, and the empty-string anticheat values.
- **`parse_env_block` matches CPython's `shlex` semantics**, including the unclosed-quote fallback and the `len(parts) > 1 and all(..)` rule (`crates/core/src/runners/launch_opts.rs:211-244`), with the `A=1 B` → `{"A": "1 B"}` case pinned at `:989`.
- **No shell is ever involved in a launch.** Every argv is a `Vec<String>` handed to `Command::args` (`crates/core/src/runners/launch.rs:373-379`, `:394-395`) or built by `tool_command` (`:468-480`), so a game path or name cannot inject arguments. The virtual-desktop slug is filtered to ASCII alphanumerics and cut to 16 (`crates/core/src/runners/launch_opts.rs:579-589`) and the size is validated (`:546-560`), so `/desktop=` cannot be broken out of. `additional_app` is deliberately *not* split — one argv element, as Python does.
- **`format_last_played` and the three sorts are exact against the reference.** A 4,184-value differential over `format_last_played` (`crates/core/src/models.rs:466-512` vs `models.py:93-117`) — exact boundaries (2/60 min, 24 h, 30-day, 12-month, ±0.5 s), 3,000 random past, 500 future, 500 negative — produced **0 divergences**; a 60-game fixture across all three sort modes and 13 query/category combinations produced **0 divergences** outside the withdrawn `BUG-11`.
- **The lenient parser's accept/reject battery is otherwise correct.** 42 documents (`NaN`, `Infinity`, `1e400`, arbitrary-precision integers, trailing commas, `01`, `1.`, `+1`, `--1`, `1e`, raw control characters, …) all matched Python except `BUG-02` and `BUG-19`. Big-integer precision is preserved rather than truncated through `f64` (`crates/core/src/json.rs:586-609`), and the non-recursive depth clamp (`:125-171`, `MAX_VALUE_DEPTH` at `:75`) skips strings, so a `[` inside a game name is not counted as nesting.
- **The wire format is byte-compatible.** `Game`'s hand-written `Serialize` (`crates/core/src/models.rs:321-360`) matches the 31 fields of `from_dict_at` and `Game.to_dict` in declaration order; `Settings`' 18 fields (`crates/core/src/settings.rs:59-76`) are in `dataclasses.asdict` order with `#[serde(skip)] path` (`:85-86`). Atomic write is `with_extension("json.tmp")` then `rename` (`crates/core/src/json.rs:358-367`); the `Path.with_suffix()` naming quirk resolves to the same `games.json.tmp`, so it is **not** a divergence.
- **`open_url`'s error arm is real and tested, and `Message::OpenUrl` is one argv element.** `crates/app/src/main.rs:2845-2847` maps the spawn failure, `:2204` toasts it, and the test drives the arm with a program that does not exist rather than asserting the string. (The missing *scheme* check is `SECURITY.md` `SEC-07`.) This is the standard `BUG-04` fails to meet.
- **`create_desktop_shortcut` propagates every step** — `fs::metadata`, `set_permissions` and the rename — with no `let _ =` on a write, rename or chmod anywhere in it (`crates/core/src/runners/desktop.rs:192-257`).
- **The removal refresh order is right, and was fixed by measurement.** `refresh` snapshots `installed_protons()` *before* the uninstall ships its reply task, so refreshing first would resurrect the removed row (`crates/app/src/view/runners.rs:1120-1127`; T-19 watched that happen live). The ordering is pinned by `the_removal_refreshes_after_the_uninstall_not_before`.
- **The CLI dispatch is genuinely GUI-free and correctly ordered.** `--list` is answered before `--launch`, which is answered before `start_gui` (`crates/app/src/main.rs:99-146`), and `App` is never constructed on those paths. Executed headless: `--version` → `GameHandler 0.8.0` rc 0; `--list` on a one-game library → `alpha-1\tAlpha` rc 0; `--launch alpha-1` → the runner-unavailable message rc 1; a bare invocation with no display → the `N-01` hint rc 1.
- **`scripts/verify.sh` has no swallowed stage outcome.** No `\|\| true`, `\|\| echo`, `\|\| :` or `set +e` anywhere; every `\|\| rc=$?` is followed by an explicit mapping; `finish_ok` refuses a stage with an empty `STAGE_RAN` (`:578`); rc mapping (98/99/77 → skip, other → fail) is explicit; exit 1 on `FAILED` (`:2581-2583`) and exit 3 on unrequested SKIP or UNRUN (`:2643-2649`). The BUG-10/16/31/32 findings are missing *floors* and *labels*, not swallowed failures.
- **`installed_protons`'s sort is not a divergence.** `children.sort()` on `PathBuf` (`crates/core/src/runners/mod.rs:1059`) against Python's `sorted(iterdir())` on `Path` objects: every entry shares one parent, so both reduce to the directory-name comparison — which is what the doc at `:1045-1048` claims. (The listing's *error* handling is not sound; see `BUG-44`.)
- **`run_version` reaps on every path.** `try_wait` on the normal path; the `Err(_)` branch (`crates/core/src/runners/mod.rs:584-588`) and the deadline branch (`:589-594`) both `kill()` **and** `wait()` before returning — no zombie, no leaked pipe — and the 64 KiB `wait_with_output` caveat is stated rather than hidden (`:556-562`).
- **The archive extraction filter's own escape logic is correct** — the bug in `BUG-35` is in the *post*-extraction walk, one function later. `plan_member` (`crates/core/src/runners/archive.rs:486-580`) computes a symlink's landing relative to the *destination root* via `relative.parent()` (`:543-553`) and resolves it with `resolve_missing` (`:363-376`), and it keeps the symlink/hardlink bases visually separate for exactly this reason. Refusing a link that lands outside, refusing a link at the destination root, and refusing special files all behave as `data_filter` does.
- **The PE parser is panic-free and allocation-bounded on malformed input** — relevant because `BUG-41`/`BUG-42` are about *results*, not safety. 4,000 structured mutation cases over the reference fixtures plus 3,000 random-header cases produced 0 panics; all arithmetic is `u64`/`checked_*` (`crates/core/src/exe_icons.rs:270-291`, `:308-325`, `:339-357`), every index has a preceding bound, there is no recursion, and the caps mirror `gamehandler/exe_icons.py:24-27` with no off-by-one.
- **`credits.rs` is byte-exact and its oracle is not tautological.** `markdown()` is byte-identical to Python's 7,260-byte output, `README.md` contains it verbatim, and the count oracle at `crates/core/src/credits.rs:586-601` legitimately distinguishes `"Credit("` from `"CreditSection("` (25 vs 5 in `gamehandler/credits.py`) — a counting check that actually counts.

---

## Refuted

Two claims raised during this audit were tested and are false. They are recorded
rather than dropped, because a reader who has heard them should know they were
checked.

| Claim | Verdict | Evidence |
|---|---|---|
| "`stage_python` passes on an empty test suite, like `stage_test` does (`BUG-10`)" | **False** | `python3 -m unittest discover -s tests -t . -v` (`scripts/verify.sh:1362`) exits **5** on zero collected tests — measured on this host: an importable package with no test modules → `Ran 0 tests in 0.000s`, rc 5; a missing or non-importable start dir → `ImportError`, rc 1. Unlike `cargo test`, unittest cannot report success with nothing run, so no count floor is needed there. `BUG-10` is specific to `stage_test` |
| "The Python reference tree has been deleted, so parity claims cannot be re-derived" | **False** | `gamehandler/` is present and runnable (see `FEATURES.md` §0.1) |

---

## Not verified

Stated rather than asserted, per the audit brief.

- **`BUG-29`'s guard.** I did not verify that `crates/app/tests/app_id_lockstep.rs` / `dispatch_coverage.rs` enumerate `Page::ALL` exhaustively, so whether a tenth `Page` variant would fail a test before it reaches the `expect` at `crates/app/src/main.rs:979` is unverified.
- **`scripts/smoke-test.sh`'s exit code 77.** That value is what converts rc into a skip at `scripts/verify.sh:2428`; whether smoke-test.sh returns 77 *only* on the genuine no-compositor path is unverified (the file is outside the audited set).
- **`verify.sh:2408-2417`, the `--stage-timeout 0` foreground path.** A stage runs in the script shell rather than a subshell there, so a bare `exit N` in a future `stage_*` body would end the run with no `ok`/`FAIL`/`SKIP` line. No current stage does this (all `exit`s are inside subshells), so the hazard is theoretical and I did not construct a probe.
- **`archive.rs`'s containment root (`BUG-26`).** I could not construct an input where the unresolved root *accepts* an escaping member, and I am not claiming one exists.
- **Whether a runner install has ever completed on this machine.** `BUG-35`'s conclusion rests on the rule being over-strict — it rejects 1,818 of the 2,068 symlinks in a real Proton tree, measured — plus five abandoned staging directories under `~/.local/share/gamehandler/runners/` whose `runner.archive` files are **0 bytes**. Those five died in the download, not at this check, so they do not evidence the refusal either way; they evidence that no install here has reached the post-extraction stage. The rule, the mechanism, the one-line fix and the missing test case are all directly verified; the *frequency* of the failure in the field is an inference, and a machine with working network access could reach the check and fail on it — which is what the rule predicts and I have not observed.
- **`BUG-42b`'s non-UTF-8 GVFS mount name** was reproduced by the sub-audit with a synthetic mount directory rather than a real gvfs mount.
- **`BUG-40`/`BUG-41`'s reproductions** are the sub-audit's synthetic fixtures. I re-read every cited line in both the port and the reference and confirmed the asymmetry in each (the early return in `bounded_attempt`; the `data_len` check in `file_offset`), but I did not re-run the two PE fixtures myself.
- **The `integer()` 2^63 boundary** in `crates/core/src/models.rs:391-405` was suspected and found self-consistent with no observable divergence; omitted as speculation.

---

## Counts

| Severity | Count | Fixed | Partial | Withdrawn / not a defect | Open |
|---|---|---|---|---|---|
| P0 | 4 | 4 | 0 | 0 | 0 |
| P1 | 8 | 8 | 0 | 0 | 0 |
| P2 | 14 | 12 | 2 | 0 | 0 |
| P3 | 20 | 0 | 0 | 0 | 20 |
| — | 2 | 0 | 0 | 2 | 0 |
| **Total** | **48** | **24** | **2** | **2** | **20** |

Every P2 that is a defect is now closed. **This table covers the `BUG-xx` family
only** — the merged, all-family view is `PLAN.md`.

Two rows here are *not defects*, and both carry a severity cell of `—` so that
they cannot enter a severity total: `BUG-11` (case-variant categories) and
`BUG-15` (an unknown family id) were both **refuted by measurement** rather than
fixed. This table has now been wrong twice in the same way. The first time, a row
counted 46 while the document held 47 rows. The second time it counted 47 while
holding 47 with `BUG-15` still at `P2` and folded into `Fixed` — a refuted row
scored as a repaired one, which is the more damaging of the two errors because it
reads as work that was done. Both were caught by recounting the `Status:` tails
rather than by reading the table, and `BUG-15`'s severity cell was changed from
`P2` to `—` so that the arithmetic cannot repeat the second mistake: a row whose
own tail says `CLOSED — NOT A DEFECT` does not belong inside a severity bucket.

**Status is recorded in each row's `Status:` tail**, not here, so the two cannot
drift. `Fixed` means the root cause was understood, the fix is implemented and
committed, and a test was shown to **fail without it** — for every one of the 23,
by restoring the pre-fix body and observing the failure, except where the fix has
no restorable pre-state (`BUG-04`, `BUG-36`) and was verified end-to-end against
the real binary instead. `Partial` means one half of the row is closed and the
other is recorded rather than papered over. Two rows are withdrawn rather than
fixed: `BUG-11` was refuted by measurement and `BUG-15` records behaviour the
code does not have.
`BUG-47` is partial — the wiring is in place and the finding's strong form is
withdrawn, but the wiring cannot be asserted from a test because iced offers no
downcast and `Text::format` is private.

The P3 rows are genuinely open and are recorded as such rather than quietly
closed: each is a faithful copy of the reference's own behaviour or a cosmetic
divergence, none is reachable as a user-visible failure, and the brief's priority
order puts correctness and feature completeness above them. Where a P3 shares a
root cause with a fixed P2 it is named in that row.

**Where the weight is.** `BUG-35` is the most serious finding in the document
and the only one that breaks an advertised feature outright: the runner install
cannot complete for any real Proton build, and it fails *after* a full download
with a message that blames the user's archive. Fix that first. Then
`BUG-01`/`BUG-02` — the pair that destroys user data, and the only two where the
port is *less* capable than the reference it is a port of. `BUG-07`/`BUG-08`/
`BUG-09` are the three missing page-header controls and the cheapest fixes in
the list: each is a few lines against an existing message with an existing
handler.

The rest of the severe rows are failure and error paths (`BUG-03`…`BUG-06`,
`BUG-13`, `BUG-14`, `BUG-16`, `BUG-36`…`BUG-39`): the port follows the reference
faithfully on every happy path and loses it exactly where the reference has an
exception the port has turned into a value.

**On the dominant defect class.** The brief asked for checks that pass without
inspecting what they claim. There are eleven, and two of them are the most
expensive defects in the document:

- **`BUG-35`'s test pair is the strongest instance in the tree.** The escape
  check compares a value that is structurally always `""`, and the two tests
  that cover it — an allowed link with no `..`, a refused link that both rules
  refuse — pass in every state of the world, including the broken one. A
  *security* check that cannot observe the property it names, and a pair of
  tests that certify it while the feature is dead.
- **`BUG-40`/`BUG-41`/`BUG-43a`** are three doc claims ("the full file cannot
  hold a better icon", "clamping cannot produce a valid icon", "same worst
  case") that were used as licences to skip a check, each measured false.
- `BUG-10` (the Rust suite's exit-code floor), `BUG-16a` (a label that survives
  a change of subject), `BUG-16b` and `BUG-32` (counts reported, never
  required), `BUG-17` (a pin against one placeholder spelling, and a smoke call
  in place of an assertion), `BUG-18` (a haystack containing its own needle) and
  `BUG-39` (a comment asserting a permission the code does not set).

Two of the tree's guards — `dispatch_coverage.rs` and `app_id_lockstep.rs` — are
the opposite: they name their own blind spots in their headers and hold off
vacuity with real floors. `BUG-12`'s guard is the one that is neither: it greps
`main.rs` for a word where the property is about key delivery, so it certifies a
shortcut list the shell does not fully honour.

**On the three missing controls (`BUG-07`/`BUG-08`/`BUG-09`).** These are worth
one note beyond their individual rows: they share a single root cause, and it is
a process one, not a code one. The reference implements its page-level actions as
four `actions: [ … ]` blocks in QML (`LibraryPage.qml:18`, `RunnersPage.qml:21`,
`GameFormPage.qml:26`/`:31`, `Main.qml:64`) plus one page-entry hook
(`Main.qml:43`). The port's porting checklist (`docs/migration/PLAN.md` §4,
`P-01`…`P-78`) has rows for Add/Edit (P-18), the Runners list (P-32…P-35) and the
Plugin states (P-64) — but no row for *page chrome*, so four of the five were
relocated into page bodies or dropped, and the fifth (`GameFormPage`'s
Cancel/Save) survived because its row was about the Save gate
(`crates/app/src/view/form.rs:832-833`). A checklist item per page-header action
would have caught all three.
