# Verification findings

**Purpose.** The running record of every defect the verification pass has found,
what fixed it, and how the fix was checked. `REPORT.md` (T-20) consolidates this;
this file exists so findings cannot be lost between here and there, and so T-19
has one list to walk.

**Status.** Live. Not the final report — `REPORT.md` does not exist yet, and the
pages (T-09…T-15) are not built.

**How to read a row.** *Found by* names the mechanism, not the author: what was
run, and what it showed. A finding whose fix has not been independently
reproduced says so.

---

## 1. The defect class

One shape accounts for almost every entry below: **a check that passes without
inspecting what it claims.** The assertion targets an *input*, or a hand-written
copy of the expected value, or nothing at all — never the mechanism.

A test that compares two constants you also wrote proves nothing. A test that
compares `initials("Half-Life 2")` against `NO_COVER_LABEL` without calling
either builder cannot notice the two being swapped.

Two variants were found later:

- **Inverted** — a check that can *never* pass (finding 5 below). Found by the
  same method, from the other direction.
- **Same observable, different mechanism** (D-40) — the assertion targets exactly
  the right observable, and simply reaches it by another route, so a guard that
  ran earlier satisfies it and the guard under test never executes. Mutation
  testing reports "caught". Only mutating the *later* guard exposes it.

A fourth shape is not a defect in a check at all but in its **reader**, and is
recorded because it recurred: a green check is inferred to cover a different
check's territory because they build the same target from the same source (D-39),
and a general claim about a build flag is made from one measured mechanism
(D-41).

## 2. Findings

| # | What was wrong | Found by | Fixed | Verified |
|---|---|---|---|---|
| 20 | `verify.sh` ignored a stage's exit code, so a SKIPPED stage could not fail a run — a toolchain-less run exited 0 without inspecting the Flatpak at all | Running the script with a stage forced to skip | `9c523d0`; exit 3 for unrequested skips | Re-run with a stage skipped: exits 3 |
| 21 | `crates/app/src/view/` held 45 green tests that **ran nowhere** (`mod view;` was never declared), while `cargo test` reported success | Counting tests in the module vs. tests executed | `mod view;` declared | `17 passed; 3 failed; 44 filtered out` observed during the B-08 mutation run |
| 22 | `DECLARES_PY` was a substring match, so a bare `: install …` shell no-op passed as a real install | Reading what the matcher matched against | Rejects both bypasses; real installs still accepted | Both bypasses rejected, real installs accepted |
| 23 | The icon install was covered by no stage at all | Listing stages against manifest installs | `FLATPAK_CONTENTS` lists all five installs; the stage fails for any manifest install not covered | Stage fails on an uncovered install |
| 24 | The manifest's install *destinations* were compared by nothing | Same audit | Fixed | Destination mismatch fails |
| 26 | Stage 4 diffed a **two**-generator directory against **one** generator's output, so it could never pass — the inverted form | Reading what each side of the diff was generated from | Fixed | Independently verified it still FAILS on a genuinely corrupted fixture, so the fix did not trade a permanent failure for a permanent pass |
| 27 | The gate checked only the wide feature configuration; the narrow one could disagree | `cargo tree -p gamehandler-core -e features -i serde_json` → not enabled narrow, enabled wide (3 occurrences) | Two invocations, both configurations | Dropping `dxgi` from `DXVK_DLL_OVERRIDES` is now caught **in the narrow scope**, where the old test provably could not see it (lead, reproduced) |
| 28 | `view/widgets.rs`: no test calls the builders, so 9 mutations survive, 2 of them behavioural | Mutation testing | Open (UX) | — |
| 29 | An early-exit summary omitted the stages that never ran — a report of what passed, presented as a report of what ran | Forcing an early exit | `verify.sh` names skipped stages and exits non-zero | Skipped stage is named; exit non-zero |
| 31 | `--list` printed "the library is empty" and exited **0** without reading the library; `verify.sh` matched `--list\|--launch\|--version` **zero times** | Running the binary against a 2-game library | `e9ecf5e` (Architecture) | CLI stage fails before the fix and passes after; a sorting mutant fails only `cli-list-non-empty` |
| 32 | An all-placeholder Flatpak passed `verify.sh` *and* `smoke-test.sh` completely: six page bodies were the app's entire user-visible surface, and nothing could tell scaffold from finished | Reading both scripts for any reference to the placeholder pages — zero | **CLOSED (`50798f9` + UX's in-source test), in three complementary halves.** *Packaging:* `crates/app/tests/pending_pages.rs` — parses the dispatch arms out of `main.rs` and pins the set, independent of any table — plus `verify.sh`'s artifact cross-check (source count vs the shipped ELF). *UX:* `PENDING_PAGES`/`pending_task` (`main.rs:997`/`:1008`) and the rendering test, which asserts each page's body actually draws the placeholder. **The third half was a tautology until UX fixed it** — it compared `pending_task`'s output against `PENDING_PAGES`, which is the list `pending_task` reads (finding #43). | Lead reproduced both directions. Porting Library's arm while leaving it pinned FAILS UX's test with `Library is listed in PENDING_PAGES as T-09 but its body does not draw the placeholder; drawn: ["Library"]`. Packaging captured the artifact pair: **every byte-identity check passes while the stage fails**, which is the point — no installed file is `main.rs`, so nothing else in the stage could have shown it. |
| 33 | `update()` had no coverage: all five real handlers, `NavigateTo` included, could be no-oped under a green suite | Deleting each arm and re-running | Open (UX) — #38/#39/#40 qualify the fix | — |
| 36 | Five DLL-override constants pinned only against themselves in both scopes; the oracle corpus could not catch it | Mutation: flipping a literal survived the whole suite | `e16dd5f` — parses `runners.py` call sites at test time | Lead reproduced in the **narrow** scope: clean passes, dropping `dxgi` fails |
| 37 | `canonicalise_object_keys`'s object-in-object path unpinned in both scopes | Mutation | `e16dd5f` — array path pinned; object path documented as *behaviourally equivalent* narrow, with the honest bound stated | Reproduced by Architecture; the honest bound is the right answer (D-33) |
| 41 | `verify.sh`: a run that *finishes* with stages in `STAGES` never reached exits **0**, while printing that nothing was verified about them. `summary` computed the list as a `local` and discarded it; the exit tail checked only `FAILED` and `SKIPPED_UNREQUESTED` | Advocate, reviewing the gate at `ed59974` | `50798f9` — `UNRUN` is a global, and the exit tail at `:1420` routes a non-empty list to exit 3, with an extra NOTE when `STOPPED` is empty (STAGES and the runner disagreeing, not an abort) | Lead reproduced on a stub harness: all stages stubbed → **rc=0**; last `run_stage` line deleted → **rc=3**, `did not run (the run finished without reaching them): flatpak-contents` + `an unrun stage is not a pass` |
| 42 | `verify.sh:517` asserted the `cli` stage "is RED until #31 lands. That is intended". The line was **born false** — `e9ecf5e` (#31's fix) is an ancestor of `398a7c0`, the commit that wrote it | `git log -S` + `git merge-base --is-ancestor` | `50798f9` — replaced, not deleted, because the danger was the *forward* instruction; the replacement says in as many words that **a red cli stage is a defect** | Read at `:530-538`; the replacement states the born-false history and the exception (a missing binary) explicitly |
| 43 | The source-side pending-page test was **a tautology**: it asserted `pending == PENDING_PAGES.to_vec()`, where `pending` was computed by `filter_map` over `pending_task` — and `pending_task` *reads* `PENDING_PAGES`. The same list against itself. **LINEAGE CORRECTED (see below): this was an in-flight, never-committed intermediate in `main.rs`, not a defect in shipped code.** `git log --all -S 'PENDING_PAGES.to_vec()'` returns **empty** — the string exists in no commit. It is kept as a finding because observing it is what caused the redesign, and because it is the cleanest example of the class; it must not be read as a bug that reached the tree. **The file it was observed in is not the file it would be attributed to today** | Packaging, building the complementary half and finding it green while the page was ported | UX refactored **before landing** (`PENDING_PAGES` became a slice, the redundant `len()==6` went, and the test renders `view_body` and asserts the drawn strings) — the fix *is* `55a1e0f`'s test, which is a real check: a page landing while still listed fails it, and a `pending_page` arm absent from `PENDING_PAGES` fails it too | Lead reproduced: porting `Page::Library`'s arm while leaving it pinned FAILS with `Library is listed in PENDING_PAGES as T-09 but its body does not draw the placeholder; drawn: ["Library"]` |
| 46 | `widgets.rs`: the photo branch of `an_icons_picture_is_inset_inside_the_plate_by_the_icon_inset` asserts on `leaves()`, which is a witness of the laid-out **box**, not of a **decode**. `framed()`'s `Fixed(188)×Fixed(218)` container sets `Limits` `min == max` (`iced/core/src/layout/limits.rs:60-66`), and `Image::layout` resolves `intrinsic.min(max).max(min)` (`iced/widget/src/image.rs:257-268`) — which returns `188×218` for *any* intrinsic, **including the `Size::ZERO` a failed decode produces**. Measured (three inputs, lead's own probe on a HEAD tree): a real 118-byte VP8L `.webp` → `classify=Photo`, `measure_image=Some(24×16)`, `leaves=188×218`; UX's 12-byte PNG-magic fixture → `classify=Photo`, `measure_image=None`, `leaves=188×218`; a nonexistent path → `classify=Plate`, `leaves=80.96×89.6`. So `leaves()` **does** separate `Plate` from `Photo` — the third row differs — but not a `Photo` that decoded from one that did not. **Stated this way deliberately: an overstated defect invites the wrong repair, and asserting on the `Plate` case would let #46 be "closed" by a check satisfied by the thing it cannot distinguish — the #43 shape.** The assertion's *claim* ("a photograph is cropped to the box rather than inset") is true; the evidence offered for it is satisfied by a broken photograph | Packaging, building T-22; **independently reproduced by the lead** with a separate probe | **CLOSED (`7e8f2b3`)** — the repair landed with T-22's `.webp` assertion and on `measure_image` rather than `leaves`, exactly as the finding required; `with_photo` was repointed at bytes that decode, so the photo branch's assertion is no longer satisfiable by undecodable input. The bound is kept in a doc comment on the test itself, with the three-row table, so a later reader cannot re-derive the weaker check | Reproduced by both parties independently, with the same numbers. **And the fail direction measured by the lead, end to end:** with `"animated-image"` removed from `crates/app/Cargo.toml`'s `libcosmic` features and the whole suite re-run, **exactly two tests fail** — `a_webp_cover_is_decoded` and `an_icons_picture_is_inset_inside_the_plate_by_the_icon_inset`, both `left: None, right: Some(24x16)` at the same `assert_decodes` line — and **144 pass**. So the new check is a check, not a claim: it moves only with the decoder. The fixture was also verified independently of the test by decoding the 118 embedded bytes against the **system** libwebp (a different implementation from the Rust `image` crate's VP8L path): `24x16`, 216 distinct RGB triples over 384 pixels, four distinct quadrant corners. It is a picture, not a flat block |
| 47 | `verify.sh:1413-1415` says a `begin X; finish_ok` that never calls the stage, and a `run_stage` naming the wrong function, "both claim a result without producing one, and **both land here**" — the exit-3 `UNRUN` branch. **Both halves of that are wrong.** (a) `begin X; finish_ok` **exits 0** with the stage listed under `passed`: `begin` appends to `ATTEMPTED`, so the stage is never in `UNRUN` and the branch cannot see it. (b) A `run_stage` naming a nonexistent function never gets that far — `run_stage`'s own `declare -F` guard exits **2** before `begin` is reached, which is the *better* outcome and is not this branch | Advocate, reviewing the gate; **reproduced by the lead** on a stub harness with every stage stubbed green | Open (Packaging). `begin cli; finish_ok` → `EXIT=0`, `passed: … cli …`. Cheap fix is the comment; the class-removing fix is `finish_ok` refusing to report `ok` for a stage `run_stage` never ran | Reproduced. **And the ancestry measured, in the form that matters for the repair:** the mutant exits 0 at `50798f9^` *and* at `50798f9` — but for **different reasons**, which "pre-existing" alone would hide. `50798f9^` contains **0** occurrences of `UNRUN`, so it exits 0 because there is no unrun-stage check at all; `50798f9` contains **7**, so it exits 0 because the new check exists and `begin` **bypasses** it. The fix therefore did not introduce the gap, but it **newly claimed** to catch "a stage that claims a result without producing one" — a class its guard cannot see. That is the precise form of #47, and it is why the repair is to make `begin`-without-a-stage detectable rather than to add the branch again. Third case in this project of a comment that was *born* false rather than gone stale |
| 48 | `run_stage <name> <fn>` never compares `<fn>` to `<name>`. #47's fix makes `finish_ok` test `[ -z "$STAGE_RAN" ]` — **non-empty, not *which* function** — and `run_stage` sets `STAGE_RAN="$fn"` to whatever it was handed. So the legitimate-looking edit `run_stage cli stage_test` (a copy-paste slip across eleven near-identical consecutive lines) reports **`cli` passed** while the test suite ran in its place, and exits **0**. The `cli` stage would contribute nothing to the run and the summary would claim it as coverage. **Filed separately from #47 on purpose:** #47's subject is a stage that claims a result *having run nothing*; this is a stage that *ran a different function*. Folding it into #47 would make a closed item's description broader than its subject, which is the class this file exists to record | Advocate, reviewing #47's fix; **reproduced by the lead** | Open (Packaging). The **class-removing** shape is to carry the function name in `STAGES` itself (`name\|fn\|description`) and drop the second argument at all eleven call sites, so the pairing exists in exactly one place and *cannot* disagree — the same move as #39's single message list. A comparison inside `finish_ok` is the detection shape: cheaper, but it leaves the wrong state representable. Deriving the name by convention does **not** work — `oracle-freshness → stage_oracle`, `flatpak-build → stage_flatpak`, `smoke-test → stage_smoke` fail `stage_${name//-/_}` on three of eleven, so it needs a table either way | Lead, independently of the advocate, on a stub harness with every stage stubbed: `run_stage cli stage_cli` → `EXIT=0`; `run_stage cli stage_test` → **`EXIT=0`**, summary byte-identical, `passed: … cli …`. Third instance of the one-directional enumeration reported in the `ed59974` review (finding 4): the deleted last `run_stage` → exit 3 (#41), `begin X; finish_ok` → exit 2 (#47), the wrong function → still 0 |
| 49 | `crates/core/src/runners/launch.rs:1153` — `the_game_environment_block_reaches_the_dxvk_installer_and_not_only_the_child` intermittently fails with `Io(Os { code: 26, kind: ExecutableFileBusy, message: "Text file busy" })`. The test writes `<scratch>/wine` as an executable `/bin/sh` script (`write_script`, `launch.rs:753-760`: `fs::write` then `set_permissions(0o755)`) and `launch` execs it, so the failure is a **write-then-exec race on a freshly created file** — the classic ETXTBSY, which is also a very strong *hypothesis* and not a demonstrated mechanism, and is recorded as one. **The consequence is what makes it a finding rather than a nuisance:** `verify.sh`'s `test` stage is non-deterministic, so a green gate is a probability rather than a fact — and a gate that is green six runs in seven is worse than one that is reliably red, because its red runs get retried away | **Packaging, while validating `889f7a5`; NOT reproduced by the lead.** Packaging saw 1 failure in 7 runs of the whole `--lib` suite (358 passed, then 357/1, then 358) and captured the panic with `code: 26`. The lead's two attempts did **not** reproduce it: 1 in 12 runs of the full git-archive HEAD tree — **and that single count is not trustworthy, see below** — then **0 in 20** runs of the isolated test, warmed first so no run compiled | **CLOSED (`847ec89`), and the standing hypothesis was falsified rather than confirmed.** The lead had recorded the write-then-`chmod` window in `write_script` as "a very strong *hypothesis* and not a demonstrated mechanism"; Architecture demonstrated it **false** with a standalone reproducer (not committed) — one writer, no forking thread: **0** refusals over 300 solo runs; refusals scale with concurrent writers (2 writers 79, 4 writers 893) and with a forking thread present (4 writers + forker 2072). The mechanism is **inode-specific, not process-wide**: the decoy arm — same load, but the exec'd script created before any thread existed — records **0** refusals at both 4 and 8 writers. A thread forks, the child inherits a copy of *another test's* just-written script, that copy holds the inode's writable reference until the child reaches its own `execve`, and an `execve` of that inode inside the interval is refused. **The textbook fix was tested and does not work:** write-temp-then-`rename` carries the *inode* to the new name, so the inherited descriptor follows it to the exec'd path — 1568 refusals under the load that gives the decoy 0. Architecture had it written down as the fix before measuring. **The refusal is the test binary's, not the launch path's** — `launch.rs` contains no `fs::write`, `set_permissions`, `File::create` or `OpenOptions` at all (its only filesystem writes are two `create_dir_all` at `:334`/`:465`), so a guard inside `launch` would be a catch on a condition the launch path cannot enter — the class ruled vacuous at `9efa4e2`. **Scope corrected (finding #54):** the earlier wording said "not production's", and that was too broad — production *does* write executables, in `archive.rs:684-688`. The claim the repair needs is only that nothing in the **launch path** writes one, which is what the evidence supports. The repair is therefore in the test module: `launched_or_busy_retry`, bounded at 100 attempts, retrying **only** `ErrorKind::ExecutableFileBusy` and returning every other error on the first attempt, with the terminal outcome returned as-is so a genuine failure still fails the caller's `expect`. **Only three sites actually exec and all three are converted**; the lead checked the four remaining raw `launch(` calls in the test module (`:1171`, `:1189`, `:1228`, `:1239`) and each asserts a *validation* refusal — "Mount the share for…", "No executable is configured", "NVAPI/DLSS requires a Proton runner through UMU" — reached in the command builder before any spawn, so none can flake. **FOLLOW-UP CLOSED (`66f4a21`), and verified by the lead with mutations rather than by reading.** The advocate's construction is now a committed test: the window is **held open** (`hold_open_for_writing` takes a write handle and returns the closure that closes it) rather than waited for, which is what makes it deterministic — the original flake was a race that only *sometimes* opened the window, and this test opens it on purpose. Three tests, and each property's removal reddens one, measured in a scratch copy of HEAD: `ATTEMPTS: 100 → 1` fails **both** `the_retry_recovers_when_the_write_window_closes` (on the raw `Io(Os { code: 26 … })`) **and** `the_retry_gives_up_when_the_window_outlasts_the_budget` (on `elapsed >= 80ms`, which is what distinguishes "retried then gave up" from "never retried"); dropping the `ExecutableFileBusy` kind check to `if true` fails `an_error_that_is_not_the_window_is_not_retried`. So the positive test is non-vacuous — it engages the retry rather than passing because no window opened — and the two control arms D-47 requires are present and load-bearing. **This is the repair done the way the finding asked for:** the mechanism is constructed, not waited for, and the instrument fires | **Both parties, two different ways.** Architecture: the launch battery went **7 failures in 40 runs → 0 in 40**, and 0 in a further 40 after the final wording edit. Counts reported as required by D-45: **358 at `847ec89` with `Compiling gamehandler-core` observed**. **The retry is not dead code, and this was measured rather than assumed** — an instrumented build (logging to a file, since `eprintln!` is captured and discarded by the harness) recorded **4 firings in 30 runs, each at `attempt=1`**, every one a would-be failure turned into a pass. That is the #51 lesson applied *proactively* by the author: a repair whose instrument never fires is a catch on unreachable code, and they checked before claiming. Lead verified the production-vs-test split, the retry's error-kind scope and bound, and the four unconverted sites by reading `launch.rs` — **Packaging's ruled-out list, kept so nobody re-derives it:** all 62 `scratch("…")` labels in `crates/core` are unique crate-wide; the three `scratch` helpers use three distinct path prefixes (`gh-env-`, `gh-runners-`, `gh-oracle-`); and it reproduces in a `--lib` run, which is a single test binary with no concurrent sibling. **And a self-report against the lead's own instrument:** the 1-in-12 number came from a shell harness whose `grep` for the result line skips a run that produced no parsable line — so its denominator cannot tell "failed" from "did not finish", and it printed a total it could not substantiate per-run. That is the *same* defect class as everything else in this table, committed by the person filing it, and it is why the count above is attributed rather than asserted |
| 50 | **A stale test binary prints a green count and `cargo test` says nothing.** The advocate appended a test to `installers.rs`, backdated that file's mtime to 2025-08-07, and ran `cargo test -p gamehandler`: cargo did not rebuild, ran the **stale binary**, and printed `test result: ok. 147 passed; 0 failed` — probe test absent, nothing in the output saying so. **This is the false-*pass* direction, which §3's rule did not cover:** that rule ("a failure that contradicts the code is more likely a stale artifact") works because a contradiction makes someone look; a green count makes nobody look, so the project's own last line of defence reported green over a binary that was not the source. `rsync -a` preserves mtimes, so ordinary syncing of a scratch tree reaches it, and D-33's build-scope rule does not apply — the mechanism is mtime skew | Advocate, while checking the count-reporting rule; measurement in §3 | **CLOSED (`74e7ff9`)** — the `test` stage forces the binary (`cargo clean -p gamehandler-core -p gamehandler`, `:603`/`:619`) so a reused artifact cannot report green. The lasting part is **D-45**: a count is admissible only as *N at commit X with `Compiling <crate>` observed* | Lead reproduced the protocol end to end: touched `main.rs` + `core/src/lib.rs`, ran the workspace suite, and observed `Compiling gamehandler-core` then `Compiling gamehandler` before `164 + 3 + 2 + 358`, 0 failed. The clause has since been used by every agent without being re-asked |
| 51 | `runners.rs` — **`remove_press` was extracted with no production caller, so the mutation the extraction was built to catch is caught *vacuously*.** The helper is defined at `:588` and called only from its own test at `:1257`; the site it was extracted from still builds the message inline at `:431` (`.on_press(Message::UninstallRunner(row.runner_id.clone()))`). M1 (helper body → `"system"`) is **CAUGHT** — but mutating a function nothing calls (the pre-wiring state) changes no production behaviour, so the red proves only that *the test reads the helper*, not that the helper is *used*. The advocate's M3 (delete the helper and its 44-line test) **SURVIVED**, which is the same fact stated as 66 deletable lines. Three doc claims were false as a result: `:578` "`InstalledRow::runner_id`'s remaining consumer" (it has none), `:416` "rather than a literal buried in a builder" (the literal **is** still buried there), `:594-596` "`installed_card` could stop calling this" (presupposes a call that did not exist). **The compiler was silent by construction:** `#[allow(dead_code)]` on `mod view` (`main.rs:38-39`) covers everything nested inside it and is not removed until T-09, so the lint that flags this immediately is off for exactly the window in which such helpers get written | Advocate, reviewing the `382f431` rename; **reproduced by the lead** by listing every occurrence of `remove_press` in `crates/` (definition, own test, doc prose — no caller) and reading `:431`. Second instance in two commits: `6823ff5` removed `BADGE_WIDTH` for having no caller | **CLOSED (`c17c7da`)** — the site was wired (`.on_press(remove_press(row))`), which is the repair the lead ruled for: it makes the M1 catch non-vacuous, turns deletion into `error[E0425]`, and makes all three prose claims true at once. **D-46** records the rule | Lead reproduced at HEAD `13e9806`. **Note on the review's commit:** the advocate reviewed `382f431`, which Architecture then amended into `13e9806`; checked with `git diff 382f431 HEAD` — **empty**, so the tree is identical and the review stands verbatim. Recorded because a review names a commit, and an amend under it would otherwise void it silently Architecture re-measured post-fix: M1 CAUGHT, M2 (delete helper) now `error[E0425]` in both targets — the advocate's forward prediction confirmed. **And Architecture self-corrected the overclaim in their own approval**: "non-vacuous *because the helper is wired*" was itself D-40's shape. The honest bound, recorded: the **helper's value** is closed; the **site's** mutation (routing around it inline) still survives; and the page is still not production-reachable, so what the wiring bought is a caller *within the crate* — which is exactly why deletion became a compile error |
| 52 | `verify.sh` — **a skipped stage printed the previous run's log, so it rendered as a stage that ran and passed.** `begin` set `STAGE_LOG` but never truncated it, so `finish_skip`'s `echo_subchecks` printed whatever the last run left at that path. Measured: a run with `--skip-flatpak` showed four `ok` lines under `SKIP smoke-test` (`cli-version`, `cli-list`, `no-display-diagnostic`, `gui-stays-up`), **byte-identical to a `smoke-test.log` written two hours earlier** — and the indented lines are the format reserved for a stage's *own* sub-checks reporting a result it produced. Worse than the `flatpak-contents` case because there is no `ok <stage>` line to contradict the sub-checks: they are the stage's only account of itself | **Packaging, self-reported** — found while testing #50's fix on the same file | **CLOSED (`a2dc427`)** — one line in `begin`, `: > "$STAGE_LOG"`. Truncating rather than teaching `finish_skip` not to read makes the state **unrepresentable at every call site**, not just the two that skip today; and `finish_skip`'s 98/99/77 reasons point a reader at the log, so those stages' pointers still resolve | Pre/post on a stub harness with a planted stale `smoke-test.log`: pre — `SKIP smoke-test` followed by the two planted `ok` lines (labelled `STALE-52`); post — nothing under it, file empty. Then the same comparison on the real tree |
| 53 | `verify.sh` — **#52's fix rests on a silent write.** `begin` now does `: > "$STAGE_LOG"`, and the script is `set -uo pipefail` with **no `set -e`** — so if that truncation ever fails, it fails **silently** and restores exactly the state #52 removed (a skipped stage reading and printing a previous run's log). The window is narrow: `LOGDIR` is created at `:268`, before any `begin`, so the directory normally exists. But the fix's own claim is "unrepresentable at every call site", and an unchecked write is the one path that re-enables it. The claim is **broader than the mechanism** — the same shape as #51's, one `verify.sh` over | Advocate, reviewing `a2dc427`; **lead confirmed the script has no `set -e`** | **CLOSED (`ac76e4e`, Packaging) — the guard does not continue.** `scripts/verify.sh:330` is now `if ! : > "$STAGE_LOG"; then … exit 2`, and it is the **only** unchecked truncation left in the file (`grep -n ': > "\$' scripts/verify.sh` → that one line, which is inside the check). Packaging demonstrated it on the real script, unstubbed: with the log directory uncreatable (`target/` at `dr-xr-xr-x`) and again with `build.log` at `-r--r--r--`, both print the explanation and **`EXIT=2`**. The "instead of passing" half was measured A/B against the pre-fix `044a031`: pre `EXIT=0` with the stale sub-checks printed under `SKIP smoke-test`, post `EXIT=2` with none. The ordinary path is unchanged — writable log dir, normal summary, `EXIT=0`. **Packaging also corrected the finding's own wording, which matters because the word was the whole difference between their finding and the lead's:** bash *does* print `Permission denied`; what it does not do is stop, and the run still exited 0. And they caught their **first A/B run being vacuous** — `HEAD^` had moved under their own `cargo-sources` commit, so the "pre" harness had been built from the post-fix file and the comparison proved nothing; the baseline is `ac76e4e^` = `044a031`, confirmed by reading the line. Options considered, kept as filed: check the redirect (taken) or make the log path impossible to fail at | Lead verified the guard's body and its `exit 2` at `:330`, that no unchecked truncation remains, and that the fix's claim is now scoped the way the finding asked — with the honest bound left in the row above: the directory normally exists, so the window is narrow, which is why the *claim* was the defect and not the exposure. The advocate's independent extraction of `begin`/`echo_subchecks`/`finish_skip`/`run_stage` reproduced #52 and its fix at the same time |
| 54 | **A commit's scoping sentence was broader than the diff, and the lead had recorded it as verified.** `847ec89` says "a guard in `launch` would be a catch on a condition production cannot enter", and the lead's findings row wrote "production `launch.rs` contains no `fs::write`, `set_permissions`, `File::create` or `OpenOptions` at all". That is true **of `launch.rs`** and false of production: `crates/core/src/runners/archive.rs:684-688` (`extract_members`, reached from `proton.rs:1046`) does `File::create` + `io::copy` + `flush` + `set_permissions(mode)`, and it is **production** — `#[cfg(test)]` sits at `:778`, after it. The practical exposure is near zero, because the tight window needs write-then-*immediately*-exec of the same inode and only the test harness does that; **the repair stands unchanged.** What was wrong was the sentence, and the row above now scopes it to "nothing in the launch path writes an executable" | Advocate, note (c), while rebuilding the mechanism from scratch | **Documentation correction**, landed with this row (the `#49` findings text was rewritten to say "the launch path", not "production"). No code change, and none needed — recorded because the lead had cited this as an independently verified claim, and "I verified it" is exactly the phrase that should mean the verification covered the claim's scope | Lead reproduced: `extract_members` at `archive.rs:622`, the `#[cfg(test)]` at `:778`, the write at `:684-688`, and the call from `proton.rs:1046` |
| 55 | **P-67 (theming) has no implementation, and the guard that would have kept Settings visible cannot see it.** The reference applies the persisted scheme at **two** sites: `theme.py:85` `apply()` → `self._app.setPalette(build_palette(scheme))`; `main.py:106` `theme.apply(backend.settings.color_scheme)` **at startup**; and `bridge.py:201-202` `self._theme.apply(value)` **on change**. The port has **no `Shell::theme()` override** and `settings.color_scheme` is written at `main.rs:975` and read by nothing but the Settings selector. So choosing light/dark/system today changes a stored string and nothing a user can see. **This is a parity item, not a nicety:** P-67's own verification column reads "see D-13". **And Settings has just left `PINNED_PENDING` (5→4)** because the page now renders — which is *correct by that test's definition*, since `PINNED_PENDING` asserts only "renders a placeholder". The gap is that `PINNED_PENDING` was never a "done" check, and no other guard covers P-67. This is #32's shape: a guard that agrees the page is finished while a P-item under it is unmet | **UX, self-reported** — their own DECISIONS entry records that the color-scheme decision "cannot be evaluated until theming exists" and that `view_mode`'s effect has no reader either. **The lead verified the gap against the reference independently** (`theme.py:85`, `main.py:106`, `bridge.py:201-202`, and the absent `theme()` override) | **Open — T-24, owner UX.** Acceptance: (a) persisted scheme applied **at startup**; (b) changing it changes the app; (c) `"system"` follows the desktop; (d) the three-value `COLOR_SCHEMES` tuple and the persisted `colorScheme` key survive so a saved choice is never lost; (e) **Phase 3 verifies P-67 in the running Flatpak** | Lead reproduced the two call sites and the absent override. **UX was right to record rather than paper over it** — the entry deliberately leaves the default at `"dark"` so it stays open instead of being closed by an unexamined default |
| 56 | **A bare `git commit` commits the whole index, so four agents sharing one tree produced a commit containing more than its message said — and the reproducibility gate the lead broadcast in response was wrong in two places.** The lead staged one docs file and got a **six-file** commit carrying Architecture's staged source work (`http.rs`, `app/Cargo.toml`, `view/runners.rs`, `main.rs`, `Cargo.lock`). The repair raced too: soft-reset plus unstage is two commands with a window between them, and Packaging's `#53` commit landed inside it, carrying the docs file back out under *their* message — so `ac76e4e` remains contaminated. Nothing was lost, and **that was luck, not design:** nobody ran `git show --stat HEAD` against the intent. **The two commits are `6b9746c` (reset away; reachable from no ref) and `ac76e4e` (still in history).** The lead then broadcast a rule — "always name paths, never `git add` first" — which **the advocate tested before adopting, and broke in two ways.** (1) A path git has never seen cannot be committed by pathspec at all: `error: pathspec … did not match any file(s) known to git`. (2) **The pathspec form commits the *working tree*, not the index** — measured with the index holding one version and the worktree another, the worktree won; and `git show --stat` **cannot see it**, because neither the path nor the file count changes. The lead reproduced both. So the rule as first written closed the original door and opened a sibling, **and the check offered to catch it passes on the failure** — #50 and #53's shape exactly | **Lead, self-inflicted** (the contamination); **advocate** (both holes, tested before adopting the rule) | **CLOSED as a rule (D-49 + its correction, `683c540`/`8f522d9`).** Corrected procedure: `git add -- <path>` only for a brand-new path; `git commit -- <paths>`; know that it takes the worktree; `git show <sha>:<path>` when index and worktree differ on purpose; `git show --name-only <sha>` after. **History deliberately not rewritten** — blast radius measured as this one commit across the last 30, both halves complete, and D-46 clause 2 makes an amend void a review | Both parties reproduced both holes. **The advocate added `git show --name-only <sha>` to their own review routine**, noting their content-pinning checks (`git show <sha>:<path> \| cmp`) did not enumerate what *else* a commit contains, so this class could have passed them — **a check narrower than the claim it supports is the same defect as no check** |
| 57 | **A dropdown closure stored the stringified index, so choosing a category filtered on `"1"` instead of `"Puzzle"` — in shipped code, with every test in the suite green.** `crates/app/src/view/library.rs:253-256` at T-09 (`2f017fd`): `cosmic::widget::dropdown(categories, category_index(&categories, page.category), \|category\| Message::SetCategoryFilter(category.to_string()))`. libcosmic's `on_selected` is `impl Fn(usize) -> Message` (`src/widget/dropdown/mod.rs:30`), so that parameter is an **index**; naming it `category` made `.to_string()` read as a name while producing `"1"`. The stored value is a valid `String`, the page draws, and the filter matches nothing — so selecting a category shows the empty state while every check reports success. **Nothing in the file could see it, because the closure is inside a builder and a builder needs a renderer** — the same wall as #51's D (a button's pressed message) and the same shape as #46: a wrong value no assertion in the file can reach | **UX, self-reported in T-13** — found while building the Settings page's selectors, and reported as a defect in their own earlier T-09 work rather than as a footnote. **Lead verified independently:** the closure is at `library.rs:253-256` of `2f017fd`, and `on_selected: impl Fn(usize) -> Message + Send + Sync + 'static` is at `dropdown/mod.rs:30` of the vendored libcosmic — the closure's parameter is an index by the type, not by convention | **CLOSED (`7de961b`)** — `library::category_selection(options, index)` (`:179`) holds the index-to-name mapping in one place, with `.unwrap_or(ALL_CATEGORIES)` for an out-of-range index, and `a_selection_carries_the_name_and_not_the_index` fails for the stringified-index version. The sort dropdown was already correct | Lead reproduced both ends: the old closure at `2f017fd:crates/app/src/view/library.rs:253-256`, and the current file where it survives only in doc prose and one test sample. **The generalisable part, which belongs in the record and not just in that file's comment: a closure inside a builder is unobservable to every test in its own module.** The mapping has to be a *function* — not because it is tidier, but because a function is the only form an assertion can reach. Third instance of this wall (#46, #51's D, this), which is what makes it a rule rather than an incident |
| 58 | **The theme's "system" mode falls back to `dark` on any error, so a check that "system" works can pass by hitting the fallback — and D-13's decision can be declared evaluated when it was not.** `system_preference()` (`libcosmic src/theme/mod.rs:119-132`) has **two** early returns, both `Theme::dark()`: `:121` when `ThemeMode::config()` fails (config unreadable) and `:125` when `ThemeMode::is_dark(&mode_config)` fails. So in any environment that cannot read `cosmic-config` — no desktop session, an unreadable config, **a sandboxed Flatpak** — "Match system" is `dark` by the error path. Two consequences, and the second is the one that matters: (a) if the target environment cannot read the config, `"system"` and `"dark"` behave **identically** there, so D-13's two options collapse; (b) **a verification that selects "Match system" and observes a theme has not distinguished "the desktop said light" from "the fallback fired"** — so Option 2 could be recorded as landed while dark is hardcoded by an error branch. That is the silent-fallback shape of the bundled-root-store concern, and it would make **D-13 look evaluated without being evaluated** — #50/#53's class, aimed at a decision instead of a stage. `Theme::dark()`/`light()` themselves are pure constructors (`:205`/`:213`, no display, no ambient state), so the light/dark arms **are** headlessly testable — which is exactly why the fallback needs an explicit check rather than a smoke test | **UX, while scoping T-24** — reported as a finding that "bears on D-13's answer, not just its plumbing". **Lead verified against the vendored source:** `system_preference()` at `theme/mod.rs:119-132` with both `return Theme::dark()` branches, and the two pure constructors | **Open.** T-24's acceptance criterion (e) must distinguish the two: a check that fails when the fallback fires, not one satisfied by it. The durable form is to assert the *selection* maps to the intended `Theme` **and** that the system arm is reachable — or to assert on the palette (`Theme::cosmic()` exposes it) rather than on "a theme exists"  **Addressed by T-24's in-flight design rather than by amending criterion (e), and it is a better answer than this row asked for.** `theme_for(scheme, system)` (`theme.rs:115`) takes the system theme as a **parameter** instead of calling `system_preference()` itself, precisely so a test can hand it a theme that is **neither light nor dark** and assert the scheme *defers* — the property that separates "asked the system" from "fell back to dark", and one that is unobservable if the function calls `system_preference()` internally. `theme.rs`'s module doc (`:52-61`) cites this row by name, and the test constant is deliberately high-contrast **light** rather than another dark theme, so a fallback cannot be mistaken for a deferral. That moves the distinction from Phase 3 down to a unit test, which is the durable form this row asked for. **It closes when T-24 lands** (`theme.rs` was uncommitted when this was written) — do not file it separately.| Lead reproduced both branches by reading `theme/mod.rs:119-132`. **Recorded because T-24's own acceptance criterion as first written ("Phase 3 verifies P-67 in the running Flatpak") is satisfied by the fallback** — the criterion had the defect it was meant to catch, before any code was written |
| 59 | **§5 named an invariant that no test asserts.** §5 (this file) says of the reference's `str(exc) or exc.__class__.__name__` (`bridge.py:157`): "**the invariant a test should assert is that the message is never empty.**" There is no such test. `a_failed_fetch_stores_its_message_for_the_status_line` (`view/runners.rs:1293`) stores and renders the sample `"rate limit exceeded"` — it pins that *a* message survives, not that an empty one cannot. And the Rust side has no equivalent of Python's fallback: `Result<_, String>` at the app boundary accepts `""`, and `RunnerError::Http { message }` / `UnreachableShare { message }` forward it through `f.write_str` unchanged (`core/src/runners/mod.rs:214-215`), so nothing in the error path enforces non-emptiness. **The exposure is small and the bound is stated rather than inflated:** the only renderer is `status_line` (`:210`), which wraps the message — `format!("Could not fetch builds — {message}")` — so an empty message produces `"Could not fetch builds — "` with a dangling em-dash, **not a blank line.** Production callers are also non-empty in practice: `http.rs:106`/`:122` use `error.to_string()` of a `ureq::Error`/`io::Error`, and the rest are literals (`"Unexpected GitHub releases response"`, `:183`/`:534`/`:538`) or a resolver-supplied sentence (`launch_opts.rs:843`). **So this is not a user-visible bug today** — it is the gap between a criterion being *stated* and being *checked*, which is the shape that lets a criterion read as satisfied | **Lead, verifying T-11 against §5** — the same check that confirmed the other two §5 items (the headers at `proton.rs:506` with their test at `:1706`, and the 12/30 s call at `runners.rs:609`/`:620`). Those two hold; this one does not | ****CLOSED (`d255d36`, Architecture) — and the repair went further than this finding asked, in the direction that matters.** The guard became `rendered_message` (`view/runners.rs:650`): `error.to_string()`, and **only when that is empty**, `error.class_name().to_string()` — so the ordinary case is untouched and the fallback is the Python `str(exc) or exc.__class__.__name__` shape rather than a blanket default. `class_name` (`core/src/runners/mod.rs:200`) is the analogue of `__class__`. The test (`a_failure_never_renders_an_empty_message`, `:1389`) is the part this file should hold up: **it asserts the premise before the conclusion** — `assert_eq!(error.to_string(), "")` with the message *"the premise: {name} renders empty, so the fallback is what is under test"* — which is exactly what the finding said a sample-based test could not do, and it closes the hole that a fallback test would otherwise **pass for an error that was never empty to begin with**. It drives **all four** variants whose `Display` can be empty (`Http`, `UnreachableShare`, `Io`, `Archive`), not just the one the fetch path produces, "because a test on `Http` alone would pass for an implementation that special-cased it". And it asserts what the user actually reads — `status_line(&ReleasesStatus::Error(rendered_message(&error)), 0) == Some("Could not fetch builds — Http")` — with "a dangling dash is the defect this prevents" | Lead verified the guard, `class_name`, the four-variant loop, the premise assertion and the `status_line` assertion by reading them, and ran the test (**1 passed**). **Non-vacuity, stated with its bound rather than inflated:** the premise assertion is what makes the target determinate — the input provably renders `""`, so the only reachable path to the asserted `"Http"` is `class_name()`; the finder's proposed `assert!(!rendered_message(&e).is_empty())` is also non-vacuous by the same premise. A mutation round-trip was **not** run: the workspace target is 11 GB and an isolated rebuild was not affordable, so this is construction plus premise, not a measured kill — recorded rather than claimed. One divergence from the finding, correctly: it wanted a one-line *test* and the author added a function plus the test, which is the better placement because it makes the invariant structural on the render path rather than only asserted about it — Lead reproduced: `:214-215` forward the `String` unchanged; the only renderer at `:210` wraps it; `:1293`'s test uses a non-empty sample; no `is_empty` assertion exists on the error path. **Recorded against my own criterion**, because §5's standing depends on its items being checked and not merely written down |
| 60 | **`mod http;` was committed without `http.rs`, so four consecutive revisions of `main` do not build — and the lead had already recorded the opposite as verified.** `git log --all -S 'mod http;' -- crates/app/src/main.rs` returns **`7de961b` and nothing else**: the declaration entered history in UX's T-13 commit. At that revision `main.rs:30` is `mod http;` and the module file is absent — `git cat-file -e 7de961b:crates/app/src/http.rs` → `fatal: path 'crates/app/src/http.rs' exists on disk, but not in '7de961b'`. `http.rs` first appears at **`ace60fa`** (`git log --all --diff-filter=A --` returns that commit alone), which is a **descendant** of `7de961b` (`git merge-base --is-ancestor 7de961b ace60fa` → yes) — so this was repaired by **committing the module**, not by dropping the declaration. Four revisions carry the declaration without the file, measured one at a time: **`7de961b`, `a52323f`, `86337d9`, `d97e01a`** — `git rev-list --count 7de961b^..d97e01a` = **4**. **Three of the four are the lead's own commits.** The diagnostic is `error[E0583]: file not found for module \`http\``, reproduced from first principles rather than inferred: `printf 'mod http;\n' > lib.rs; rustc --edition 2024 --crate-type lib lib.rs` emits it verbatim, help and note lines included | **Advocate, blocking T-13's sign-off** — filed against UX's premise that the declaration was removed before committing. **Lead verified every link independently:** the `-S` lineage, the absent blob at `7de961b`, the adding commit, the ancestry, and the per-revision table. UX's report is **false as stated** — measured, not argued | **Documentation only; history deliberately not rewritten.** `ace60fa` is the repair and it is already in `main`; the four revisions are ancestors of HEAD, so rewriting them rewrites every commit since — and D-46 clause 2 makes an amend void a review. **The live cost is `git bisect`:** the window is exactly four consecutive commits that cannot build, so a bisect across it lands on a non-building revision at every step in the range. No code change and none needed | Lead reproduced the `E0583` text byte-for-byte, and re-derived the four-revision table with each cell's command |

| 61 | **T-26 landed 2022 lines of new core code that nothing declares, so it is compiled nowhere and 37 of its tests run nowhere — under a suite reporting a confident `361 passed`.** `e98b4b1` adds `crates/core/src/plugins.rs` (**1204** lines, **23** `#[test]`) and `crates/core/src/credits.rs` (**818** lines, **14** `#[test]`) and touches nothing else — `git show --stat e98b4b1` is those two files, 2022 insertions. `crates/core/src/lib.rs` at that commit declares `covers, json, models, paths, runners, settings`: **no `plugins`, no `credits`**, and the working tree agrees. An undeclared module is invisible to `cargo check`, `cargo clippy` **and** `cargo test` — it is not compiled at all — so every one of those gates stayed green over 2022 lines that no compiler had read. The suite's count at HEAD is `361 passed; 0 failed` (measured), a number that omits all 37. **Nothing in `crates/` references them either**, which is why no unresolved-path error makes the build point at the gap: the only occurrence anywhere is a comment at `main.rs:822` | **Lead, by running #60's sweep against the working tree** while the two files sat staged — the sweep found it **because** #60's row was written to check both directions, and the reverse direction is this one. Second time in this file that a check written for one defect caught its sibling. **ATTRIBUTION CORRECTED — this was not UX's oversight, and UX disclosed it.** This row was filed as "Open — UX, T-26", i.e. a defect in UX's commit. It is not, and `e98b4b1`'s own message says so in as many words: *"`crates/core/src/lib.rs` belongs to Architecture and is untouched, so the two `pub mod` lines that make these modules compile are theirs to add."* Those words restate an ownership boundary **the lead set** ("Architecture keeps `lib.rs`"; "do not edit `lib.rs`"), so UX **could not** add the declarations and **said where they were owed**. The count was labelled too — *"taken in a detached worktree with those two lines applied"* — so **398 was never presented as HEAD's number**. So the tree-external fact stands and the person-level charge does not: the defect is an **ownership gap** — a file one agent owns, a change a second agent needs, and no handoff that either can trigger — compounded by the lead having created the boundary and then not watching it | ****CLOSED (`4cf3351`, UX, "declare core::plugins and core::credits") — and the count moved exactly as predicted.** `core/src/lib.rs` now carries `pub mod credits;` (`:45`) and `pub mod plugins;` (`:49`). Full workspace suite, **`Compiling gamehandler-core` and `Compiling gamehandler` both observed** after touching the two roots: **`398 passed; 0 failed`** — precisely the 361 + 37 that this row specified before the fix existed, which is the whole reason D-45 requires a number rather than a green tick. Had it stayed 361 the declarations would not have taken, and a "the modules are declared now" reading of the diff would have looked identical | **Lead ran** the suite and counted: 187 + 3 + 2 + 398, 0 failed, with both `Compiling` lines in the output. **The prediction was made in the finding and then matched, in that order** — the repair was not back-fitted to whatever number appeared — Lead reproduced all four numbers: `git show --stat e98b4b1`, the `mod` list at that commit and in the working tree, both `#[test]` counts, and the suite's `361 passed`. **The lead warned UX before the commit landed and it landed anyway** — the warning was queued for UX's next tool round and the commit preceded it, which is why the repair is a new commit rather than an amend (D-46 clause 2) |
| 62 | **A shipped constant's justification records the opposite of the truth about the reference.** `crates/app/src/view/runners.rs:616-623` justifies `RELEASES_TIMEOUT = 30s` with: *"Python passes no timeout and inherits `requests`' — which is none at all, so a stalled connection leaves its worker thread parked forever. […] 30s is this port's bound, not Python's"*. **All three claims are false.** The reference does not use `requests` — `grep -rn 'import requests' gamehandler/` returns nothing, and `runners.py:33` is `from urllib.request import Request, urlopen`. It **does** pass a timeout, explicitly: `fetch_available(self, limit: int = 15, **timeout: int = 30**, family=None)` (`runners.py:815-819`) with `with urlopen(req, timeout=timeout)` (`:830`). So a stalled connection fails after 30 s rather than parking a thread, and **30 s is the reference's bound, not this port's** — the conclusion is inverted, not merely decorated. **The constant's value is right; the recorded *reason* is the opposite of the truth**, which is what matters: that comment is the entire justification for the number, and it is what a future maintainer will trust instead of re-reading the Python. **Corroborated as a slip rather than a misunderstanding, by the adjacent constant:** `INSTALL_TIMEOUT` (`:690-696`) cites `install`'s default `timeout: int = 60` (`runners.py:864`) and `installRelease` passing none (`bridge.py:750` — verified: `self.proton_manager.install(release, progress_cb=…)`), and is **correct**. Two adjacent provenance records, one right and one inverted | **Advocate, reviewing `ace60fa`** — filed as blocking, on the rule that a comment recording a false provenance is worse than no comment. **Lead verified every clause against the reference:** no `import requests` anywhere in `gamehandler/`; `urllib.request` at `runners.py:33`; the `timeout: int = 30` default in the signature; `urlopen(req, timeout=timeout)` at `:830`; and the sibling `INSTALL_TIMEOUT` comment correct on all three of its claims | **Open — one comment rewrite, Architecture (`view/runners.rs`).** The constant **stays at 30 s**; only the *reason recorded for it* changes. The honest form names the reference as the source of the value and states what the port actually adds (a cancel-free task whose `Loading` would otherwise be the page's last word), rather than inventing a hazard the reference does not have. **Not a code change and not a behaviour change** — which is exactly why it is worth fixing: the only thing wrong is the record, and the record is what the next reader inherits | Lead reproduced all of it. **The false claims are not merely unverified — they are checkable in two commands**, which is why the row records the `grep` and the signature. |
| 63 | **Citations that do not land: four `bridge.py` line numbers quoted verbatim but pointing at the wrong lines, a stale test count in a module header, and a malformed rustdoc link.** All from `ace60fa`/`d255d36`, all cosmetic, grouped because the *class* is one thing — the record drifting from the tree while the quoted text stays correct, so the citation reads as verified when it is only quoted. Measured: the failure toast `f"Failed to install {release.tag}: {message}"` is cited at `bridge.py:758` and is at **`:767`** (`:758` is the closing `)` of the *success* notify); the two early returns are cited at `:743-746`/`:743-744` (`runners.rs:1595`) and are at **`:739-740`** (`if self._runner_busy:`) and **`:742-743`** (`if release is None:`); `installRelease` is cited as `bridge.py:742-764` (`runners.rs:798`) and spans **`:738-769`**; "Python's `limit=12`" is cited at `bridge.py:701` and is at **`:702`** (`:701` is `def work():`). Separately, `crates/app/src/http.rs:3` says the module keeps "its **358** tests" offline — it was 361 one commit later and is **398** now; the sentence's *claim* (core owns the trait and stays hermetic) is true and the number is archaeology. And `crates/core/src/runners/mod.rs:198` is a link definition missing its closing backtick | **Advocate, reviewing `ace60fa` and `d255d36`**; the lead spot-checked the first two citations and confirmed both | **Open — should-fix, Architecture.** Four line numbers, one test count, one backtick. **Why it is worth a row despite being cosmetic:** the citation *is* the handle — a reviewer who follows `bridge.py:758` finds a success notify and has to decide whether the code or the review is wrong, which costs the verification the citation was meant to save. This file already carries the rule from #56's review that **a check narrower than the claim it supports is the same defect as no check**; a coordinate that does not resolve is the same shape at a smaller scale. The **inert** part is recorded so nobody "fixes" a bug that is not there: `[`ArchiveError`]` resolves by *scope* (`ArchiveError` is in scope at `mod.rs:46`), so rustdoc never consults the malformed definition — the advocate confirmed no rustdoc error names it. (`-D rustdoc::broken_intra_doc_links` fails on **6** pre-existing `core` errors; none from these two commits.)  **FIXED (`da6935a`), and verified against the reference rather than taken on this row's word.** All four citations now resolve, re-measured in `gamehandler/bridge.py` here: `limit=12` is cited at `:702` (`runners.rs:707`, `:1510`), `installRelease` at `:738-769` (`:915`), the two early returns at `:739-740` and `:742-743` (`:919`, `:921`). The failure toast's own line is `:767` and the success text is cited at `:756-758` — which is exactly the `done` notify block (`:756-758`), so the old defect (the *failure* toast cited at the *success* notify) is gone rather than moved. The four wrong strings (`:758`, `:701`, `:742-764`, `:743-746`) appear **nowhere** in `crates/`. **The commit also corrected a fifth citation this row never listed** (`_progress_cb`, `:730-732` → `:733-735`), so the row found four of five — worth recording because it means the row's own sweep was narrower than the defect. `http.rs`'s stale count is deleted and the deletion documented in the sentence itself, and `runners/mod.rs`'s link has its backtick. **The row stayed `Open` while all six items were fixed on disk** — the record drifting from the tree, in the row whose subject is records drifting from the tree.| Lead verified `bridge.py:767` vs `:758`, `:739-740` and `:742-743`, the `http.rs:3` text, and the `mod.rs:198` line. **Bound stated:** neither party has run the full mutation batteries for these two commits — only confirmed each named mutation is CAUGHT by construction from its assertion. |
| 64 | **The tree-changed guard's first and only hypothesis is the wrong suspect, and it will fire on every concurrent edit.** `scripts/verify.sh:1619` prints: *"NOTE: the working tree changed during this run. verify.sh must be read-only with respect to tracked files — please investigate"*. **Measured on the first fully green end-to-end run** (all 11 stages, 09:06): the run itself was read-only, and the change was **another agent editing** — `crates/app/src/main.rs`, `state.rs`, `view/mod.rs`, `tests/pending_pages.rs` modified and `view/plugins.rs` added, i.e. UX mid-flight on the Plugins page. The guard is **right to fire** (the run's greenness is not attributable to one tree state, which matters for the same reason D-45 exists), and its **diagnosis sends the reader to the wrong place**: in a four-agent shared tree — the project's actual working mode, and the cause of #56 and D-49 — verify.sh is the *least* likely author of a mid-run change, yet it is the only one named | **Lead, on the first full run**; the guard fired and the diff identified a teammate's work in flight | **Open — one-line message fix, Packaging (`scripts/verify.sh`).** The note should lead with the likely cause ("another agent may be editing this shared tree") and keep verify.sh's read-only obligation as the *check*, not the *accusation*. Cheap, and it recurs: with four agents on one tree this will fire often, so a reader who follows it to verify.sh each time spends the check on a suspect that is almost never the author — the same waste as a citation that does not land (#63) | Lead read `:1619` and reproduced the note's text and its diff verbatim against `git status --short`, which shows the four modified files and the new `view/plugins.rs`. **The guard itself is sound and kept** — this is the message, not the mechanism, which is why the row is `should-fix` and not a defect in the gate. **FIXED (`afc3231`), verified by the lead rather than taken on report:** `git show afc3231 -- scripts/verify.sh` returns **only `printf` lines** — no control flow, no `diff`, no exit path — and the block at `:1617-1635` confirms it: the `if`, the `diff` and the `fi` are byte-identical. The note now leads with the likely cause, demotes verify.sh's read-only obligation to "the check, not the verdict", and states that a run over a moved tree is provisional (**D-45**). Packaging's own verification described the pre/post diff as byte-identical below the note, a failing stage plus a mid-run change still exiting 1, and a still tree printing no note at all — I re-checked only the mechanism claim, which is the load-bearing one, and it holds |
| 65 | **The Library page's primary call-to-action opens nothing — and `Library` has already left `PINNED_PENDING`.** `view/library.rs:309` renders `button::standard(ADD_FIRST_GAME).on_press(Message::OpenNewGameForm)`, and it sits in the **empty-state** branch — the exact screen a first-run user sees. `main.rs:937` is `Message::OpenNewGameForm => {}`. The form it would open does not exist either: **no `view/` module renders one**, and `state.game_form` is **never read by any view** (`git grep 'game_form' -- crates/app/src/view/` → nothing), so populating the field would still draw nothing. `SaveGameForm` is likewise `{}` (`main.rs:1056`). The user-visible result on a fresh install: *"No games yet"* and a button that does nothing when pressed, with every check green. **This is #55's shape exactly** — a page leaves `PINNED_PENDING` because it *renders a real body*, while a P-item under it is unmet — and this is the **second** instance, which makes the finding about the guard and not about either page. `PINNED_PENDING` was built to answer *"does this body say 'not ported'?"* and has twice been read as *"is this page done?"*. The two questions are different, and only the first is mechanised | **Lead**, by sweeping every `Message` variant against both its handler and its emission sites after #64. Method: parse the enum for its variant set; scan `Shell::update` (`main.rs:924-1403`) for arms whose body is exactly `{}`; then grep every production use of each variant, **excluding** `main.rs`'s test module (`:1436+`) and any line that is itself an arm pattern. **Result at `41a6612` (the revision this row quotes): 50 arms, 34 empty, 16 handled.** **These numbers are CORRECTED TWICE, and the second correction is the advocate's.** My first pass published 30/20 — wrong; the true figure at that revision is **37 empty / 13 handled**. I then corrected to 34/16 and **attributed 34 to `da6935a`, which is wrong**: measured across the three revisions, `da6935a` has **50 arms, 37 empty, 13 handled**; `41a6612` (T-26) has **50 arms, 34 empty, 16 handled**; and `045e08d` (T-11/T-12) has **44 arms, 28 empty, 16 handled** — fewer arms only because six empty ones were merged there, so the arm totals are not directly comparable and **the empty count is the only figure that means the same thing across all three**. **34 belongs to `41a6612` — which is the revision whose `:937`, `:1056` and `:1077-1081` this row quotes**, so the row was citing one commit's line numbers under another commit's name: **#63's class, inside the row that corrects a count.** The three-arm delta 37→34 is precisely `RefreshPlugins`, `InstallPlugin` and `PluginInstallFinished`, which T-26's wiring takes from `{}` to real bodies — so the correction is self-consistent, and the honest reading is that the number was right for a revision I had not named. The four I originally missed (`EasyInstallFinished`, `EasyInstallWizardFinished`, `FetchCoverForForm`, `StartEasyInstall`) are all **multi-line struct-variant arms**, invisible to a one-line regex that expects the whole arm on one line; the advocate's parser is comment-stripped and block-aware and agrees with `grep`. All four turned out to be emitted only from the pinned Installers page, so the correction does not change this row's conclusion — but the *count* was published to three agents before it was checked, which is why it is corrected here rather than quietly. **My first pass at that sweep was itself the defect class** — I matched emissions with `Message::<V>(` and so was blind to every **struct** variant, reporting `InstallRunner`/`FetchReleases` as unemitted. I caught it only because the result contradicted an earlier independent `git grep`; the corrected matcher uses `Message::<V>\b`. A check that cannot see the shape it is looking for is #1's class, and it was mine | ****FIXED — `2ce15e9` (`app: the game form is drawn, and #65's dead button opens it (T-10)`).** The four links above were all real and are all answered: `Message::OpenNewGameForm` now has a body at `main.rs:1096-1103` (it mints an id, builds `GameForm::new_template`, and clears `confirm_delete`); `view/form.rs` exists (1590 lines) and `view_body` renders the form; and `state.game_form` is read at `main.rs:992`, which matches on it and passes a `GameFormView` into `view::form::view` (`:993`). **The read is in `main.rs`, not in `view/` — and that is deliberate, not an oversight:** `main.rs:3276` states the property ("`view_body` does not read `game_form` at all") and a test pins it, so `view/form.rs` is a pure function of the struct handed to it. The original finding's grep (`git grep 'game_form' -- crates/app/src/view/` → nothing) is therefore **still true today and always will be** — it was a true observation that stopped being evidence of a defect once the data got a carrier. **A grep scoped to one directory answers "does *this* directory read it", never "is it read".** The guard — `crates/app/tests/dispatch_coverage.rs`, 935 lines, landed separately as `8177da0` — is what actually closes the finding, and it is not in this commit: it is Packaging's and is tracked as #64. **Verified by the guard rather than by the handler existing:** `dispatch_coverage`'s `KNOWN_DEAD` — whose one entry was this button — is now `[(&str, &str, &str); 0]`, and the run prints `0 dead emissions (0 deferred by KNOWN_DEAD)`. `KNOWN_DEAD`'s own doc predicted exactly this ("delete the entry, and thank you, this list is supposed to shrink"), and `uncovered()` asserts the list is not stale, so an empty list is a claim the guard checks rather than a silence. The row stays `Open` only for its **structural half, owner Packaging** — the mechanised sweep — which is what stops #55's shape recurring; that half is `crates/app/tests/dispatch_coverage.rs` and is tracked as #64, not here. **Original finding and its open half follow.** **Open — owner UX (T-09's files).** Acceptance: the Add/New-Game path either opens a form that can be filled and saved, or the button is **not drawn** — a control that silently does nothing is worse than an absent one. `SaveGameForm` must persist through `Library::add`/`update` per its TODO. **Structural half, owner Packaging:** the gate should not need a human to notice this. Recommended: a test in `crates/app/tests/` that, for each dispatched page, asserts every `Message` constructed in that page's view module has a non-empty handler in `Shell::update` — the matrix above, mechanised, which catches this and all 13 siblings without a per-page list | Lead reproduced all five links in the tree: the emission at `library.rs:309`, both empty arms at `:937`/`:1056`, the absent form view, and the unread `state.game_form`. **`Quit` is `{}` in `Shell::update` deliberately** — it is handled in `App::update` via `if matches!(&message, Message::Quit)`, the layer that owns the window — and is **not** part of that count. **The count of "how many empty arms are reachable" is RETRACTED and I am not replacing it with another derived figure.** My parser had **three** blind spots, all the same shape as the defect this row is about, and each was found only because a result contradicted something else I had: (1) a one-line regex missed multi-line arms, under-counting empties 30 vs 34; (2) it never excluded the `#[cfg(test)]` modules **inside** `view/*.rs`, so `Message::LaunchWatchTick` at `view/runners.rs:1564` counted as a production use when it is a test's; (3) it read the **opening line** of a multi-line arm as a use (that line carries no `=>`), which is how `FetchCoverForForm` — actually an arm at `main.rs:1077-1081`, not an emission — was reported as reachable. **Three passes, three holes, in the row about holes.** What survives is what I verified by **reading the lines and quoting them**, not by counting: `library.rs:309` emits `OpenNewGameForm` in the empty-state branch, `main.rs:937` is `{}`, no `view/` module renders a form, and `state.game_form` is read by no view. That claim is checked, and it is the finding. **The lesson is the one this file keeps recording: a derived number is a claim, and a claim is only as good as the parser behind it** — so the mechanised version of this sweep is Packaging's `tests/dispatch_coverage.rs` (T-65's structural half), where it is a real test with a real parser, and not an ad-hoc script living in `/tmp`. Packaging has already reported two false positives of the same family (test-only constructions), which is the guard telling us the hard part is the parser, exactly as predicted |
| 66 | **21 of the 78 parity items are named by no task row — and for the launch group there is no task to name, because the app-side launch flow does not exist.** Mechanical audit: every `P-nn` in the matrix matched against every `P-` range and id appearing in any `T-` row. **57 of 78 are claimed; these 21 are not:** **P-40…P-50** (eleven launch items: umu-run/`PROTONPATH`, the Proton-only gates, the esync/fsync/DXVK/VKD3D env mapping, MangoHud/GameMode/Gamescope, anti-cheat runtimes, bundled DXVK, the ~6 s grace, `mark_played`, `winecfg`/`winetricks`, the prefix folder, native launch) and **P-69…P-78** (toasts, CLI, desktop shortcuts, GVFS, corrupt-JSON tolerance, timestamp normalisation, unknown keys, category order, duplicate ids, Flatpak paths). **The split matters and I got it wrong on my first pass**, so it is recorded as three categories rather than one number: **(a) implemented but untraceable — P-73…P-77.** `unknown_keys_are_dropped`, `invalid_timestamps_regenerate_added_and_zero_last_played`, `a_boolean_is_never_a_timestamp`, the `Uncategorized`-last ordering and the duplicate-id rule are all present and tested in `core::json` (847 lines, 17 tests) and `core::models` (1239 lines, 28 tests), so **T-02 covers them in substance and the matrix simply does not say so.** These are a *traceability* defect, not missing work — the distinction I nearly erased by reporting one count. **(b) landed under another task — P-70.** The CLI exists and is gated (`finding #31`, T-07). **(c) genuinely unwired and unowned — P-40…P-50**, which is the real finding. **(d)** the remainder of P-69…P-78 (notably P-71 shortcuts and P-72 GVFS) needs the same triage and I have not done it, so this row does not claim them either way | **Lead**, by auditing the matrix against the task scopes during a critical-path review. **It is the D-50 class one level up:** a dependency no gate can see. `PENDING_PAGES` cannot reach it (those pages are dispatched), the test suite cannot reach it (nothing tests a P-item), and `verify.sh` cannot reach it — **so nothing in the repository would have gone red while 21 parity items, several of them the app's entire reason for existing, went unowned.** It also directly threatens the project's definition of done, which requires *every* parity item ticked and verified in the running Flatpak: a list that cannot reach 21 of its own entries cannot be completed | **Open — split into a new task (`T-29`, app-side launch flow, owner Architecture; `main.rs` is theirs under D-51) plus a triage pass.** `T-29` carries P-40…P-50's app-side half; the core half is **already written** — `core::runners::launch` exists and is declared at `runners/mod.rs:33`, with the env mapping, the NVAPI/FSR/Wayland gate and the DXVK install in place. What is missing is every caller: `git grep -n 'runners::launch' -- crates/app/src/` returns **two comments and no code**, and `main.rs:160` says in as many words *"the launch itself is not ported yet"*. **The TODO tags in the tree are actively misleading about this** and must be fixed with it: `main.rs:1057-1066` assigns `mark_played`, the grace watch, `runners::run_tool`, `xdg-open` and `create_desktop_shortcut` to **`TODO(T-10)`** — but `T-10` is scoped *"app: Game form \| P-18…P-31. 15 toggles, 6 sections."* An agent following the tree's own tags would do the launch work under the name of a task that does not contain it, and the matrix would still show P-40…P-50 unowned: the tag and the plan would disagree, and neither would be checked | Lead reproduced every count: the 57/78 split, the exact 21, `runners/mod.rs:33`, the empty `git grep` for callers, the `main.rs:160` sentence, the `T-10` scope string, and the five core tests that implement P-73…P-77. **Two of my own errors are in this row rather than quietly fixed:** the first count was reported as a single "21 unowned" figure before I checked substance, and the range-matching regex that produced it was the *only* thing standing between this finding and a false positive — a `P-40…P-50` written with a different dash or spacing would have read as covered |
| 67 | **`PLAN.md`'s task table — the project's own source of truth — has three malformed rows, and no gate reads it.** **T-11** has been missing its **closing pipe since `7c23e04`** (*"PLAN: T-11/T-12 need the fetch handlers first"*), so its row has 5 cells where every sibling has 6; and **T-07** and **T-09** each carry **two unescaped pipes inside code spans** — a regex alternation `` `--list\|--launch\|--version` `` and a Rust closure `` `\|category\|` `` — which split each of those rows into 8 cells in GFM, because a pipe inside a code span **still delimits a table cell** unless it is written `\|`. Found while adding T-29: the check I used to confirm the new row was well-formed reported 25 rows "malformed", which was *my checker's* expected count being wrong, not the table's — the real signal was the two rows that disagreed with their own siblings. **The reason this matters more here than the severity suggests:** both defects are **silent to a reader** (the rendered table hides them) and **invisible to every check** (nothing parses `PLAN.md`), while this project repeatedly treats that file as the record that resolves disputes — and it is now being edited by four agents. A truncated T-11 row is also a truncated *scope*: the row's own text notes it was re-pointed after `253498d` and that the wiring diff is Architecture's to hand over, so the part a parser loses is the part that says what is still owed | **Lead**, auditing the table while landing `T-29` | **FIXED (`41077df`)** — T-11's pipe restored; both code spans escaped; verified mechanically, `re.split(r'(?<!\\)\|')` over all 28 rows gives exactly 6 cells each. **No gate was added**, and that is the honest bound: the check lives in `/tmp` and would have been discarded with the session. If this class recurs a fourth time (the lead has now broken this same table three times on its own separators) it should become a real test in `tests/`, since `PLAN.md` is where ownership and scope are recorded | Lead reproduced all three: the `-L`/`--stat` cell counts, the closing-pipe state at `HEAD` and at `7c23e04`, and the escaped forms after the fix. **The defect is pre-existing, not mine** — and the accompanying check *was* mine, and was wrong, which is why the row states the count that mattered rather than the count that alarmed me |
| 68 | **T-09 is marked LANDED while `P-02` is half met and `P-10`…`P-16` are unmet — and #66's audit is blind to exactly this shape.** **`P-02`/`P-16` (last-played):** `LibraryPage.qml:269` renders `subtitle + " · " + lastPlayed` and `bridge.py:311` supplies it, but the port's `list_body` (`view/library.rs:340`) passes only `resolved_runner_label`, and **`format_last_played` / `format_last_played_now` have no app caller at all** — `git grep 'last_played' -- crates/app/src/` returns **empty** while both functions sit tested in `core::models:458/507`. **This one is unblocked** — the core work is done and the wiring is a string — and nothing owns it. **`P-10`…`P-16`:** double-click play, the nine-item context menu, Play/Edit/Find-cover, Winecfg/Winetricks/Open-prefix with the Linux-disabled state, create-shortcut, and remove-with-confirm **do not exist**: `view/widgets.rs` constructs **zero** `Message`s, and `card`/`row` take only `(game, label)`. Most are blocked behind T-10 (the form) and T-29 (launch) — but the *widgets* are themselves parity items and the reference draws them, so "blocked" describes the handler, not the card. **Why #66 could not see any of this:** #66 audits for P-items named by **no** task. T-09 **names** `P-01`…`P-17` and is marked LANDED, so every one of those ids reads as covered. **That is #55 and #65 a third time, and the mechanism is now clear enough to state: every guard in this repository asks "does this page render a real body?" — none asks "are this page's P-items met?"** `PENDING_PAGES` is keyed on the string, `dispatch_coverage` on the handler, the label tests on the text. Parity is the one axis with no check | **Advocate**, as item 5 of the Phase-3 review | **Open.** Two parts: (a) the last-played wiring is small, unblocked and should ride with T-10 or T-29; (b) **the structural half is the finding** — a parity-coverage check, or a rule that a task cannot be marked LANDED while an id in its own scope is unmet, with the remainder written into the task row the way T-09's own row already does for `#34`/`#33`/`#35`. **Recommendation: the rule, not the check** — a per-page P-matrix would be a second hand-copied list, and this file has now recorded three separate instances of a hand-copied fact nothing reads (#43, `PINNED_PENDING`'s own header, T-09's row). Owner: UX for (a), Lead for (b) | Lead reproduced every claim: `git grep 'last_played' -- crates/app/src/` → **empty**; `core::models:458/507` define both functions with tests; `view/widgets.rs` constructs **zero** `Message`s; `list_body` at `view/library.rs:340` passes only `resolved_runner_label`. The advocate read `LibraryPage.qml:269` and `bridge.py:311` against the port — the reference-oracle comparison no test in this repo makes |
| 69 | **`view/meta.rs:52` carries an inverted justification for a guard that is correct — and it is the #62 shape (citations that don't land) in a second file.** The paragraph reads: *"the Rust view has no runner manager in its contract yet (T-07) and passes `\"\"`"*. **False at HEAD**: `view/widgets.rs:417-425` explicitly records that empty-string path as **#30, fixed**, and the view has taken a `&RunnerManager` since T-08. The guard the paragraph justifies is right and should stay — what is wrong is the reason given for it, which now describes a defect that no longer exists and a contract the module no longer has. **A reader who trusts the comment would conclude the empty-label case is unreachable** and could delete the very guard that handles it | **Advocate**, item 5 | **Open — small, and it should ride with T-10 or T-29 rather than get its own task.** Fix: state the real reason (a manager exists now; the empty label is still constructible and #30 is why the guard is needed), the way #62's repair did — **corrected in place with the history recorded, not silently deleted**. Owner: UX (`view/` is theirs) | Lead re-read `meta.rs:48-56` and `widgets.rs:417-425` and confirmed both halves: the comment says the view *"has no runner manager in its contract yet (T-07) and passes `\"\"`"*, while `widgets.rs` records that empty-string path as **#30, fixed**, and the view has taken a `&RunnerManager` since T-08 |
| 70 | **The Credits page's 27 links are dead, the page emits nothing at all, and no task owns it.** `view/credits.rs` (827 lines, `f5c8587`) contains exactly **four** `Message::` occurrences and **all four are inside doc comments** — it constructs no message in production code. The reason is structural: **`Message::OpenUrl` does not exist anywhere in `crates/`** (`git grep OpenUrl -- crates/app/src/main.rs` → nothing), so `button::link` has no message to carry and the 26 "Visit" buttons plus the GitHub footer render **disabled**. The reference's Credits page uses `Kirigami.UrlButton`, which opens URLs — so on this page the port is strictly less capable than what it replaces, on the page whose entire content is links. **UX disclosed this honestly in-file** (`the_links_are_still_unwired` asserts both halves, and the module doc says why), which is the right way to land a known gap — but a disclosure inside the source is not an owner, and `PLAN.md` names no task for it | **Advocate**, item 5, from a red run of `dispatch_coverage` at `8035995` | **Open — needs a task, not a finding.** `Message::OpenUrl` plus a link-opener in the shell is small, but it is a **new `Message` variant**, so it also touches `every_message!` and the two length pins (#38/#39) — which is exactly why it should be a named task rather than a drive-by. Owner: UX (the page is theirs), after T-12's Installers wiring | Lead confirmed: `view/credits.rs` has exactly **four** `Message::` occurrences and all four are doc comments (`:48`, `:117`, `:205`, `:654`); `git grep 'OpenUrl' -- crates/app/src/main.rs` returns nothing. **And the red run is itself worth recording** — the guard asserted "dispatched to a real body and draws controls ⇒ must emit a Message"; `view/credits.rs` **falsifies the premise**, because a page whose only affordances are links emits nothing in a port with no `OpenUrl`. **The assertion was the defect class it was written to catch** — a check that cannot pass for a legitimate page, the inverted variant (§1). It was removed before landing, replaced by a global floor (reasoning in the working tree at `:595-608`) |
| 71 | **Two tasks have no row in `PLAN.md` — one of them already landed with a real commit — and the table has now been called "the project's own source of truth" in #67.** `T-26` and `T-28` appear **nowhere** in `PLAN.md`: not as rows, not as mentions. `git log -S'\| T-26 \|' -- docs/migration/PLAN.md` and the `T-28` equivalent both return **nothing**, so they never did. `T-26` is landed work (`41a6612`, *"T-26: the Plugins page, and `plugins::` gets a real caller"*, covering P-64/P-65); `T-28` is outstanding (*verify the closure claim independently*). **Why #66's verdict is not thereby wrong, and why that is luck:** #66 matched every `P-nn` against the ids appearing in any `T-` row, and P-64/P-65 **are** named — in **T-06's** row, which carries the reassignment note written *before* the split into T-26. So the ids stayed traceable by the order two rows happened to be written in, not because the plan enumerated its tasks. Had the split happened the other way round, #66 would have reported P-64/P-65 as unowned work while a landed commit sat in the log. **The class is D-50 turned on the plan itself:** D-50 says a dependency stated only in a commit message is invisible to every gate; this is a **task** stated only in a commit message and the agent task list, invisible to the plan-of-record. **And it is the second failure mode of this one table, unchecked:** #67 was rows **malformed**; this is rows **absent**. Nothing reads `PLAN.md` at all, so neither is visible without a human comparing it against the commit log | **Lead**, while tracing the critical path to T-12 and reconciling the plan against the task list. Found by enumerating `T-01`…`T-33` and diffing against the ids the table actually carries — `T-26` and `T-28` were the only two missing, and a `git log -S` per id established that neither had ever been a row | **FIXED for the two ids (same commit)** — rows added for `T-26` (marked done, with `41a6612` and the `#61` declaration repair recorded) and `T-28` (open, runs under T-19). `T-01`…`T-33` now all carry rows, all four columns. **The mechanism is NOT fixed, and should be:** a check that every `T-nn` in a commit subject has a row in `PLAN.md` is mechanisable and small, and it is the only thing that would have caught this without a human diffing two lists. Recommended owner: Packaging, alongside `dispatch_coverage.rs`, which is the same "read the tree, assert a property of it" shape | Lead verified both halves: `grep -n 'T-26\|T-28' docs/migration/PLAN.md` returns **nothing**; `git log --oneline -S'\| T-26 \|' -- docs/migration/PLAN.md` returns **nothing**; and `git log --oneline --all \| grep -iE 'T-26\|T-28'` returns `41a6612` for T-26, confirming the task is real landed work rather than a phantom of the task list. After the fix, `T-01`…`T-33` enumerate with no gaps at 6 cells each |
| 72 | **D-49's own proposed checks cannot detect its worst hole, and the check that can was not among them.** `#53` left (B) `git show --stat <sha> -- <path>` and (C) `git diff-tree -r` + numstat vs `git show --stat` as the round-trip to close **Hole 2** (the pathspec form commits the *working tree*, so an edit made after staging is swept in and neither the path nor the file count changes). Measured in a scratch repo, both are blind to it: **(B)** prints `a.txt \| 3 ++-` / `1 file changed`, byte-identical to the form without the pathspec, because `--stat` is enumeration plus line counts and Hole 2 moves neither. **(C)** is vacuous as stated — `git diff-tree -r --numstat` and `git show --numstat --format=` print the same bytes on the incident, so comparing them cannot fail; the one divergence they can produce is incidental (bare `diff-tree -r` defaults rename detection **off** where `git show --stat` defaults it **on**, so a pure rename reads as 1 line vs `D`+`A`), which is a real difference and not Hole 2 — a check built on it would be green on the incident and red on an unrelated rename. **The check that works is a content comparison**, `git show <sha>:<path> \| cmp - <intended>`, which **fails** on the Hole-2 commit; enumeration has an identical file set (`--name-only` prints exactly what was intended), so no listing-based check can reach it. **Hole 3 held as written** (sha `23f116c` still resolved after HEAD advanced to `f8b1587`). **The class, third time in this file:** (B) and (C) are checks narrower than the claim they support, inside the entry whose subject is the defect class | **Lead**, on the `#53` round-trip — the exact task #53 asked for when it said 'round-trip the commit-contents check'. Scratch repo at `/tmp/d49rt`, not a test: the measurements are the artefact and they are reproduced in D-49 | **FIXED for the artefact (same commit)** — D-49's procedure gains a new step: keep the enumeration, and **add a content comparison for every path whose content the commit claims**, `git show <sha>:<path> \| cmp` against the intended bytes. Every other check in that entry is a listing or a count, and Hole 2 is a content defect. **The advocate needs no change** — their review routine already pins content with `git show <sha>:<path> \| cmp`, which is the half that closes Hole 2; D-49 now says which half closes which hole. **Still open:** (B) and (C) should be struck from the `#53` **task description** — where they actually live, and where a reader meets them — rather than left as checks someone might trust. **I first wrote "struck from the D-49 text" and that was false**: `grep -n diff-tree docs/migration/DECISIONS.md` returns only my own amendment at `:2510`/`:2527`/`:2531`. D-49's numbered procedure (steps 1–5) never contained (B) or (C), so there was nothing there to strike — the citation did not land, in the row about a citation that did not land, which is the same class a fourth time and is recorded rather than quietly repaired | Lead ran the round-trip in a scratch repo and reproduced every step: the worktree-wins commit (`git show HEAD:a.txt` = `index-v1\nworktree-v2\n` against an index holding `index-v1\n`); (B)'s identical output with and without `-- <path>`; (C)'s byte-identical numstat; the rename divergence (1 stat line vs `D`+`A`, 1 vs 2); the content check **failing** on the bad commit; `--name-only` listing exactly the intended path; and the sha surviving a HEAD move. **Both remaining vacuous checks are named, not deleted**, so the correction is auditable against what it corrects |
| 73 | **The lead scoped `T-04` by its smallest sufficient part, told Architecture that was "the whole ask", and then traced the critical path as if it were all of it — and the task's own row said otherwise.** The brief: *"Scope is `installers()` plus `search_installers` (`installers.py:241-255`) — a catalog and a filter, not a subprocess. That is the whole ask."* Measured: `gamehandler/installers.py` is **702 lines**; the catalog half is ~220 of them. PLAN.md's `T-04` row — written **before** the brief — says *"Wizard/poll state machine with injectable clock"*, which is `wait_for_installer` (`:360-410`) and `wait_for_prefix_idle` (`:312-345`) plus the command, download and game halves. **Architecture built what the brief asked, correctly, and refused to let it read as the whole task** — commit subject `T-04 (catalog half)` with a *** This is not T-04 *** paragraph, which is what kept the gap visible. **The critical-path trace was short by one step for the same reason:** the lead's `T-04 → T-12 → PENDING_PAGES empty → T-19 → T-20` assumed T-04 was the small half, so the real chain is `T-34 → T-12 → …`. **This is D-52 turned on its own author** — the rule the lead wrote this session (a task may not be LANDED while an id in its own scope is unmet) would have caught it, applied to the plan the lead was writing rather than to a teammate's row. **And it is the #66/#55/#65 mechanism again with the polarity flipped:** those were a scope read as met when it was not; this is a scope read as *small* when the row said otherwise | **Architecture**, who checked the brief against the task's own row rather than executing the brief | **FIXED (same commit)** — `T-04`'s row corrected in place with the error and its measurement recorded, `T-34` created for the remainder, the critical-path chain restated as one step longer. `T-01`…`T-34` all carry rows. **The mechanism is not fixed:** a brief from the lead that contradicts a task's own row has nothing comparing them, and the only thing that caught it was the agent reading the row. Recommended: when a brief restates a task's scope, quote the row's scope verbatim in the brief — the cheap version of the check is that the two texts are visible to each other | Lead reproduced every number: `wc -l gamehandler/installers.py` = **702**; the catalogue functions at `:230-256`; `wait_for_installer` at `:360` and `wait_for_prefix_idle` at `:312`; and PLAN.md's `T-04` row reading *"Wizard/poll state machine with injectable clock"*. Architecture's commit `60ee689` contains `crates/core/src/installers.rs` and `crates/core/src/lib.rs` **only** — audited with `git show --name-only`, so the narrow scope is also a clean commit |
| 74 | **`view/installers.rs`'s category dropdown would have offered only "All" — a filter that looks implemented and cannot filter.** `installer_categories()` (`:103-105`) returns `["All"]`, with a comment recording that the catalog's categories were missing and belonged to T-04. They now exist (`ALL_CATEGORIES` / `INSTALLER_CATEGORIES` in `core::installers`, landed `60ee689`), so the function is a stale placeholder over landed data — and wiring T-12 without the one-line change draws a working-looking dropdown whose only option is the unfiltered one. **The trap is that nothing looks wrong:** `InstallerRow::category` still renders badges correctly, so a card can read "Launchers" while the dropdown cannot select it. **Same class as the defect the page's own module doc warns about**, and the same class as #65: a control that renders and does nothing. **Caught before wiring, which is the point** — Architecture named it while reporting, not after a user found it | **Architecture**, while assessing what T-12's wiring would need | **Open — one line, and it must land with or before T-12's wiring.** `installer_categories()` draws from `core::installers::INSTALLER_CATEGORIES`. Owner: **UX** (`view/` is theirs under D-51), handed over as hunks by Architecture rather than edited across the boundary — the pattern D-50 exists to make routine. **Acceptance:** the dropdown lists every category the catalog can produce, and a test pins that the set is derived from `INSTALLER_CATEGORIES` rather than re-listed  **LANDED at `28e6150` (UX), and the fix was verified by mutation rather than by the suite going green.** The function is now `["All", *INSTALLER_CATEGORIES]` **derived** from the constant (`view/installers.rs:133`), so it cannot drift from the catalog again, and the old `the_category_list_leads_with_the_all_sentinel` — which asserted only `categories[0]`, and so was green before *and* after — is replaced by a whole-list comparison plus `every_category_the_catalog_can_produce_is_one_the_filter_offers`, which walks the real nine recipes. **Mutations re-run by the lead, not taken on report: restoring `["All"]` fails exactly 3 tests** — the whole-list test, the catalog cross-check, and `the_selected_filter_is_the_position_of_the_current_value` — matching UX's own table, so the assertion is not agreeing with itself. **The one survivor is recorded rather than papered over:** replacing the call site's `selected_category(...)` with `Some(0)` in `view` leaves the suite green, because a `Dropdown`'s selected index is not one of `Operation`'s arms — the same wall `install_press`'s call site hits. It is written into `selected_category`'s own doc, so the limit is stated where the next reader meets it, which is the disposition this file asks for and not a gap. Also discharged here: the stale `:218` doc comment (*"or empty until T-04 lands"*) | Lead confirmed the state: `installer_categories()` at `view/installers.rs:103-105` returns `["All"]`, `ALL_CATEGORIES`/`INSTALLER_CATEGORIES` exist in `core::installers` at `60ee689`, and the page's module doc already records the missing-catalog reason the function was written under. **The placeholder was honest when written and is stale now** — which is the ordinary way a placeholder becomes a defect, and why the comment naming its blocker is what made it findable |
| 75 | **`cargo doc` is in no gate, and 8 intra-doc links do not resolve — a citation that stopped landing, one level down from #69.** Measured: `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" cargo doc -p gamehandler --no-deps` errors on **4** links in `crates/app` (confirmed by the lead: `view/installers.rs:156`, `view/installers.rs:218`, `view/runners.rs:647`, `view/runners.rs:682`), and `gamehandler-core` has **4** of its own (`installers.rs:57`, `paths.rs:16`, `runners/mod.rs:934`, `runners/proton.rs:109`). All the same shape: a doc comment writes `` [`name`] `` where `name` is a `#[cfg(test)]` item — which rustdoc does not document — or names a symbol without its path. **The bracket form degrades silently to an unlinked code span**, so the rendered page reads correctly and only the author's intent is lost, which is why it survived review. **The mechanism is #67's**: nothing in `scripts/` or `bin/` runs `cargo doc` at all (`grep -rn 'cargo doc' scripts/ bin/` → empty), so a link that stops resolving — as these did when their targets moved — is never noticed. UX measured **7 → 4** across `db1ecbf` in worktrees at both shas. One of the four is not the `cfg(test)` shape: `view/installers.rs:218`'s `search_installers` **does** exist (`core::installers::search_installers`), so the fix is a path — and the same file already writes it correctly as a code span at `:20` | **UX**, while repairing #69's citations — and it is the #69 class found one level down *by doing* #69, which is the pattern this file keeps recording | **Open — a tidy, not an emergency, but it needs a gate or it recurs.** Two parts: (a) fix the 8 links (`view/installers.rs` and `view/runners.rs` are UX's; `core::installers`, `core::paths`, `core::runners` are Architecture's); (b) **add `cargo doc` to `verify.sh` with `-D rustdoc::broken_intra_doc_links`**, which is the part that stops the drift — the same reasoning as #67, where three malformed rows in the project's source of truth persisted because no gate read the table. Severity is low *today* (the docs read correctly); the cost is that the next citation to stop landing is also invisible. Owner: Packaging for the gate, UX and Architecture for their own files **CLOSED — all ten fixed and the gate is committed at `0289ffe`, entering history green.** The repair was per-file by the owner under D-51: Architecture's six at `5506963` (`installers.rs`, `paths.rs` ×2, `runners/mod.rs` ×2, `runners/proton.rs`) and UX's four across T-12 and #30 (`view/installers.rs` ×2, `view/runners.rs` ×2 — the last two written as code spans, since a `#[cfg(test)]` item is undocumentable by construction and the bracket form cannot be made to resolve). **Re-measured by the lead, not taken on report:** `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" cargo doc --workspace --no-deps --offline` exits **0** with **0** errors and **18** warnings, and the stage is wired into `verify.sh` between `clippy` and `test` with a matching banner (the sequence check passes). **The gate was proved to fail rather than assumed:** injecting `[`this_link_goes_nowhere_xyz`]` into `view/meta.rs` gives exit **101** naming the link, and the file was restored — so it is a real failing stage, which is the property #75's own row could not claim for the count it stated. **The count went 4 → 10 → 5 → 2 → 0 across five measurements in one day**, which is the argument for the gate rather than for a better list: a doc link is a fact that a re-measure is the only way to know, and this row's own numbers were wrong twice| Lead reproduced the count independently: `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" cargo doc -p gamehandler --no-deps` errors on exactly the four listed paths and fails with `could not document gamehandler`; `grep -rn 'cargo doc' scripts/ bin/` returns **nothing**. UX's isolation is the right method and worth naming: the 276-test figure below is attributable to a commit only because it was measured in a `git worktree` at that sha — the shared tree reports **473** core tests, not 423, because other agents' in-flight additions are in it, which is D-45 exactly. **THE COUNT IS 10, NOT 8, AND THE TWO THE ROW MISSES ARE MISSED BY THE SAME MECHANISM THE ROW IS ABOUT.** Re-measured after T-34 landed, at `2fead15`: `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" cargo doc --workspace --no-deps --offline` exits **101** with **10** fatal errors — the 8 `unresolved link` ones this row counted, plus **2 that say `X is both a module and a macro`** (`paths.rs:12` on `[`std::env`]`, `runners/mod.rs:19` on `[`env`]`, the latter colliding with the `env!` macro). These are a *different rustdoc diagnostic* with the same consequence, and **an instrument that greps `unresolved link` cannot see them by construction** — which is this file's recurring defect, now inside the finding about it, for the sixth time. The line number drifted with the file it lives in: this row cites `installers.rs:57`, and after T-34's +3800 lines the same link is at `installers.rs:858` — the drift is honest, the citation is not wrong, and it is the reason T-34's row exists. **One more, found by reading the site rather than the diagnostic:** `view/installers.rs:218`'s doc comment reads *"From [`search_installers`]' port, or empty until T-04 lands"* — T-04 **and its remainder T-34** have both landed (`5e1485b`), so the second clause is now false. That is #69's shape exactly, sitting in the same comment as one of the broken links, and it is the kind of thing no gate catches at all: a *prose* claim that expired. Fixed by UX rather than by me, since the file is theirs and they are in it for T-12. Also measured and **not** part of the 10: **14 `links to private item` warnings** (`covers`, `installers`, `json`, `parse_lenient`, `launch_opts`, `normalize_desktop_size`, `proton`, `hash::sha256_hex`, `MAX_VALUE_DEPTH`, `clamp_depth`, `is_numeric_digit`, `env_tokens`, `write_metadata`, `rename_noreplace`, `proton_entry_exists`, `is_staged_runner`, `python_str`). These are warnings, not errors, they do not fail the build, and **the new gate must decide about them explicitly rather than silently** — `-D rustdoc::broken_intra_doc_links` denies one lint by name, so a private-item link stays a warning and the gate stays green. That is a deliberate choice to record, not an oversight: denying every rustdoc warning would fail a doc comment for naming a private helper, which is sometimes the correct thing to do |
| 76 | **The lead marked #65 FIXED with three wrong facts in the marking, one of them the exact claim the row was about — written into the row about the defect class.** Drafting #65's fix-up, I asserted: (a) `8177da0` is "the form view and the two handlers"; (b) `2ce15e9` is what verified it; (c) "`state.game_form` is read". **All three are false.** `8177da0` is **one file** — `crates/app/tests/dispatch_coverage.rs`, 935 lines, the guard, no view and no handler; the form view is `2ce15e9` (`view/form.rs`, 1590 lines, `main.rs` +610). And `state.game_form` **is** read — at `main.rs:992`, which matches on it and hands a `GameFormView` into `view::form::view` at `:993`; my grep was `grep -rn 'game_form' crates/app/src/view/`, **scoped to one directory**, and I read its silence as "nothing reads it". **That is verbatim the original #65 row's own evidence** (`git grep 'game_form' -- crates/app/src/view/` → nothing) — so I inherited a true observation and promoted it to a live claim, in the row that exists to record that promotion. The unexamined premise is that a container is where its data is read; the port's `view/form.rs` is a **pure function of the struct handed to it**, `main.rs` is the carrier, and `main.rs:3276` states that property and a test pins it | **Lead**, by re-running each citation before committing the row that carried it — the check that has caught the last several, and it caught this one at the file-edit stage rather than the commit stage | **Row 65 rewritten** (uncommitted at the time of writing, so no false claim reached a commit): `8177da0` and `2ce15e9` now carry their real contents, the read is located at `main.rs:992`, and the original grep is recorded as **still true and no longer evidence** — which is the durable half, since a future reader running it will again get nothing. The guard is named as what actually closes the finding, and as Packaging's #64 rather than as part of #65 | Re-ran across **all** of `crates/app/src/`, not one directory: declaration `state.rs:752`, initialiser `:851`, **the read** `main.rs:992`, the three writes `as_mut()` at `:1293`/`:1302`/`:1310`, the property statement `:3276`, and the assert `:3632`. `git show --stat` on both shas to get their real contents |
| 77 | **The lead nearly committed the `T-35` doc gate as green off a grep that could not match anything — one command away, and only the exit code caught it.** Verifying that UX's five app-crate links were fixed, I ran `cargo doc ... 2>&1 \| grep -E '^error.*-->'` and got **no output**, which I read as no errors. It is structural: rustdoc prints `error: unresolved link to ...` on one line and its ` --> crates/...:156:7` location on the **next**, so `^error.*-->` can never match a rustdoc error in any state — it is a pattern whose silence is guaranteed, and silence was exactly the signal I wanted. Re-running and reading **`$?`** gave **exit 101** with **5** live errors. **This is the `#75` finding one level in:** `#75` counted 8 where there were 10 because it grepped one diagnostic string; I then wrote a check that greps **zero** of them and believed it. The difference is that `#75` undercounted and mine would have published a **false pass** on a gate whose entire purpose is to catch this class | **Lead**, caught by the exit code rather than by reviewing the grep — the grep looked right, and what exposed it was running a second, independent instrument on the same question | **Not a defect in the tree — nothing was committed off it.** The gate stayed uncommitted, which is what it was already doing, so the near-miss cost nothing. The lasting action is the habit: **a pipeline whose output you are about to read for absence must be joined to a status that cannot be silent**, and every `verify.sh` stage is written that way (return codes, never output-matching) while my ad-hoc checks were not. Recorded rather than quietly fixed because it is the seventh instance of the class in this file and the first where the *check for the class* was itself the failure | Re-ran with `grep -A1 '^error' \| grep -E '^error\|-->' \| paste - -` (which pairs the lines instead of trying to match across them) and got the 5 paths, agreeing with the count in the exit-101 run. Both instruments now agree, and neither alone would have been enough: the grep gives the paths, the exit code gives the verdict |
| 78 | **`35e8085` is red at its own sha — a commit whose message states the outcome of a run it did not make, and whose entire argument is that the guard's reject branch fired on real work rather than on a fabricated entry.** The commit (*`dispatch_coverage: empty KNOWN_DEAD, and record that the reject branch fired (#65)`*) deletes `KNOWN_DEAD`'s only entry and explains why: *"UX landing that handler is what emptied the list, and the guard said so on its own: the suite failed naming the entry as no longer describing a dead emission."* **Neither half is true at that revision.** `Message::OpenNewGameForm`'s arm is still `{}` at `main.rs:1046` — the handler landed at **`2ce15e9`, four commits later** (`2ce15e9~1` still has `=> {}`; `2ce15e9` has none) — so the emission is genuinely dead and the guard **fails**, rather than reporting a stale entry. I reproduced it by extracting the sha rather than trusting the tree: `git archive 35e8085 \| tar -x -C /tmp/rc35`, then `cargo test -p gamehandler --test dispatch_coverage` gives **FAILED. 2 passed; 1 failed**, panicking at `dispatch_coverage.rs:752` with *1 message(s) a dispatched page emits have an empty `Shell::update` arm* naming `page Library, view/library.rs: Message::OpenNewGameForm`, while the summary line prints **`1 dead emissions (0 deferred by KNOWN_DEAD)`**. **The mechanism is worth stating because the commit's own disclosure predicted it and was read the wrong way:** `uncovered`'s doc comment does say the deferral's `page` component is compared against the page's **module** (`"library"`) — but the earlier version passed `"Library"`, so `v == variant && p == page` never matched and the deferral was silently inert. An inert deferral is precisely the state that comment names — *"a hand-copied fact that no assertion reads has nothing to keep it true"* — occurring in the list, one commit after the sentence was written | **Advocate**, whose Phase-3 review led with it; **independently reproduced by the lead** from the sha, in an isolated `CARGO_TARGET_DIR` | **The redness is history, not tree state: HEAD is green** (`3 passed`), because `2ce15e9` handled the arm and is itself the repair. So nothing needs reverting and no file is wrong on disk — what is wrong is the **claim**, in a message that will outlive the state which falsified it. The lasting action is the one this row exists for: **a commit message reporting a run is a claim about a sha, and only the sha can confirm it** — D-45 already says a green local run is evidence about the working tree; this is that rule applied to the *message*. Four commits were red (`35e8085`, `32f5601`, `ce1cd7a`, `e27c4ea`), and nobody noticed because, as §2's own guard-gap paragraph records, no `verify.sh` stage builds a commit | Extracted the sha, built it with its own target dir, and read the failing assertion and summary line out of its output; then bisected the fix to `2ce15e9` by grepping the arm in `2ce15e9~1` (one empty arm) and `2ce15e9` (none). **The first attempt was wrong and is worth recording:** reusing the repository's `CARGO_TARGET_DIR` made the extracted tree fail to compile with `missing field final_url` — cargo served a cached `gamehandler-core` rlib built from a *later* tree, so that error described `5e1485b`, not `35e8085`. A shared target dir is a second way to measure a tree that is not the commit, which is the class this row is about |
| 79 | **The tree's `TODO(T-nn)` tags name tasks that do not own the work — #66's break from the other side, and it runs in both directions.** Architecture audited every `TODO(T-nn)` against its task's declared scope while planning T-29 and found four groups; the tag/site pairs below were re-read here by grep: (1) `main.rs:1127` is tagged **T-10** and is `Library::remove`, which is **P-15** and so belongs to **T-09** (T-10 is P-18…P-31); (2) `main.rs:1134`, `:1136`, `:1332`, `:1337`, `:1345` are tagged **T-11** and are cover fetch/import and the saved-game fetch — **P-24** and **P-31**, both inside T-10's range (T-11 is P-32…P-39); (3) `main.rs:1129` is tagged **T-09** and is the portal file chooser, which is **T-15**'s title verbatim; (4) `main.rs:1374`, `:1377`, `:1383`, `:1389`, `:1393` are tagged **T-13** and are the whole install flow — **P-51…P-59**, i.e. T-12/T-38 (T-13 is P-64…P-68). **#66 found P-items no task named; this is the same break seen from the tags — an agent following the tree's own markers would do the launch work under the form task's name and the cover work under the runners task's**, which is exactly why the rider on T-29's row mattered. The audit's first application was to its own task: **six** sites were retagged `TODO(T-29)` at `c4eced2`, and **`:1127` was deliberately left alone** because retagging it would have recreated the identical defect pointing the other way | **Architecture**, while planning T-29 under that row's mandatory rider — and the rider's premise (*"six sites"*) was itself wrong in the useful direction: there were **seven**, and the seventh is what exposed the rot as bidirectional | **Partial.** The six T-29 sites are retagged and landed (`c4eced2`, comment-only, 6 insertions / 6 deletions, `main.rs` alone, app suite unchanged). The other four groups are **not** retagged, and the reason is a scope decision rather than an oversight: *"does this task's scope contain this work"* is a prose judgement that no mechanical check can make, and Architecture said so rather than shipping a check that pretends. The narrower mechanical form — **every `TODO(T-nn)` names a row that exists in PLAN.md, plus a report of which ids carry TODOs** — is worth having and is the check that would have caught T-38's absence before T-38 existed, so it is filed rather than dropped, as `#70`'s neighbour. It reads PLAN.md (lead's) and edits `verify.sh` (Packaging's), so it needs a three-way | The four groups read out of the tree with `grep -n 'TODO(T-[0-9]*)' crates/app/src/main.rs`, each task's declared scope read from PLAN.md's row for that id, and the retag confirmed at `c4eced2` — no `TODO(T-10)` remains at any launch site, and `main.rs:1127` still reads T-10 as intended |
| 80 | **The dispatch guard cannot see overlay views, and its module header's coverage model does not say so — `view/form.rs` is scanned by no covered set.** The guard covers *pages `main.rs` dispatches to a real body* (`=> view::<module>::view`), and its header's *"What it cannot see"* list closes with *"None of those limits can make this pass when it should fail."* **The form is not a dispatch arm:** `Shell::view_with_overlays` (`main.rs:996`) returns `toaster(...)` over a body that draws `view::form::view` only when `state.game_form` is `Some` (`main.rs:998`), so the form is an *overlay*, and `grep 'form\.rs' tests/dispatch_coverage.rs` returns **nothing**. The consequence is a live hole rather than a hypothetical: **`Message::FetchCoverForForm`, emitted from `form.rs:731` by the Find cover button, has an empty arm at `main.rs:1339`, and the guard says nothing about it** — measured, not inferred from the code's shape. **The page is honest about it, which is why this is a hole and not a shipped defect:** `COVER_FETCH_MISSING` withholds the button while the arm is empty, and `main.rs:3341`'s `the_find_cover_button_is_drawn_iff_its_message_is_handled` pins both directions including a compile-time assertion, so control and arm cannot diverge. That disclosure lives in `main.rs`, outside the guard's model — the property holds today because an author wrote it down, not because a check enforces it | **Advocate**, in the review of T-31; **verified by the lead at HEAD** by reading `view_with_overlays`, the emission site and the empty arm | **Open, and the fix is a design decision rather than a patch.** Two shapes, and the choice belongs with the guard's owner (Packaging): (a) widen the covered set to *every view module `main.rs` names in a rendered position*, which includes the overlay `view_with_overlays` composes and would have caught this; or (b) keep the model page-only and add the limit to the header, so the next author does not read *"cannot make this pass when it should fail"* as covering the overlays. **(a) is the honest fix; (b) is the minimum, because a limit that is stated is not a hole — and the header currently states the opposite** **AMENDED — UX measured the extent and it is larger than `form.rs`: the blind spot is *every view module that is not itself a dispatch arm*, and in this app those are the modules the controls actually live in.** `grep -rn 'widgets' tests/dispatch_coverage.rs` → **0 matches in 964 lines**, and `view/widgets.rs`, `view/badge.rs` and `view/meta.rs` are where the Library's card and row controls are built. So `form.rs` is the *instance* and the shared components are the *class* — which matters for T-29: UX measured that the reference's Play control (both triggers) belongs in `widgets.rs`, so the launch action will be born outside the guard's coverage model unless the covered set is widened first. Option (a) therefore reaches the launch flow, not just the form, and it is now the difference between an inert control being *caught* and being *noticed by someone reading the file* **FIXED (`beaf18f`), and verified by mutation rather than by the commit subject.** Shape (a) was taken: the covered set is now every view module reached from a rendered position, computed **transitively** — a page's view module pulls in the components it calls, so `main.rs` naming is no longer the test. The run prints **11 covered modules**, five of them *reached by call rather than named in `main.rs`*: `widgets`←`library`, `meta`←`widgets`, `cover`←`widgets`, `badge`←`runners`, `metrics`←`badge` — which is the generalisation UX asked for, delivered. The file's false closing sentence (*"None of those limits can make this pass when it should fail"*) is **gone**. **The independent check, because the guard's own test is self-referential:** no shared component constructs a `Message` today, so I injected T-29's actual future case — `Message::LaunchGame` constructed inside `view/widgets.rs`, whose arm is `{}` at `main.rs:1334` — and the guard **failed**, naming the module and the empty arm (suite 5 tests, 1 FAILED). That is the hole this finding predicted, converted into a caught error before the Play control exists. The mutation was then reverted and `cmp`'d byte-identical against `HEAD`'s blob. **`KNOWN_DEAD` was also re-populated** with `FetchCoverForForm`/`"form"` in the same commit, and that is the *better* answer to #78's inert-deferral class: an entry names a **module**, and a new assertion requires that module to be one the guard actually covers ("these KNOWN_DEAD entries name a module this guard does not cover, so they can never defer"), so an entry can no longer be silently inert — the `page`-vs-module mismatch that made `35e8085` red is now a test failure rather than a no-op.| `grep 'form\.rs' tests/dispatch_coverage.rs` → 0 matches; `view_with_overlays` at `main.rs:996` with `view::form::view` at `:998`; `FetchCoverForForm` emitted at `form.rs:731` and its arm empty at `main.rs:1339`; the disclosure test at `main.rs:3341`. **The generalisable part: a guard that defines its universe as "what the dispatcher reaches" cannot see anything the shell composes outside the dispatcher — and in this app that is where the dialogs and overlays live** |

**Why nobody saw it, and this is the durable part.** The working tree has always built, because `http.rs` was on disk as an **untracked** file before `ace60fa`. Every agent's `cargo build` / `cargo test` / `cargo clippy` compiles **the working tree**; none of them compiles **the commit**. So UX observed a green build at `7de961b` — which is only possible in a tree containing `http.rs`, and `http.rs` exists at no revision before `ace60fa`; the file that made those builds green was therefore untracked-on-disk, an inference from those two measurements and stated as one. **A local build is evidence about the working tree, never about the commit**, and that gap is invisible precisely when it is most dangerous: the tree is clean, the suite is green, and the object you are writing the message about is a different object. This is **#50's class in a third medium** — #50 is a stale *binary* under a green count, #53 a silent *write* under "unrepresentable at every call site", this a green *build* of a tree that is not the commit. Every one of them is a check reporting on something other than what it claims.

**The guard gap is measured, not asserted:** `grep -c 'git archive\|git stash\|git worktree\|git checkout' scripts/verify.sh` → **0**. `verify.sh` runs against the working tree, so it **structurally cannot see this class** — no stage builds a commit. That is not a defect to fix (a stage that built a commit would fail on every in-progress tree, which is the normal state here), but it must be stated, because "`verify.sh` passes" is the project's closing criterion and it says nothing whatever about whether the commits it names are buildable. The rule that follows: **to make a claim about a commit, extract it (`git archive <sha> | tar -x`) and build that** — the shape the lead used to get the per-revision table above.

**The class's extent, measured in both directions — and this is the only instance of the forward one.** "Four revisions" from one grep is a count, not an extent, so the sweep was made mechanical: for every commit reachable from HEAD, parse each `.rs` under `crates/*/src` for `mod <name>;`, resolve it the way rustc does (a crate root resolves into its own directory; any other file resolves into a subdirectory named for itself), and compare against the paths actually in the tree. **Forward — a declaration with no file: exactly the four revisions above, out of 180 reachable commits — and `4ea5bfc`'s own message states that denominator while the number at `4ea5bfc` is **182** (`git rev-list --count`: 180 at `8d0c6bc`, 182 at `4ea5bfc`, 184 at `5f4990c`), so the commit that mandates counts carries one from an earlier sweep. **Corrected here rather than by amend**, because D-46 clause 2 makes an amend void the review that just landed on it; the extent itself (four) is unaffected, since the extra commits in the range are the documentation commits themselves. The rule is self-applying in the same way: `5f4990c` says the reverse count "is six now" and at `5f4990c` it is **7**, because the commit is itself a reverse hit — correct at write time and stale the instant it landed, which is the strongest argument available for the rule rather than against the commit.** **Reverse — a file no declaration reaches (#21's class): five revisions**, all of them `view/mod.rs`, `view/cover.rs`, `view/meta.rs` and `view/metrics.rs`, plus `view/widgets.rs` from the second onward. So the forward count is *complete*, not merely accurate; and the reverse result is the sweep's own correctness check — an instrument that found nothing in the direction already known to hold a defect (#21, `9c523d0`-era) would itself be the finding.

**That reverse direction then cost one more measurement, because the fifth commit's subject says it closes #21.** `30830113` reads `D-32: wire \`mod view;\` now behind a dead_code allow T-09 deletes`, and the sweep lists it as *still* orphaned. **It is not a false positive.** `git show --stat 30830113` is **`docs/migration/DECISIONS.md | 55 +`** — one file, documentation only — and `git show 30830113:crates/app/src/main.rs | grep '^mod '` prints **`mod state;`** alone, `mod view;` absent. The wiring landed in **`e75454f`** (`T-07 slice: declare \`mod view;\` under D-32's scoped allow; move \`accent_index\` into core`), a different commit whose message does describe what it does. I checked which of the two was wrong before writing it down — an instrument defect would have been a finding in its own right. The commit was. That gives this class **three distinct forms, all found today, and they need different checks:** #56 is a commit containing **more** than its message says; #60 is the lead **trusting a report** about a commit rather than reading one; and this is a message **claiming a change the tree does not contain**. The first two are caught by `git show --stat <sha>`; the third is not, and needs the tree read or the sweep to run.

**A count from a sweep is a snapshot, not a standing property — and this one was stale within the hour.** The paragraph above states the reverse direction as five revisions; `e98b4b1` made it six while this row was being written (§2, #61). The forward count is unaffected (that commit *added* files, it did not declare them). The rule that follows is the one this file applies to every other number: **re-run the sweep, do not cite its last result** — a count is admissible only as *N at commit X*, which is D-45 and applies to a structural count exactly as it does to a test count.

**And the record-keeping failure, which is the reason this row exists rather than the E0583.** The lead had already messaged Architecture "Leave it out; I am telling them to land the declaration with the file" — a statement resting on UX's report that the declaration was removed before committing. It was never checked. **#54 is the same shape** — a scoping sentence broader than its evidence, filed earlier the same session against the lead, whose corrective lesson was that "'I verified it' is exactly the phrase that should mean the verification covered the claim's scope." It recurred within the hour, in a different file, and was recorded as settled. The advocate's framing is the one to keep: *"If this goes into VERIFY-FINDINGS as 'never committed,' that is the #54 error repeating — an unverified premise recorded as verified."* The inverse also holds and is worth naming: **#43 was a defect that never reached history and was corrected to say so; #60 is a defect that *is* history and must not be read the same way.**

### 2.1 What #32's guards still do not catch

Stated by the author of the primary half, and worth keeping because it bounds
what the three halves above actually establish. **Both source-side tests classify
a page by whether its dispatch arm calls `pending_page`** — Packaging's by parsing
the arm, UX's by checking the drawn strings and the table. So a page "ported" by a
*second placeholder mechanism under another name* still reads as ported to both.
Only the artifact half would notice, and only if the new mechanism's text differs
from the string `verify.sh` greps for — which it would.

The division of labour is nonetheless real and each half covers what the other
cannot:

- **Parsed arm** (Packaging) catches a page whose arm still renders the
  placeholder while the bookkeeping says otherwise. A table-driven test cannot
  catch this, because the table is the thing that agrees with itself.
- **Rendered strings** (UX) catches a table entry claiming a page is done whose
  body does not actually draw real content.
- **Artifact cross-check** (`verify.sh`) catches a shipped binary that disagrees
  with the source about whether any placeholder exists at all — the only half that
  looks inside the artefact the tag ships.

## 3. The hazard: stale test binaries

**This is the highest-risk item for T-19's mutation work.** Concurrent editing
produces *impossible* failures.

`cargo test` reported two failures that vanished on a forced rebuild (`touch` +
retest): `view::cover` tests asserting `Icon` where the code provably returns
`Photo` for a 2-byte fixture, and a `parse_env_block` vector mismatch. In both
cases the fixture on disk was correct, the source was hash-stable across the run,
and the test passed in isolation and after `touch`.

The same family produced `/tmp/wire-test` contamination: a teammate ran
`CARGO_TARGET_DIR=<repo>/target` while building a scratch copy, so a foreign
tree's artifacts — with `/tmp` paths baked into `CARGO_MANIFEST_DIR` — were cached
in the repo and reused.

**Rule.** Before diagnosing a test failure as a defect, confirm the binary is
current: `touch` the source or `cargo clean -p <crate>` and re-run. **A failure
that contradicts the code is more likely a stale artifact than a bug.**

**The rule above guards the false-failure direction. The false-pass direction is
worse and was unguarded — this is #50.** Measured by the advocate, not reasoned:
a test was appended to `installers.rs`, that file's mtime was backdated to
2025-08-07, and `cargo test -p gamehandler` was run.

```
'Compiling gamehandler' present: False
running 147 tests
test result: ok. 147 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
probe test seen in output: False
```

Cargo did not rebuild. It ran the **stale binary** and printed a clean green
summary. The probe test was absent from the run and nothing in the output said
so. **A failure contradiction prompts a check; a green count prompts nothing** —
which is why every count recorded in this project was exposed to this and the
guard, until now, existed only for the direction nobody is tempted to trust.

`rsync -a` preserves mtimes, so this is reachable by the ordinary act of syncing
a scratch tree, and D-33's "test feature unification too" does not cover it: the
mechanism is mtime skew, not build scope.

**Clause, now required of every count reported in this project:** give the count
**with the commit named and `Compiling <crate>` observed** (or state that the
binary was forced with `touch`/`cargo clean -p`). A bare count plus a commit hash
is **two independent things that can disagree silently** — it is not a receipt.

The same lesson landed on the instrument that found it: the advocate's own
printer read `npass[0]`, the *first* `test result: ok` line, rather than the app
harness's, and separately printed an unreproducible `145` against `147/3/2` on
byte-identical files. Both faults were the advocate's, both are reported rather
than smoothed, and both are now fixed in the harness.

Two mechanisms, and they need different handling — this is D-33:

| | stale artifact | real defect |
|---|---|---|
| reproduces after `touch` + rebuild | no | **yes** |
| flips with build scope (`--workspace` vs `-p`) | no | **yes, if feature unification** |

Never answer "stale artifact" without testing "feature unification". A verdict
without its build scope is not evidence.

The related per-mechanism rule (D-41): **"does the target directory matter?" is
not a question with an answer.** It is cache hygiene for feature unification
(Cargo keys by `-C metadata`), and load-bearing for test-binary staleness in a
bin-only crate like `crates/app`. The answerable form names the mechanism:
*matters for what?*

## 4. Open at the time of writing

- **#41 and #42 are closed** (`50798f9`, in the table above). They are named here
  only because this section listed them as open and the listing outlived the fix:
  leaving a closed item in the open list is the same failure as leaving a stale
  instruction in a comment — a reader acting on it looks for a defect that is not
  there. **Section 4 is walked at T-19 and everything in it must be re-verified
  before it is restated**, because this has now happened once.

- **#32 is closed**, and the two questions it carried are answered. The
  before/after pair was captured for both halves. The reachability question —
  `build.sh:36` passes `--force-clean`, so is the artifact branch reachable? —
  was answered correctly by Packaging: **not under a full run**, but reachable via
  `--skip-flatpak`/`--skip-smoke`, which skip the build and still reach the stage
  against whatever `build-flatpak/` an earlier run left. And porting a page is
  exactly the change nothing else in the stage can see, because
  `crates/app/src/main.rs` is in none of the five `FLATPAK_CONTENTS` entries — so
  the byte-identity checks stay green over a stub. The failure text now
  distinguishes the two cases.
- **#28** — nine mutation survivors in `view/widgets.rs`. **CLOSED (`ee18527`)**,
  by rendering rather than by a seam: the builders now destructure their spec
  (`let spec = card_cover_spec(); cover_box(game, spec)`), so nothing restates a
  number, and the tests read what is observable — widget `Id`s through a real
  `Tree`/`Operation` traversal, and the laid-out `layout::Node` — under the
  software renderer, so no display is needed. Two `CoverSpec` readers were
  removed as dead. 26 mutations, and the two behavioural survivors are caught.
  **Verified by the author, not yet independently reproduced by the lead** — the
  94-test run above is the lead's, and it passes, but that is not the same claim.

- **#33 — CLOSED for the variants that exist today; #39 keeps it open for the
  ones T-09…T-15 will add.** `253498d` moved the dispatcher to `Shell` (whose
  `update` a test *can* call, `App` holding a `Core` that only the framework can
  build) and pinned the written handlers in both directions. The four emptiable
  arms are now caught. `#38` (the redundant length assertion) and `#40`
  (`message_changes_state` observing only `State`, so a `Task`-only handler reads
  as a placeholder) were **closed with it**: `observe` now reports a `Debug`-diff
  of `State` **and** `task.units()`, and a mutation making `FetchCover` return a
  task is caught. That last one is #40's inverse — a *written* handler read as a
  placeholder — which is the shape #32 is about, one level down.

  One blind spot was measured rather than declared and is worth keeping: a
  `ToastId` a test can construct cannot name a live toast (`push` returns only
  the expiry `Task`; the slot map and queue are private), so `DismissToast` is
  written but unobservable. `a_test_cannot_observe_which_toast_was_dismissed`
  measures that and goes red if libcosmic ever grows an accessor — which is the
  right shape: the gap is asserted at the point it would close, not papered over.

- **#39 — RE-MEASURED, and the claim of closure is false.** The doc comment on
  `every_message` (`main.rs:1858-1865`) says the length pin means "once the enum
  compiles again that test is red until the new variant is also driven here".
  It is not. Probe, run by the lead against the committed tree: add
  `Message::LeadProbe(String)` to the enum, an arm to `variant_name` and a real
  handler arm to `Shell::update` (both compile-forced — no wildcard in either),
  and deliberately **not** to `every_message`. Result: **94 passed, 0 failed.**
  Nothing anywhere noticed. The pin is `assert_eq!(every_message().len(), 49)`
  and the hand-written list has 49 entries, so a *missing* entry keeps the count
  correct; the pin only catches the other direction (someone appends to the list
  and forgets to bump the number). So a new variant is driven by no test, and
  `only_the_written_handlers_change_anything` cannot see it either — it iterates
  `every_message`, so the variant is outside its universe.

  **This is the one to fix before the pages land, not after.** T-09…T-15 add
  variants; each one added without an `every_message` entry is a handler covered
  by nothing, and the suite stays green while it is. The class-removing fix is a
  single source of truth — declare the enum through a macro that also emits
  `variant_name` and the sample list, so adding a variant is one edit and drift
  is unrepresentable. The cheaper interim is to keep the list and pin its length
  against a constant that is itself compile-forced, which buys a reminder rather
  than a guarantee. Filed to UX; **not yet decided.**
- **#46 is CLOSED** (`7e8f2b3`) — the photo assertion now reads `measure_image`,
  which is the one observable that moves when a decode fails, and `with_photo`
  no longer feeds the branch bytes that cannot decode. The fail direction was
  measured by the lead rather than taken on report: strip `"animated-image"` and
  **exactly two** tests go red, both at the same `assert_decodes` line, with 144
  still green. Kept in this section rather than only in the table because it was
  listed as open here and a closed item left in an open list is the failure this
  section's first bullet is about.
- **#47 is CLOSED** (`e82c153`) — `finish_ok` now refuses to print `ok` unless
  `run_stage` ran a function for that stage, exiting 2. Packaging's reasoning for
  preferring this over widening the exit tail is the part worth keeping, because
  it rules the alternative out rather than merely declining it: `begin` appends to
  `ATTEMPTED` **before** anything runs, so a stage that announced itself and then
  claimed `ok` without running is *in* `ATTEMPTED`, and no widening of an
  `STAGES`-minus-`ATTEMPTED` computation can find it. The exit tail is
  structurally the wrong place for this family. A `PAST` set filled by the
  `finish_*` functions was rejected on the same grounds — it would record "a
  finish was called", not "the stage ran", and would pass
  `begin cli; stage_test; finish_ok` all the same.
- **#48 is CLOSED** (`889f7a5`, follow-up `030ad18`) — Packaging took the
  removal shape: `STAGES` is `name|function|description`, `run_stage` takes only
  the name and reads the function from the entry, and all eleven call sites are
  one argument. Verified by the lead by *running* it rather than reading it, on a
  stub harness where each stage function echoes its own name so the log records
  which one ran: baseline runs `stage_cli`; appending an old-style second
  argument (`run_stage cli stage_test`) still runs `stage_cli` — the table wins
  outright. The stated residual was measured rather than taken: an entry
  deliberately naming another function (`"cli|stage_test|…"`) exits 0 and reports
  `cli` passed, with the log showing `stage_test` ran. **That residual was then
  closed too**, on the lead's argument that a table cannot check its *mapping* but
  can check the **bijection**: `030ad18` asserts every `stage_*` function defined
  in the script is named by exactly one entry, and caught a real bug in its own
  first draft (the helper was named `stage_table_check`, matched its own
  convention, and failed the baseline). The remaining boundary is a *permutation*
  that preserves the bijection — two coordinated edits — which is measured and
  written into the comment rather than left implied.
- **#49 is CLOSED** (`847ec89`) — the ETXTBSY flake. The standing
  write-then-`chmod` hypothesis was **falsified**, not confirmed, and the real
  mechanism measured: a forked child inherits a copy of another test's
  just-written script, and that copy holds the inode's writable reference until
  the child's own `execve`. The refusal is inode-specific (a decoy script
  created before any thread existed records **0** refusals at the same load),
  which is why the textbook temp-then-`rename` fix does **not** work — rename
  carries the inode. The repair is test-side, because the lead verified that
  production `launch.rs` writes no executable at all; and the retry was
  **instrumented to prove it fires** (4 firings in 30 runs) rather than assumed
  live, which is #51's lesson applied proactively. Full detail in the table.
- **#50 is open** — **a stale test binary prints a green count, and `cargo test`
  says nothing.** Found by the advocate while checking the count-reporting rule;
  the measurement and the required clause are in **§3**. Repair belongs to
  Packaging in `scripts/verify.sh`: the `test` stage should either force the
  binary (`touch` the crate sources / `cargo clean -p`) or assert that
  `Compiling gamehandler` appeared in the output, so a run that reused a stale
  artifact cannot report green. **Do not accept a count as evidence until this
  lands** — every count written into PLAN and this file before today was exposed
  to it.
- **N-01** — the no-display panic, assigned to T-08; `smoke-test`'s only failing
  check.
- **`run_stage` → `finish_fail` exits by default: contract, not defect.** Settled
  (D-42). The item as originally written was *substantially false* — it said a
  failing early stage "**silently** skips later ones … while the summary lists
  only the stages that ran". Measured on a stub harness with `clippy` forced to
  fail and every other stage stubbed green: the summary prints

  ```
  failed:  clippy
  did not run (an earlier stage failed): test cli oracle-freshness python-tests
      cargo-sources flatpak-build smoke-test desktop-metainfo flatpak-contents
    these are neither passes nor skips: nothing was verified about them
  ```

  and exits **1**. Every skipped stage is named, the reason is given, and the
  exit code is non-zero. **The same harness run against `50798f9^` produces the
  identical summary**, so this was true *before* the #41 fix as well as after:
  the description was not stale, it was wrong when written. What #41 actually
  repaired was the run that **finishes** with stages unreached (no `STOPPED`),
  which is the opposite path. Kept as a bullet rather than deleted, because the
  next reader of the header's `--keep-going` line deserves the measurement, and
  because "defect or contract?" is answered by running the case, not by taste.
- **Minor:** the `view/cover.rs` fixture helper creates
  `/tmp/gh-cover-{name}-{pid}` per test per run and never removes them — 1245
  directories accumulated in one session.
- **Python flake, verified unreproducible.** One live-repo run reported
  `FAILED (failures=1)`: `test_a_failing_launch_reports_the_runners_own_error`,
  `'cannot find' not found in 'the runner exited with status 53'`. Six
  consecutive `python3 -m unittest discover -t . -q` runs then returned
  `OK (skipped=1)`, and the test passes alone. Not reproducible, so not a task —
  but the reason string varies with a runner's exit status, which is worth
  watching. **If it recurs, capture the run.** Python is the *reference* rather
  than the shipped code beyond this point, so a sporadic failure here affects
  the oracle's authority, not the product.

## 5. The fetch handlers' oracle, measured before the code lands

Pre-measured by the advocate while T-11/T-12 was on hold, then **verified
independently by the lead against the reference**. This is the acceptance
criteria for the fetch path, and it exists because a mocked `HttpClient` cannot
reach any of it — a mock never hangs, never 403s, and never raises an exception
with an empty `str()`.

**The reference does have a timeout.** `_async` (`bridge.py:152-165`) is a bare
daemon `threading.Thread` with none of its own, but the request carries one:
`urlopen(req, timeout=timeout)` (`runners.py:830`) with `fetch_available`'s
default `timeout=30` (`runners.py:818`). The bridge passes only `limit=12`
(`bridge.py:702`), so **30 s is the effective request timeout.** A port with an
unbounded fetch diverges; so does a materially shorter one.

**The error text is non-empty by construction, and the fallback fires on the
commonest failures.** `bridge.py:157`:

```python
message = str(exc) or exc.__class__.__name__
```

The lead re-measured this rather than trusting the summary:

```
ConnectionResetError()            str()=''          -> "ConnectionResetError"
TimeoutError()                    str()=''          -> "TimeoutError"
http.client.RemoteDisconnected()  str()=''          -> "RemoteDisconnected"
socket.timeout()                  str()=''          -> "TimeoutError"
ValueError('bad json')            str()='bad json'  -> "bad json"
```

**Counting basis matters here, and the two counts differ because the words
differ.** By **row**, four of the five have an empty `str()`. By **class**,
three do — `socket.timeout()` *is* `TimeoutError` (an alias since 3.10), so it
renders identically. The advocate said three and the lead's first pass said
four; both are right about their own basis. The invariant is the same either
way and is what a test should assert: **the rendered message is never empty.**
A port that can render an empty string shows the user `"Could not fetch builds
— "` with a dangling dash, and it will do so on exactly the failures that are
most common in the field.

**Three more, all invisible to a mock:**

* `Accept: application/vnd.github+json` and `User-Agent: GameHandler`
  (`runners.py:828`). GitHub's API **rejects a request with no User-Agent**; a
  mock never 403s, so a port that dropped the header would pass every test and
  fail in the field.
* A non-list payload raises `RuntimeError("Unexpected GitHub releases
  response")` (`runners.py:833`) — an explicit sentence to preserve, not a
  parse error to let escape.
* `limit` is **12** as called (`bridge.py:702`) against the function's own
  default of **15** (`runners.py:817`). The port can pass either and look
  right; the reference's behaviour is 12.

**On the staleness guard, already stronger here than in the reference.** The
guard is duplicated across both callbacks in Python (`bridge.py:705`, `:712`).
The committed Rust arm hoists it **above** the `Ok`/`Err` match
(`runners.rs:677-680`), so both halves are covered structurally rather than by
repetition — one place, two outcomes, which is the shape #39 argued for.

## 6. Unmeasured numbers

Commit `09237d9` claims "165 core + 3 app integration tests pass". At that commit
the workspace reports 142 + 3 = 145. **Recorded as an unmeasured number in a
commit message — do not restate it.** The same failure produced D-39's own
correction, where a `--workspace` total was quoted from one binary's line.

| 81 | **The committed tree's clippy gate has been red for 16 commits, and nobody saw it because nobody builds the commit — `#78`'s class, applied to the stage that caught none of it.** `verify.sh:536` runs `cargo clippy --all-targets -- -D warnings` at the workspace root (D-08). **Measured on `ec1ae26` in an isolated `git archive` extraction with its own target dir: EXIT 101, two errors** — `fields netloc, user, port, and path are never read` (`installers.rs:1173-1176`, the `UrlParts` widening landed by `4606d90`, *"widened to five components **for netpaths**"*), and `very complex type used` (`clippy::type_complexity`, `installers.rs:3005`, the 7-tuple × 58-element vector array in `the_url_parser_matches_cpython`). **The working tree is green** (`cargo clippy -p gamehandler-core --all-targets -- -D warnings` → EXIT 0), and the entire difference is the two uncommitted files: `crates/core/src/netpaths.rs` (which reads `netloc` ×3, `user` ×1, `port` ×3, `path` ×4 — the consumer) and `crates/core/src/installers.rs` (which adds `type UrlVector<'a>` and makes `url_parts` `pub(crate)`). **`crates/core/src/netpaths.rs` has never been committed** — `git log --all -- crates/core/src/netpaths.rs` is empty — so by construction **no commit in `4606d90..HEAD` has a reader for those fields, and all 16 of them are clippy-red.** That last inference is a deduction and is marked as one; the two ends are measured. **`cargo test` is NOT red:** the committed tree passes `--workspace` at EXIT 0 with **756 tests**, because rustc reaches `dead_code` as a warning and only clippy's `-D warnings` promotes it — which is why the test stage stayed green and the red lived in one stage only | **UX**, as a finding appended to T-25's report (*"the workspace clippy gate is RED on the committed tree"*), with the owner and the mechanism both named; **verified and widened by the lead**, who reproduced EXIT 101 from the sha rather than from the tree, found the *second* error UX had not reported, and established that `cargo test` is green so the blast radius is clippy alone | **Open, and the fix is already written — it needs landing, not writing.** The consumer exists; it is uncommitted. **Landing `netpaths.rs` + `installers.rs` turns the committed tree green**, and the two must go together: `netpaths` alone clears the `dead_code` error and not `type_complexity`, whose fix is the alias in the same uncommitted `installers.rs`. Owner **Architecture** (both files are theirs; `netpaths` is T-05). **The exposed risk is the repair, not the red:** if `netpaths` lands with a reader *narrower* than the struct, or someone reaches for `#[allow(dead_code)]` or `#[expect]`, the gate goes **quiet** instead of green and this finding recurs silently. The alias is the precedent — it fixes the lint by naming the type, not by suppressing it — and the same standard applies to the four fields: a field the port genuinely does not need is **deleted**, not allowed. **The durable half is the instrument:** D-45 says a green local run is evidence about the working tree, and this is that rule failing for the *clippy* stage the way `#78` had it failing for the test stage — four red commits there, sixteen here, one cause: in a four-agent shared tree the working tree is green whenever someone's in-flight work completes the picture, and no `verify.sh` stage builds a commit  **FIXED (`aa54275`), verified with the same instrument that found it.** Architecture landed `netpaths.rs` (1165 insertions) plus its `lib.rs` declaration and the two `pub(crate)` helpers; the earlier `4606d90` had supplied the widening and `7a077f0` two fidelity fixes. I re-checked rather than accepting the report: `git archive HEAD` into a fresh tree with its own target dir gives **clippy EXIT 0** and **775 tests passed, 0 failed** — the identical isolated-extraction method that produced EXIT 101 before, so the answer does not depend on anyone's target cache. `netpaths.rs` is in the commit and in the archive, so D-49's Hole 1 did not fire on the one file the finding turned on. **Both standards held:** no suppression was added (the `type_complexity` error is fixed by *naming* the type, `type UrlVector<'a>`), and nothing was deleted because **every widened field has a genuine non-test reader** — `netloc` 2, `user` 1, `port` 1, `path` 3, counted in production code. The one `#[allow]` in `installers.rs:1813` predates this work (`5e1485b`). **The differential earned its keep twice and both were the port's own gaps** — a `unreachable_share_message` that trimmed its input *survived*, because `urlsplit` strips leading space itself and only whitespace **inside** the netloc distinguishes the two; and an `is_remote_url` testing `host.is_some()` instead of `bool(netloc)` survived, because `smb://:8080/x` is remote to the reference with `host == None`. Both are now permanent vectors. Measured against the real `gamehandler/netpaths.py` over 55 inputs on one GVFS tree: **55/55 agreed**, imported rather than restated. Mutation **21 caught / 0 survived**; a 22nd mutant is recorded as non-terminating rather than counted as a pass. **The sixteen red commits are history** — the tree is green at HEAD, and the thing that made them red was exactly the file a pathspec commit could have dropped.| `git archive ec1ae26` into `/tmp/redcheck` with `CARGO_TARGET_DIR=/tmp/redcheck-target`: clippy EXIT 101 naming `installers.rs:1173-1176` and `:3005`; `cargo test --workspace` in the same extraction EXIT 0 with 756 tests; `cargo clippy -p gamehandler-core --all-targets -- -D warnings` on the working tree EXIT 0; `git log --all --oneline -- crates/core/src/netpaths.rs` → empty; the four fields' read counts grepped out of the uncommitted `netpaths.rs`; `git log --oneline 4606d90~1..HEAD` → 16 |
| 82 | **T-27's stated subject — “the thirteen toggle labels have no render-level check” — understated the defect and misnamed its cause. The thirteen labels and their thirteen subtitles were compared to **nothing at all**, and the close-on-launch switch was missing one of the reference's two strings on that control.** The bound `main.rs` records is that those strings “rest on the pure functions `the_toggles_are_the_reference_pages_toggles_in_order` and `the_toggle_label_is_the_references_wording`”. Measured: the first parses `key: "…"` out of `SettingsPage.qml`'s `defaultRows` and **stops there** — `the_toggles_are_the_reference_pages_toggles_in_order` collects `key:` only and `assert_eq!`s the list against `table_keys()`, which is `DEFAULT_TOGGLES.iter().map(\|(key, _, _)\| *key)` — so a typo'd label, a dropped subtitle, or two rows' subtitles swapped were invisible to the entire suite; the second exercises `toggle_label` with two strings of its own invention and never asks whether the table holds the reference's words. Separately, `CLOSE_ON_LAUNCH_LABEL` was declared and referenced **nowhere** — not by `view`, not by any test — so the reference's `FormData.label` for that switch (`SettingsPage.qml:121`) was **absent from the page** while the constant sat in the source: a live parity defect, which is #74's shape (a declared thing nothing draws) in a second file. The row's own count of “fourteen” was also short, because it counted the switch as **one** string where `SettingsPage.qml:120-125` puts two on it: `FormData.label` and `text`. **“No render-level check” was also the wrong diagnosis of what to do about it** — see the decision below | UX, building T-27; the label's absence surfaced only because the test written for the *content* question had to name the constant, and it would not compile against a `Toggler`'s label | **`CLOSE_ON_LAUNCH_LABEL` is now passed through `row()`** — the helper the page's three appearance rows already use for a form label — so the reference's two strings are drawn and the label becomes a real `text::body` child; plus **five tests**, all in `view/settings.rs`: `the_toggle_rows_carry_the_references_wording` (key **and** label **and** subtitle, parsed from the QML by `reference_toggle_rows`, equality so a row the port invents fails as loudly as one it drops), `every_toggle_rows_label_is_the_references_own_expression` (the reference's ternary at `SettingsPage.qml:104-106`, both branches, **all thirteen rows** — the older formatting test covers one row of each kind, so an unconditional `format!("{label} — {subtitle}")` passed it while putting a dangling separator on the **eight** subtitle-less rows), `the_close_on_launch_switch_carries_the_references_two_strings`, and two page-level render tests built on a local `drawn_strings` (see the decision) | **Eight mutations run, eight caught; a ninth is enumerated but not performed, and is not counted as a pass.** N1 a label typo → CAUGHT; N2 subtitles swapped between two rows → CAUGHT; N3 a subtitle dropped → CAUGHT; N4 unconditional separator → CAUGHT by **two** tests (the new one plus the old — so the new check subsumes it); N5 the `row()` wiring reverted → CAUGHT **only after the page-level render test was added, and it SURVIVED the table-level suite**; N6 the label constant typo'd → CAUGHT; N8 the explanation also routed through `row()` → CAUGHT; N9 `view` returns an empty `Column` → CAUGHT by two tests (the anti-vacuity half works). **N7 is not simulable**: it would mean giving `Toggler` a child text widget, i.e. editing libcosmic, so the staleness direction that `main.rs`'s pin guards is asserted in the message rather than performed |

### T-27's answer: the thirteen labels do need a second, non-render assertion

T-27 asked whether they do, and the answer is **yes** — but not as a preference, and
the reason matters more than the answer. A render-level check is **impossible** for
these strings, and that is measured rather than argued: the page's own traversal
(`the_toggle_labels_reach_no_text_operation_on_the_real_page`) finds **none** of the
thirteen composed labels while finding `SECTION_APPEARANCE` in the same pass, so the
absence is a property of the `Toggler` and not of an empty page. The choice was never
"a render check or a table check" — it was "a table check or nothing at all".

What the table-level assertions prove, and what they do not, so a later reader does
not over-read them:

- **PROVES** the data the widget is built from is the reference's — key, label and
  subtitle, row for row, in the reference's order — and that `view` builds a `Toggler`
  from every row: the loop over `DEFAULT_TOGGLES` is unconditional, and the render
  tests build the page through `view` rather than through a hand-made widget.
- **DOES NOT PROVE** that a label reaches the pixels. The final hop is `Toggler::draw`'s
  own `iced_widget::text::draw`, which is the instrument's blind spot, and nothing in
  this port can see it. If that hop breaks, the user gets a missing label with every
  test green — #46's shape, on a different widget. The bound is `main.rs`'s
  `the_toggler_labels_do_not_reach_the_text_operation`: it fails if libcosmic ever
  gives `Toggler` a child text widget, and that failure is the signal to move these
  assertions onto the render.

Two consequences worth stating, because both are easy to get backwards:

1. **The count in `main.rs` is now wrong, and UX could not fix it.** Its paragraph says
   "fourteen of this page's strings have no render-level check" and names the two
   tests they "rest on". All three parts are stale: the close-on-launch label is now a
   real `Text` child and observable (so the count is thirteen plus one explanation),
   the two named tests no longer pin the content, and the claim that the thirteen
   "rest on" `the_toggles_are_the_reference_pages_toggles_in_order` **was false when
   written** — that test reads `key:` only. T-27 was scoped away from `main.rs`, which
   Architecture holds for T-29, so the correction is recorded here for whoever next
   holds that file.
2. **One control now sits on both sides of the line, and that is the clearest
   statement of the bound.** `SettingsPage.qml:120-125` puts two strings on the
   close-on-launch switch; the port draws the `FormData.label` through `row()` (so
   `drawn_strings` sees it) and the `text` as the `Toggler`'s own label (so it does
   not). `the_close_on_launch_label_is_drawn_and_its_explanation_is_not_observable`
   asserts both in one test, which is where a reader can see that the difference is
   per-string and mechanical rather than a judgement about importance.

| 83 | **"The doc gate was checking a renamed lint" — a true observation that reads as a false one, and the false reading invites a repair to a gate that is correct.** UX, closing T-27, reported that their `RUSTDOCFLAGS="-D broken_intra_doc_links"` run emitted *"lint `broken_intra_doc_links` has been renamed to `rustdoc::broken_intra_doc_links`"*, and summarised it as the doc gate checking a renamed lint. **The gate is not affected and was never vacuous**, and the difference is one word: a **renamed** lint still fires, a **removed** one does not — `renamed_and_removed_lints` warns about both categories, which is exactly what makes the warning a poor guide here. Measured on an isolated `git archive HEAD` with a deliberately broken link injected into `theme.rs`: **the bare form is EXIT 101** and **the namespaced form is EXIT 101**, both naming `no_such_item_exists_anywhere`. The bare form adds the rename warning and still denies. `verify.sh:563` uses the namespaced form (`-D rustdoc::broken_intra_doc_links`, landed at `0289ffe`), so the gate in the tree is the correct one and needs no change | **UX**, as a defect found in their own working method while closing T-27; **the framing corrected by the lead by measurement**, because the sentence as written would send a reader to fix `verify.sh` | **Not a defect in the tree — nothing was committed off it, and no file needs changing.** UX's *own* command was the affected one and they identified it correctly; only the summary sentence generalised past its evidence. **The durable rule is narrow and worth keeping: a deprecation warning is not an inertness proof, and "renamed" is not "removed".** The related and more useful habit UX did apply is the one that would have caught this unaided — they then **proved the gate can fail** by breaking a link in their own file and getting EXIT 101, rather than trusting a green. That is the check that converts "my command printed a warning" into "my command still fails when it should" | Ran the injected-link experiment in `git archive HEAD` under both flag forms: bare → 101 with the rename warning, namespaced → 101 without it. `verify.sh:563` read to confirm which form ships. **A count checked while doing this, and UX's number was the right one:** the committed tree emits **13** `private_intra_doc_links` warnings — UX said 13, and my first count said 14 because `cargo` also prints the aggregate *"generated 13 warnings"* line, which `grep -c '^warning'` counts. The off-by-one was mine, in a measurement whose only purpose was to check someone else's count |
| 84 | **A Rust snippet in a markdown table cell breaks the row: a literal `\|` is a column separator, and the cell contained two.** `9eaf11d`'s row 82 quoted the key projection as `DEFAULT_TOGGLES.iter().map(\|(key, _, _)\| *key)` — two bare pipes — and a markdown table reads every unescaped pipe as a delimiter, so the row it was written into has **nine** parts where the table declares **seven**. **The class, not the instance, is what this row is for:** a snippet copied from source is exactly the text most likely to contain the one character the container cannot hold, and the failure is silent because the row still renders — markdown publishes the overflow as extra cells, so the prose is on screen and only the column structure is wrong. The same mistake was made twice more in this tree this session: **the lead's own T-24/T-25 PLAN edit** put `state_changed \|\| task_units > 0` into a four-column table, and the same two-character remedy was needed. **Counting is the detector, and it is stronger than grepping:** a grep for `\|` in cell prose matches every row, so it distinguishes nothing, while a **part-count histogram over the table's own rows** is exact — measured before the fix, the findings table had **54 rows at 7 parts and 1 at 9**, so the odd one out is unambiguous and no knowledge of *where* the pipe sits is required. That is the check worth keeping, and it is the reason this was caught at all | **UX**, in the T-27 landing, where it was **committed**; **found by the lead while verifying that landing, by measuring the table instead of reading it** | **FIXED** in the same commit as T-27's landing: the snippet is escaped (`DEFAULT_TOGGLES.iter().map(\|(key, _, _)\| *key)`), the file goes from one malformed row to **none**, and all **55** rows now measure 7 parts. **No content was lost either way**, which is why this is a rendering defect and not a data loss — but the row's columns were shifted, so a reader or a script reading the *fourth* cell of row 82 was reading the wrong field. **The generalisable instrument rule: verify a table by its column count, never by its visible text** — a broken row and a correct row read identically on screen, and the D-45 habit of checking the artefact rather than the claim has a markdown form | `re.split(r'(?<!\\)\|', line)` over every row line of both tables: findings **54×7 + 1×9 → 55×7**, PLAN **39×6**. The first run of this check reported `(433, 9)` and the row was read to find the two pipes rather than guessed at. **A second instrument error, recorded because it is the same shape as the finding:** the lead's first verification pass asserted `body != src` where `src` had already been rebound to the *modified* string, so the assertion could never pass and the edit it was meant to confirm had in fact been written. Fixed by comparing against `git show HEAD:<path>` instead of an in-memory value that the edit itself had overwritten **This row needed the remedy it documents, which is the best evidence the check works:** its first draft carried **four** bare pipes — the two in the snippet it quotes *and* the two it names in prose, `\|` once as "a literal" and once as what a grep would match — for **11** parts instead of 7, and the part-count histogram caught it on the next run with no knowledge of where they were. A row *about* pipes is where this is most likely to recur |